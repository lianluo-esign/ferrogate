{
  "id": "body/payload-too-large",
  "schema": 1,
  "description": "An over-cap body is refused with 413 and the connection closed, and -- because the read happens after authentication -- an unauthenticated over-cap body is still missing_api_key.",
  "world": {
    "config": {
      "providers": [
        {
          "name": "primary",
          "kind": "openai-compatible",
          "base_url": "https://provider.invalid/v1",
          "region": "us-east-1"
        }
      ],
      "models": [
        {
          "name": "fast-chat",
          "provider": "primary",
          "provider_model": "gpt-4o-mini"
        }
      ],
      "api_keys": [
        {
          "id": "key-1",
          "name": "conformance key",
          "key": "secret-1",
          "scopes": ["chat.completions", "responses.create"],
          "organization_id": "tenant-1",
          "project_id": "project-1"
        }
      ]
    }
  },
  "request": {
    "endpoint": "chat.completions",
    "headers": {
      "authorization": "Bearer secret-1"
    },
    "now_unix": 1784937600,
    "body_over_limit": true,
    "body_over_limit_max_bytes": 1048576
  },
  "expect": {
    "schema": 1,
    "outcome": "deny",
    "status": 413,
    "code": "payload_too_large",
    "durable_writes": ["request_log"],
    "audit_events": []
  },
  "worker_shell": {
    "deny_list": [],
    "expect": {
      "schema": 1,
      "outcome": "deny",
      "status": 413,
      "code": "payload_too_large"
    }
  }
}
