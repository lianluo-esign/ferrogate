{
  "name": "@ferrogate/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TESTING.md layer 3 — Playwright black-box HTTP over real `wrangler dev`.",
  "scripts": {
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
