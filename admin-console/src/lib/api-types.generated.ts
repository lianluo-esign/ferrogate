// GENERATED FILE — DO NOT EDIT.
// Source contract: docs/openapi/admin-api.openapi.json (repo root).
// Regenerate with: npm run generate:api   (from admin-console/)
/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/healthz": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Return gateway health. */
        get: operations["getHealthz"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/readyz": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Return gateway readiness. */
        get: operations["getReadyz"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Serve the built-in Admin dashboard HTML. */
        get: operations["getAdminDashboard"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/mcp": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Native MCP JSON-RPC endpoint through FerroGate auth, policy, billing, and audit. */
        post: operations["mcpJsonRpc"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/mcp/identity/{server}/authorize": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Create a subject-bound MCP OAuth authorization-code flow. */
        post: operations["authorizeMcpIdentity"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/mcp/identity/{server}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                server: string;
            };
            cookie?: never;
        };
        /** Get subject-bound MCP identity metadata without token material. */
        get: operations["getMcpIdentity"];
        put?: never;
        post?: never;
        /** Revoke the current subject's MCP identity locally before optional upstream revocation. */
        delete: operations["revokeMcpIdentity"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/mcp/identity/callback": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Consume a one-time OAuth state and persist an encrypted subject-bound credential. */
        get: operations["completeMcpIdentityOauth"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agent-runs": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Opt into a bounded FerroGate agent run without changing default chat/completions behavior. */
        post: operations["createAgentRun"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agent-jobs": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Submit a long-running agent job and get a durable run_id back immediately, without holding the connection open.
         * @description Idempotent submission. The job id is DERIVED from (tenant, idempotency key) -- supply the key via the `Idempotency-Key` header or the `idempotency_key` body field -- so a retried submit addresses the ORIGINAL run and answers 200 with `deduplicated: true` instead of spawning a second run. Without an explicit key the request id is used, which makes each submit its own job. The run row and its runtime dispatch are both durable, so the job survives the request that created it and a restart of the serving component. Concurrency: a tenant may hold at most 200 jobs OPEN -- submitted through this route, not yet acknowledged by the runtime, and whose run has not reached a terminal state -- and a further NEW submit is refused with 429 `agent_job_open_limit_reached` (a retry of an existing idempotency key deduplicates and is never refused). A job stops counting the moment its run SETTLES, by any of: the runtime reporting it finished or failed, the runtime acknowledging the dispatch, or `POST /v1/agent-jobs/{run_id}/cancel`. Settlement is read from the durable run row, so a job settled through another gateway replica releases its slot here too; the OPEN count itself is per-replica, so a cluster of N replicas admits at most N x this limit. Settled runtime rows are reclaimed by worker settlement or acknowledgement, a peer refusing settled work, or a retried cancel. An unleased cancel first withdraws only the serving node's local copy, then reclaims the shared StartRun row as settled work; the terminal run row prevents peers from leasing copies they restored earlier. Runs started by a schedule do not consume this budget.
         */
        post: operations["submitAgentJob"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agent-jobs/{run_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Durable agent job id returned by submitAgentJob (the agent run id). */
                run_id: string;
            };
            cookie?: never;
        };
        /**
         * Observe an async agent job's status. Tenant-isolated: another tenant's run_id is reported as not found.
         * @description Requires agent.runs.read; a key carrying agent.runs.create (the submit scope) is also accepted, so a submitter can always observe its own job.
         */
        get: operations["getAgentJob"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agent-jobs/{run_id}/events": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Durable agent job id returned by submitAgentJob (the agent run id). */
                run_id: string;
            };
            cookie?: never;
        };
        /**
         * Read an agent job's incremental events from the existing agent-run timeline, resumable by a position cursor that survives retention.
         * @description Requires agent.runs.read; a key carrying agent.runs.create (the submit scope) is also accepted, so a submitter can always observe its own job.
         */
        get: operations["listAgentJobEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agent-jobs/{run_id}/result": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Durable agent job id returned by submitAgentJob (the agent run id). */
                run_id: string;
            };
            cookie?: never;
        };
        /**
         * Collect a terminal agent job's result: the runtime-reported output carried on the run timeline plus its artifact evidence.
         * @description Requires agent.runs.read; a key carrying agent.runs.create (the submit scope) is also accepted, so a submitter can always observe its own job.
         */
        get: operations["getAgentJobResult"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agent-jobs/{run_id}/cancel": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Durable agent job id returned by submitAgentJob (the agent run id). */
                run_id: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Cancel an in-flight agent job: withdraws local unleased work or dispatches cancel_run, and terminalizes the run. */
        post: operations["cancelAgentJob"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/mcp/tool/execute": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Execute a namespaced MCP tool through gateway auth, policy, billing, and observability. */
        post: operations["executeMcpTool"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/overview": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Scoped control-plane overview: inventory counts, durable token totals, and bounded critical alerts.
         * @description Returns the dashboard's global control-plane overview in one bounded request. Counts and token totals are produced by bounded repository aggregation (COUNT/SUM pushdown on Postgres, in-process fold elsewhere), never by loading whole collections. A platform-operator key sees every tenant; a tenant-scoped key is confined to its own tenant. Each section carries an explicit available/unavailable state so a storage failure is reported honestly instead of as a fabricated zero. The `control_plane` section's `data` object (present only when the section is `ok`) additionally carries the issue #458 breakdowns: `virtual_keys` as a `{total,enabled}` split (disabled is `total-enabled`); `assets.referenced`/`assets.unreferenced` (channel-pinned vs not) and `assets.storage_quota_bytes` (the applicable per-scope asset-storage quota override, `null` for the global view or a tenant with no override, never a fabricated global aggregate); `pending_tool_approvals` (a scoped count, with a bounded `tool_approvals_pending` alert); a bounded, nearest-to-cap-first `quota_pressure` list of tenant scopes at/over 80% of a budget or asset-storage cap (with a bounded `quota_pressure` alert); and `policy_governance` (guardrail-revision/binding, quota-policy, and policy-rule counts) which is intentionally global-only -- serialized as `null` for a tenant-scoped key because guardrail revisions carry no tenant column and the quota/policy tables are not per-tenant attributable without a join. Every #458 field uses a nullable/not-applicable representation rather than a fake zero, and a durable failure nulls the whole `control_plane` section instead of zeroing any breakdown.
         */
        get: operations["getAdminOverview"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/status": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Return gateway status and configured resource counts. */
        get: operations["getAdminStatus"];
        put?: never;
        /** Register a self-hosted worker in durable control-plane storage. */
        post: operations["registerAdminSelfHostedWorker"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/status": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Compatibility alias for /admin/v1/status. */
        get: operations["getAdminStatusAlias"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/providers": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List configured providers without exposing provider secrets. */
        get: operations["listAdminProviders"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/provider-health": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List provider health and circuit-breaker status. */
        get: operations["listAdminProviderHealth"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/provider-models": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List read-only upstream provider model catalog candidates without exposing provider secrets. */
        get: operations["listAdminProviderModels"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/provider-credentials": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List this tenant's BYOK provider credentials, redacted.
         * @description Every alias the calling tenant owns, live and revoked. A platform-operator credential is refused with 403 rather than defaulting to some tenant.
         */
        get: operations["listProviderCredentials"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/provider-credentials/{alias}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description BYOK alias, held to the same grammar the `byok://` reference parser enforces (lowercase alphanumerics plus . _ -, no path separators, 1-64 characters). A segment outside it is 400 `invalid_request_body`, never a lookup. */
                alias: string;
            };
            cookie?: never;
        };
        get?: never;
        /**
         * Register or rotate the BYOK credential behind one alias.
         * @description Idempotent by alias: the first call registers, every later call rotates in a single request. The response never contains the submitted key.
         */
        put: operations["putProviderCredential"];
        post?: never;
        /**
         * Revoke the BYOK credential behind one alias.
         * @description Marks the alias revoked. An alias this tenant does not own answers the same 404 as one that never existed, so it is not a cross-tenant oracle.
         */
        delete: operations["revokeProviderCredential"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-workers/{id}/heartbeat": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Record a self-hosted worker heartbeat in durable control-plane storage. */
        post: operations["recordAdminSelfHostedWorkerHeartbeat"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-workers/{id}/events": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Read a self-hosted worker telemetry event stream page with optional event-id cursor replay. */
        get: operations["listAdminSelfHostedWorkerTelemetryEvents"];
        put?: never;
        /** Record a self-hosted worker telemetry event in durable control-plane storage. */
        post: operations["recordAdminSelfHostedWorkerTelemetryEvent"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/managed-workers": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List managed worker runtime contracts and agent-worker process boundary evidence. */
        get: operations["listAdminManagedWorkers"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/framework-adapters": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List framework adapter contracts and implementation status for managed and self-hosted worker modes. */
        get: operations["listAdminFrameworkAdapters"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-workers": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List self-hosted worker runtime contract and registration/persistence implementation status. */
        get: operations["listAdminSelfHostedWorkers"];
        put?: never;
        /** Register a self-hosted worker identity. */
        post: operations["registerSelfHostedWorker"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-workers/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Get one persisted self-hosted worker record by id. */
        get: operations["getAdminSelfHostedWorker"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-worker-records": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List persisted self-hosted worker records without fabricating registrations. */
        get: operations["listAdminSelfHostedWorkerRecords"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/observability": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List configured observability exporters and health counters. */
        get: operations["listAdminObservability"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/extensions": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List configured gateway plugins through the legacy extensions alias without exposing plugin config. */
        get: operations["listAdminExtensions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/plugins": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List configured gateway plugins without exposing plugin config. */
        get: operations["listAdminPlugins"];
        put?: never;
        /** Create or replace a configured gateway plugin and persist the control-plane record. */
        post: operations["createAdminPlugin"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/plugins/{plugin_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Gateway plugin id. */
                plugin_id: components["parameters"]["PluginId"];
            };
            cookie?: never;
        };
        /** Inspect one gateway plugin capability bundle without exposing plugin config. */
        get: operations["getAdminPlugin"];
        put?: never;
        post?: never;
        /** Delete a configured gateway plugin from the control plane. */
        delete: operations["deleteAdminPlugin"];
        options?: never;
        head?: never;
        /** Update a configured gateway plugin and persist the control-plane record. */
        patch: operations["updateAdminPlugin"];
        trace?: never;
    };
    "/admin/v1/plugins/{plugin_id}/tools": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Gateway plugin id. */
                plugin_id: components["parameters"]["PluginId"];
            };
            cookie?: never;
        };
        /** List executable tools registered by one gateway plugin without exposing provider secrets. */
        get: operations["listAdminPluginTools"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tools": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List registered plugin tools without exposing provider secrets or plugin config. */
        get: operations["listAdminTools"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tool-approvals": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List human-in-the-loop tool approval records. */
        get: operations["listAdminToolApprovals"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tool-approvals/{approval_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Tool approval id. */
                approval_id: string;
            };
            cookie?: never;
        };
        /** Inspect a tool approval record without exposing raw arguments. */
        get: operations["getAdminToolApproval"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tool-approvals/{approval_id}/approve": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Tool approval id. */
                approval_id: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Approve a pending tool call if its immutable action fingerprint still matches. */
        post: operations["approveAdminToolApproval"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tool-approvals/{approval_id}/deny": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Tool approval id. */
                approval_id: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Deny a pending tool approval and fail the waiting call closed. */
        post: operations["denyAdminToolApproval"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tool-approvals/{approval_id}/expire": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Tool approval id. */
                approval_id: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Expire a pending tool approval and fail the waiting call closed. */
        post: operations["expireAdminToolApproval"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/mcp-servers": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List MCP server session health and discovered tool counts. */
        get: operations["listAdminMcpServers"];
        put?: never;
        /** Create an MCP server registration and apply it through process-local reload. */
        post: operations["createAdminMcpServer"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/mcp-servers/{name}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable MCP server name. */
                name: string;
            };
            cookie?: never;
        };
        /** Get one MCP server status and discovered tool count. */
        get: operations["getAdminMcpServer"];
        /** Replace MCP server metadata or reconnect settings while preserving durable registration history. */
        put: operations["putAdminMcpServer"];
        post?: never;
        /** Delete an MCP server registration without deleting its session evidence. */
        delete: operations["deleteAdminMcpServer"];
        options?: never;
        head?: never;
        /** Update MCP server metadata or reconnect settings while preserving history. */
        patch: operations["patchAdminMcpServer"];
        trace?: never;
    };
    "/admin/v1/tool-sessions/{session_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Caller-supplied tool session identifier. */
                session_id: string;
            };
            cookie?: never;
        };
        /** List tool execution audit events grouped by caller-supplied tool session. */
        get: operations["listAdminToolSessionEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/models": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List configured logical models.
         * @description Lists the logical models visible to the caller. Scoped to the caller (issue #535): a platform-operator key receives every model verbatim; a tenant-scoped admin.read key receives only the models its tenant may actually call, matching the visibility gate GET /v1/models and the invocation path already apply (can_tenant_use_model). An empty visible_organization_ids/visible_project_ids is a wildcard, so an unrestricted model is returned unchanged; a non-empty list is an allow-list, so the model is hidden unless it names the caller's organization or one of its projects, and the two lists are then rendered with only the caller's own ids. The lists themselves are other tenants' identifiers, which is why they are narrowed rather than merely filtered on.
         */
        get: operations["listAdminModels"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/api-keys": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List configured API keys with secret material redacted. */
        get: operations["listAdminApiKeys"];
        put?: never;
        /** Create an API key and apply it through process-local reload. */
        post: operations["createAdminApiKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/api-keys/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["ApiKeyId"];
            };
            cookie?: never;
        };
        /** Get one API key with secret material redacted. */
        get: operations["getAdminApiKey"];
        /** Replace or create an API key by id. */
        put: operations["putAdminApiKey"];
        post?: never;
        /** Delete an API key and apply the change through process-local reload. */
        delete: operations["deleteAdminApiKey"];
        options?: never;
        head?: never;
        /** Upsert an API key by id. */
        patch: operations["patchAdminApiKey"];
        trace?: never;
    };
    "/admin/v1/tenants": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List tenant references derived from API key configuration. */
        get: operations["listAdminTenants"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/policies": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List configured policy rules.
         * @description Lists the policy rules visible to the caller. Scoped to the caller (issue #535): a platform-operator key receives every rule verbatim; a tenant-scoped admin.read key receives only the rules that can act on it, with other tenants' ids removed. The selector semantics match the runtime's own: an empty organization_ids/project_ids/api_key_ids is a wildcard, so a rule with no selectors at all is genuinely platform-wide and is returned unchanged; a non-empty selector is an allow-list, so the rule is hidden unless it names an organization, project or api key the caller owns, and is then rendered with only the caller's own ids. Policy rules are deliberately not public-to-tenants: they carry other tenants' organization, project and api-key ids together with the models and providers each is denied.
         */
        get: operations["listAdminPolicies"];
        put?: never;
        /** Create a policy rule and apply it through process-local reload. */
        post: operations["createAdminPolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/policies/{name}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                name: components["parameters"]["PolicyName"];
            };
            cookie?: never;
        };
        /**
         * Get one policy rule.
         * @description Returns one policy rule, scoped exactly as GET /admin/v1/policies is (issue #535) and rendered with only the caller's own organization, project and api-key ids. A tenant-scoped admin.read key asking for a rule outside its own slice receives 403 tenant_scope_denied -- the identical answer it receives for a name that does not exist, so this endpoint cannot be walked as an existence oracle to rebuild the list. A platform-operator key still receives 404 policy_not_found for a genuinely absent name.
         */
        get: operations["getAdminPolicy"];
        /** Replace or create a policy by name. */
        put: operations["putAdminPolicy"];
        post?: never;
        /** Delete a policy rule and apply the change through process-local reload. */
        delete: operations["deleteAdminPolicy"];
        options?: never;
        head?: never;
        /** Upsert a policy by name. */
        patch: operations["patchAdminPolicy"];
        trace?: never;
    };
    "/admin/v1/request-logs": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List retained request logs. */
        get: operations["listAdminRequestLogs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/agent-runs": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List retained agent run evidence aggregated from request logs, audit events, and metering events. */
        get: operations["listAdminAgentRuns"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/agent-runs/{run_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Agent run id from x-ferrogate-agent-run-id or the gateway-generated run id. */
                run_id: string;
            };
            cookie?: never;
        };
        /** Inspect one agent run timeline without exposing raw prompt or tool bodies unless existing recording policy allowed them. */
        get: operations["getAdminAgentRunTimeline"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-runs/{run_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Self-hosted run id reported by a customer-owned worker. */
                run_id: string;
            };
            cookie?: never;
        };
        /** Inspect one self-hosted worker reported run timeline with explicit customer-reported trust marking. */
        get: operations["getAdminSelfHostedRunTimeline"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/request-log-exports": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Export retained request logs as redacted JSONL for offline evaluation workflows. */
        get: operations["exportAdminRequestLogsJsonl"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/billing-events": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Compatibility alias for /admin/v1/metering-events.
         * @deprecated
         */
        get: operations["listAdminBillingEventsCompat"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/metering-export-status": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List recent external metering export outcomes.
         * @description Shows recent non-blocking external billing export successes and failures. Gateway AI responses are not failed by export errors unless a future strict mode is added.
         */
        get: operations["listAdminMeteringExportStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/usage-aggregates": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List in-memory token usage aggregates by tenant/model/provider. */
        get: operations["listAdminUsageAggregates"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/audit-events": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List retained Admin API audit events. */
        get: operations["listAdminAuditEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/guardrail-evaluations": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List sanitized overall and per-check Guardrail evaluation evidence. */
        get: operations["listGuardrailEvaluations"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/investigations": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Join identity, route, policy, Guardrail, approval, execution, usage, cost, and outcome evidence. */
        get: operations["getGuardrailInvestigation"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/config/validate": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Validate a candidate TOML, Caddyfile, or source-file config without committing it. */
        post: operations["validateAdminConfig"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/config/reload": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Validate and apply a process-local config reload when the candidate can be committed without listener replacement. */
        post: operations["reloadAdminConfig"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/drain": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Return the node drain status. */
        get: operations["getAdminDrain"];
        put?: never;
        /** Set or clear node drain mode for rolling maintenance. */
        post: operations["setAdminDrain"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/metrics": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Return Prometheus text metrics. */
        get: operations["getMetrics"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/metering-events": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List retained token metering events. */
        get: operations["listAdminMeteringEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/gateway-configs": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List configured reusable gateway config profiles.
         * @description Lists the reusable gateway config profiles visible to the caller. Scoped to the caller (issue #535): a platform-operator key receives every profile verbatim; a tenant-scoped admin.read key receives only the profiles one of its own api keys may actually select -- those whose api_key_ids is empty (usable by any key, exactly as profile resolution treats it) or names a key the caller owns -- and api_key_ids is rendered with only the caller's own key ids.
         */
        get: operations["listAdminGatewayConfigs"];
        put?: never;
        /** Create a reusable gateway config profile and apply it through process-local reload. */
        post: operations["createAdminGatewayConfig"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/gateway-configs/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["GatewayConfigId"];
            };
            cookie?: never;
        };
        /**
         * Get one reusable gateway config profile.
         * @description Returns one reusable gateway config profile, scoped exactly as GET /admin/v1/gateway-configs is (issue #535), with api_key_ids rendered as only the caller's own key ids. A tenant-scoped admin.read key asking for a profile none of its keys may select receives 403 tenant_scope_denied -- the identical answer it receives for an id that does not exist. A platform-operator key still receives 404 gateway_config_not_found for a genuinely absent id.
         */
        get: operations["getAdminGatewayConfig"];
        /** Replace or create a reusable gateway config profile by id. */
        put: operations["putAdminGatewayConfig"];
        post?: never;
        /** Delete a reusable gateway config profile and apply the change through process-local reload. */
        delete: operations["deleteAdminGatewayConfig"];
        options?: never;
        head?: never;
        /** Upsert a reusable gateway config profile by id. */
        patch: operations["patchAdminGatewayConfig"];
        trace?: never;
    };
    "/admin/v1/agent-workflows": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List configured agent workflow policies with runtime counters.
         * @description Lists the agent workflow policies visible to the caller, each with its runtime counters. Scoped to the caller (issue #546, the same narrowing as issue #535): a platform-operator key receives every workflow verbatim; a tenant-scoped admin.read key receives only the workflows it could actually invoke, with other tenants' ids removed. The selector semantics match the runtime's own can_use_workflow check: an empty organization_ids/project_ids/api_key_ids is a wildcard, so a workflow with no selectors at all is genuinely platform-wide and is returned unchanged; a non-empty selector is an allow-list, so the workflow is hidden unless it names an organization, project or api key the caller owns, and is then rendered with only the caller's own ids -- a selector that would narrow to nothing hides the workflow rather than being emptied, because an empty selector reads as a wildcard. The counters are not narrowed: they are aggregate request, error, billing and audit totals for the workflow id and version, not tenant identifiers.
         */
        get: operations["listAdminAgentWorkflows"];
        put?: never;
        /** Create an agent workflow policy and apply it through process-local reload. */
        post: operations["createAdminAgentWorkflow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/agent-workflows/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["AgentWorkflowId"];
            };
            cookie?: never;
        };
        /**
         * Get one agent workflow policy with runtime counters.
         * @description Returns one agent workflow policy with its runtime counters, scoped exactly as GET /admin/v1/agent-workflows is (issue #546) and rendered with only the caller's own organization, project and api-key ids. A tenant-scoped admin.read key asking for a workflow it could not invoke receives 403 tenant_scope_denied -- the identical answer it receives for an id that does not exist, so this endpoint cannot be walked as an existence oracle to rebuild the list. A platform-operator key still receives 404 agent_workflow_not_found for a genuinely absent id.
         */
        get: operations["getAdminAgentWorkflow"];
        /** Replace or create an agent workflow policy by id. */
        put: operations["putAdminAgentWorkflow"];
        post?: never;
        /** Delete an agent workflow policy and apply the change through process-local reload. */
        delete: operations["deleteAdminAgentWorkflow"];
        options?: never;
        head?: never;
        /** Upsert an agent workflow policy by id. */
        patch: operations["patchAdminAgentWorkflow"];
        trace?: never;
    };
    "/admin/v1/agent-schedules": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List time-based agent schedules (tenant/workspace scoped). */
        get: operations["listAdminAgentSchedules"];
        put?: never;
        /** Create a time-based agent schedule. */
        post: operations["createAdminAgentSchedule"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/agent-schedules/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        /** Get one agent schedule by id. */
        get: operations["getAdminAgentSchedule"];
        /** Replace an agent schedule by id. */
        put: operations["putAdminAgentSchedule"];
        post?: never;
        /** Delete an agent schedule by id. */
        delete: operations["deleteAdminAgentSchedule"];
        options?: never;
        head?: never;
        /** Partially update an agent schedule by id (e.g. pause/resume via enabled). */
        patch: operations["patchAdminAgentSchedule"];
        trace?: never;
    };
    "/admin/v1/agent-schedules/{id}/run-now": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Trigger an agent schedule immediately, regardless of its cadence. */
        post: operations["runAdminAgentScheduleNow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/agent-schedules/{id}/fires": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        /** List the fire history (newest first) for an agent schedule. */
        get: operations["listAdminAgentScheduleFires"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/prompt-templates": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List versioned prompt templates. */
        get: operations["listAdminPromptTemplates"];
        put?: never;
        /** Create a versioned prompt template and apply it through process-local reload. */
        post: operations["createAdminPromptTemplate"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/prompt-templates/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        /** Get one prompt template with inspectable versions. */
        get: operations["getAdminPromptTemplate"];
        /** Replace prompt template metadata or append supplied versions. */
        put: operations["putAdminPromptTemplate"];
        post?: never;
        /** Archive a prompt template without deleting version history. */
        delete: operations["archiveAdminPromptTemplate"];
        options?: never;
        head?: never;
        /** Update prompt template metadata or append a new version while preserving history. */
        patch: operations["patchAdminPromptTemplate"];
        trace?: never;
    };
    "/admin/v1/prompt-templates/{id}/labels": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        /**
         * List every deployment label on one prompt template.
         * @description Read from the document store, not from KV: a KV prefix scan is eventually consistent and would show an operator a stale view of a change they just made.
         */
        get: operations["listAdminPromptTemplateLabels"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/prompt-templates/{id}/labels/{label}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
                /** @description Deployment label name, URL-decoded and normalized before use. An illegal name is 400 `invalid_prompt_label` — an operator who typed `Production!` needs to be told the name is illegal, not sent looking for a missing resource. */
                label: string;
            };
            cookie?: never;
        };
        get?: never;
        /**
         * Point a deployment label at a prompt template revision.
         * @description Upsert. This is the call that replaces a deploy. The admin document is written first and the gateway's KV pointer second, so a crash between the legs leaves traffic on the previous revision and the next PUT heals it.
         */
        put: operations["putAdminPromptTemplateLabel"];
        post?: never;
        /**
         * Retire a deployment label.
         * @description Pointer first, document second — the mirror of the PUT ordering: a crash between the legs leaves a recorded label that no longer resolves at the edge, which fails loudly on the next request that names it.
         */
        delete: operations["deleteAdminPromptTemplateLabel"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/prompts/{id}/render": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Render a prompt template into an OpenAI-compatible request body.
         * @description Requires prompts.render scope. Render audit events record template id, revision, API key, and request id without raw variable values.
         */
        post: operations["renderPromptTemplate"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/skills": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List enabled skill packages visible to the authenticated agent client. */
        get: operations["listAgentSkills"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/skills/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Get one enabled skill package visible to the authenticated agent client. */
        get: operations["getAgentSkill"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/agent-upstreams": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List registered agent upstreams.
         * @description Lists the agent upstreams visible to the caller. Scoped to the caller (issue #535): a platform-operator key receives every upstream verbatim; a tenant-scoped admin.read key receives only the upstreams one of its own api keys may reach, matching the gate the data-plane paths (GET /v1/agents and agent invoke) already apply. Note tenant_ids is matched against the caller's API KEY id, not its organization id: an empty list is a wildcard (any key may reach the upstream) and a non-empty list is an api-key allow-list, rendered with only the caller's own key ids.
         */
        get: operations["listAdminAgentUpstreams"];
        put?: never;
        /** Register an agent upstream. */
        post: operations["createAdminAgentUpstream"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/agent-upstreams/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Get one registered agent upstream.
         * @description Returns one agent upstream, scoped exactly as GET /admin/v1/agent-upstreams is (issue #535), with tenant_ids rendered as only the caller's own api-key ids. A tenant-scoped admin.read key asking for an upstream none of its keys may reach receives 403 tenant_scope_denied -- the identical answer it receives for an id that does not exist, so this endpoint cannot be walked as an existence oracle to rebuild the list. A platform-operator key still receives 404 agent_upstream_not_found for a genuinely absent id.
         */
        get: operations["getAdminAgentUpstream"];
        /** Replace an agent upstream. */
        put: operations["putAdminAgentUpstream"];
        post?: never;
        /** Delete an agent upstream. */
        delete: operations["deleteAdminAgentUpstream"];
        options?: never;
        head?: never;
        /** Update an agent upstream. */
        patch: operations["patchAdminAgentUpstream"];
        trace?: never;
    };
    "/.well-known/agent.json": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List enabled agent upstreams visible to the authenticated agent client. */
        get: operations["getAgentDiscovery"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/skill-packages": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List registered skill packages.
         * @description Lists the skill packages visible to the caller. Scoped to the caller (issue #535): a platform-operator key receives every package verbatim; a tenant-scoped admin.read key receives only the packages one of its own api keys may load -- those whose api_key_ids is empty (loadable by any key, exactly as agent-facing discovery treats it) or names a key the caller owns -- and api_key_ids is rendered with only the caller's own key ids. Unlike agent-facing discovery this listing still includes disabled packages, because enabled is an operator-authored flag rather than a cross-tenant identifier.
         */
        get: operations["listAdminSkillPackages"];
        put?: never;
        /** Register a skill package. */
        post: operations["createAdminSkillPackage"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/skill-packages/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Get one registered skill package.
         * @description Returns one skill package, scoped exactly as GET /admin/v1/skill-packages is (issue #535), with api_key_ids rendered as only the caller's own key ids. A tenant-scoped admin.read key asking for a package none of its keys may load receives 403 tenant_scope_denied -- the identical answer it receives for an id that does not exist. A platform-operator key still receives 404 skill_package_not_found for a genuinely absent id.
         */
        get: operations["getAdminSkillPackage"];
        /** Replace a skill package. */
        put: operations["putAdminSkillPackage"];
        post?: never;
        /** Delete a skill package. */
        delete: operations["deleteAdminSkillPackage"];
        options?: never;
        head?: never;
        /** Update a skill package. */
        patch: operations["patchAdminSkillPackage"];
        trace?: never;
    };
    "/admin/v1/managed-worker-sessions": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List persisted managed worker sessions and lifecycle events without fabricating sessions. */
        get: operations["listAdminManagedWorkerSessions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-workers/{id}/artifacts": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Record self-hosted worker artifact metadata in durable control-plane storage. */
        post: operations["recordAdminSelfHostedWorkerArtifact"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-workers/{id}/checkpoints": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Record self-hosted worker checkpoint metadata in durable control-plane storage. */
        post: operations["recordAdminSelfHostedWorkerCheckpoint"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/guardrail-policies": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List immutable Guardrail policy revisions and their draft, active, or archived state.
         * @description Requires admin.read plus the Supabase-backed guardrails.policy.read action. Tenant roles see only revisions explicitly scoped to their own tenant.
         */
        get: operations["listGuardrailPolicyRevisions"];
        put?: never;
        /**
         * Create the first immutable revision of a Guardrail policy.
         * @description Requires admin.write plus the Supabase-backed guardrails.policy.create_revision action. Platform-operator keys remain the RBAC bootstrap path. Server assigns revision, created_at_unix, and created_by.
         */
        post: operations["createGuardrailPolicyRevision"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/guardrail-policies/{policy_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List every immutable revision for one Guardrail policy.
         * @description Requires admin.read plus the Supabase-backed guardrails.policy.read action. Tenant roles may only read their own tenant-scoped policies.
         */
        get: operations["listGuardrailPolicyRevisionsByPolicy"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/guardrail-policies/{policy_id}/revisions": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List Guardrail policy revision history.
         * @description Requires admin.read plus the Supabase-backed guardrails.policy.read action. Tenant roles may only read their own tenant-scoped policies.
         */
        get: operations["listGuardrailPolicyRevisionHistory"];
        put?: never;
        /**
         * Create the next immutable revision for a Guardrail policy.
         * @description Requires admin.write plus the Supabase-backed guardrails.policy.create_revision action. Tenant roles may only create revisions explicitly scoped to their own tenant. The policy_id may be omitted from the body; if supplied it must match the path.
         */
        post: operations["createNextGuardrailPolicyRevision"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/guardrail-policies/{policy_id}/revisions/{revision}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Get an exact immutable Guardrail policy revision.
         * @description Requires admin.read plus the Supabase-backed guardrails.policy.read action. Tenant roles may only read their own tenant-scoped revisions.
         */
        get: operations["getGuardrailPolicyRevision"];
        put?: never;
        post?: never;
        /**
         * Archive an inactive Guardrail policy revision.
         * @description Requires admin.write plus the Supabase-backed guardrails.policy.archive action. Tenant roles may only archive their own tenant-scoped revisions. An active revision cannot be archived.
         */
        delete: operations["archiveGuardrailPolicyRevision"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/guardrail-policies/{policy_id}/activate": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Atomically activate a validated Guardrail policy revision and reload the runtime.
         * @description Requires admin.write plus the Supabase-backed guardrails.policy.activate action. Tenant roles may only activate their own tenant-scoped revisions. The previous active revision is archived. A failed runtime reload restores the prior binding.
         */
        post: operations["activateGuardrailPolicyRevision"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/guardrail-policies/{policy_id}/rollback": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Atomically roll back to an archived Guardrail policy revision.
         * @description Requires admin.write plus the Supabase-backed guardrails.policy.rollback action. Tenant roles may only roll back their own tenant-scoped revisions. Omitting revision selects the highest archived revision.
         */
        post: operations["rollbackGuardrailPolicyRevision"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/guardrail-policies/{policy_id}/dry-run": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Select and inspect a Guardrail policy revision without dispatching a model provider, external detector, or action.
         * @description Requires admin.read plus the Supabase-backed guardrails.policy.dry_run action. Tenant roles may only dry-run their own tenant-scoped revisions, and the organization context is pinned to the caller's tenant.
         */
        post: operations["dryRunGuardrailPolicyRevision"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/self-hosted-workers/{id}/rotate": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Rotate the durable identity fingerprint for a registered self-hosted worker.
         * @description Updates the stored identity fingerprint projection. This does not implement final mTLS transport token rotation or secret issuance.
         */
        post: operations["rotateAdminSelfHostedWorkerIdentity"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Serve the Admin dashboard alias. */
        get: operations["getAdminDashboardSlash"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/dashboard": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Serve the Admin dashboard alias. */
        get: operations["getAdminDashboardAlias"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/models": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List enabled models. */
        get: operations["listModels"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/models/{model}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                model: string;
            };
            cookie?: never;
        };
        /** Describe one enabled model. */
        get: operations["getModel"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/chat/completions": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Create an OpenAI-compatible chat completion. */
        post: operations["createChatCompletion"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/responses": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Create an OpenAI-compatible response. */
        post: operations["createResponse"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/embeddings": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Create OpenAI-compatible embeddings for one or more inputs. */
        post: operations["createEmbedding"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/rerank": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Rerank documents against a query, served by Workers AI reranker models. */
        post: operations["createRerank"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/messages": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Create an Anthropic-native Messages completion (streaming or non-streaming). */
        post: operations["createMessage"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/messages/count_tokens": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Count the input tokens an Anthropic-native Messages request would be charged, without dispatching it.
         * @description Returns the same prompt-token number the gateway reserves against the caller's token-per-minute window and monthly budget when the identical body is sent to POST /v1/messages, so a client can size a context window or pre-estimate cost without paying for a completion. No provider is contacted. The operation carries the same bearer scope as createMessage (messages.create): it is not an unauthenticated counting oracle.
         */
        post: operations["countMessageTokens"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/images/generations": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Generate images from a text prompt (OpenAI-compatible; billed per generated image). */
        post: operations["createImage"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/tools": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List tools visible to the authenticated tenant. */
        get: operations["listTools"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/tools/execute": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Execute an extension tool. */
        post: operations["executeTool"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/functions/execute": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Execute a function through the fail-closed egress broker. */
        post: operations["executeFunction"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agents/{name}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Invoke a configured agent upstream. */
        post: operations["invokeAgent"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/self-hosted-workers/heartbeat": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Self-hosted worker heartbeat transport. */
        post: operations["recordSelfHostedWorkerHeartbeat"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/self-hosted-workers/events": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Self-hosted worker events transport. */
        post: operations["recordSelfHostedWorkerEvent"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/self-hosted-workers/artifacts": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Self-hosted worker artifacts transport. */
        post: operations["uploadSelfHostedWorkerArtifact"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/self-hosted-workers/checkpoints": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Self-hosted worker checkpoints transport. */
        post: operations["uploadSelfHostedWorkerCheckpoint"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/self-hosted-workers/runs/poll": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Self-hosted worker runs/poll transport. */
        post: operations["pollSelfHostedWorkerRun"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/self-hosted-workers/runs/ack": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Self-hosted worker runs/ack transport. */
        post: operations["acknowledgeSelfHostedWorkerRun"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tenant-accounts": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List tenant accounts. */
        get: operations["listTenantAccounts"];
        put?: never;
        /** Create a tenant account. */
        post: operations["createTenantAccount"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tenant-accounts/{tenant_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Get a tenant account. */
        get: operations["getTenantAccount"];
        /** Replace a tenant account. */
        put: operations["replaceTenantAccount"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /** Update a tenant account. */
        patch: operations["updateTenantAccount"];
        trace?: never;
    };
    "/admin/v1/tenant-accounts/{tenant_id}/resolved-defaults": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Read effective plan and quota defaults. */
        get: operations["getTenantResolvedDefaults"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tenant-accounts/{tenant_id}/plan": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        /** Assign a subscription plan to a tenant. */
        put: operations["assignTenantPlan"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/projects": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List projects. */
        get: operations["listProjects"];
        put?: never;
        /** Create a project. */
        post: operations["createProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/projects/{project_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                project_id: string;
            };
            cookie?: never;
        };
        /** Get a project. */
        get: operations["getProject"];
        /** Replace a project. */
        put: operations["replaceProject"];
        post?: never;
        /** Delete a project. */
        delete: operations["deleteProject"];
        options?: never;
        head?: never;
        /** Update a project. */
        patch: operations["updateProject"];
        trace?: never;
    };
    "/admin/v1/workspaces": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List workspaces. */
        get: operations["listWorkspaces"];
        put?: never;
        /** Create a workspace. */
        post: operations["createWorkspace"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/workspaces/{workspace_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                workspace_id: string;
            };
            cookie?: never;
        };
        /** Get a workspace. */
        get: operations["getWorkspace"];
        /** Replace a workspace. */
        put: operations["replaceWorkspace"];
        post?: never;
        /** Delete a workspace. */
        delete: operations["deleteWorkspace"];
        options?: never;
        head?: never;
        /** Update a workspace. */
        patch: operations["updateWorkspace"];
        trace?: never;
    };
    "/admin/v1/virtual-keys": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List durable virtual API keys. */
        get: operations["listVirtualKeys"];
        put?: never;
        /** Create a durable virtual API key. */
        post: operations["createVirtualKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/virtual-keys/{key_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Get a virtual API key. */
        get: operations["getVirtualKey"];
        put?: never;
        post?: never;
        /** Revoke a virtual API key. */
        delete: operations["revokeVirtualKey"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/quota-policies": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List quota policies. */
        get: operations["listQuotaPolicies"];
        put?: never;
        /** Create a quota policy. */
        post: operations["createQuotaPolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/quota-policies/{scope_type}/{scope_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Get a scoped quota policy. */
        get: operations["getQuotaPolicy"];
        /** Replace a scoped quota policy. */
        put: operations["replaceQuotaPolicy"];
        post?: never;
        /** Delete a scoped quota policy. */
        delete: operations["deleteQuotaPolicy"];
        options?: never;
        head?: never;
        /** Update a scoped quota policy. */
        patch: operations["updateQuotaPolicy"];
        trace?: never;
    };
    "/admin/v1/semantic-cache-policies": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List semantic cache policies.
         * @description The generic admin list envelope: `total`/`offset`/`limit` are present only when the request carried a query string.
         */
        get: operations["listSemanticCachePolicies"];
        put?: never;
        /**
         * Create a semantic cache policy for one scope.
         * @description `scope_type` and `scope_id` are required in the body and form the policy id. Re-creating an existing scope is 409.
         */
        post: operations["createSemanticCachePolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/semantic-cache-policies/{scope_type}/{scope_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Scope kind. `tenant` is the only kind accepted today; anything else answers 404, like a route miss. */
                scope_type: "tenant";
                /** @description Scope identifier. Resolved to its owning tenant before a tenant-scoped caller may touch it; resolution failure denies. */
                scope_id: string;
            };
            cookie?: never;
        };
        /** Get the semantic cache policy governing one scope. */
        get: operations["getSemanticCachePolicy"];
        /**
         * Replace the semantic cache policy governing one scope.
         * @description States the WHOLE governed policy: a field omitted from the body returns to inheriting the deployment value. There is no PATCH on this family for exactly that reason.
         */
        put: operations["replaceSemanticCachePolicy"];
        post?: never;
        /**
         * Delete the semantic cache policy governing one scope.
         * @description Removes the admin document and the typed enforcement row the gateway reads; leaving the second behind would keep applying a policy no console shows.
         */
        delete: operations["deleteSemanticCachePolicy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/semantic-cache-policies/{scope_type}/{scope_id}/invalidate": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Scope kind. `tenant` is the only kind accepted today; anything else answers 404, like a route miss. */
                scope_type: "tenant";
                /** @description Scope identifier. Resolved to its owning tenant before a tenant-scoped caller may touch it; resolution failure denies. */
                scope_id: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Purge everything the semantic cache holds for one scope.
         * @description Bumps the scope's invalidation epoch, which every cache key is derived from. Takes no request body. Unlike the write legs, this one REFUSES with 503 when the control database is unbound: a purge that cannot reach the typed row has not happened, and reporting 200 would tell an operator that bodies they are trying to stop serving are gone.
         */
        post: operations["invalidateSemanticCachePolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/plans": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List subscription plans. */
        get: operations["listPlans"];
        put?: never;
        /** Create a subscription plan. */
        post: operations["createPlan"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/plans/{plan_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Get a subscription plan. */
        get: operations["getPlan"];
        /** Replace a subscription plan. */
        put: operations["replacePlan"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /** Update a subscription plan. */
        patch: operations["updatePlan"];
        trace?: never;
    };
    "/admin/v1/wallets": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List tenant wallets. */
        get: operations["listWallets"];
        put?: never;
        /** Create a tenant wallet. */
        post: operations["createWallet"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/wallets/{tenant_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Get a tenant wallet. */
        get: operations["getWallet"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /** Update wallet recharge settings. */
        patch: operations["updateWallet"];
        trace?: never;
    };
    "/admin/v1/wallets/{tenant_id}/adjust": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Atomically adjust wallet credits. */
        post: operations["adjustWallet"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/wallets/{tenant_id}/charge": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Charge a payment method and credit a wallet. */
        post: operations["chargeWallet"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/wallets/{tenant_id}/ledger": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List wallet ledger entries. */
        get: operations["listWalletLedger"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/payment-methods": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List tenant payment methods. */
        get: operations["listPaymentMethods"];
        put?: never;
        /** Create a tenant payment method. */
        post: operations["createPaymentMethod"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/payment-methods/{payment_method_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Delete a payment method. */
        delete: operations["deletePaymentMethod"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List tenant assets.
         * @description Returns metadata only; bytes, the internal storage_uri field, bucket endpoint, and credentials are not serialized. Use each asset manifest for variant and yank state.
         */
        get: operations["listAssets"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/withheld": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List withheld (pending_scan/quarantined) assets for operators.
         * @description Operator-only inverse of GET /v1/assets: returns the assets the ordinary list/manifest/resolution paths hide from consumers (issue #366), each row carrying its durable visibility state and the screening evidence (scan/signature/approval + verification manifest) recorded at push/commit time. Read-only; use POST /v1/assets/{asset_type}/{name}/{version}/visibility to promote or quarantine. Tenant-scoped and paginated.
         */
        get: operations["listWithheldAssets"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/{asset_type}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List tenant assets by type.
         * @description Returns metadata only; use GET /v1/assets/{asset_type}/{name}/manifest for channel, variant, and yank state.
         */
        get: operations["listAssetsByType"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/{asset_type}/{name}/{version}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Resolve and fetch tenant asset bytes.
         * @description Resolves an exact version, channel, or semver range, selects a platform variant, enforces download quotas, and returns raw bytes using the stored content type. A yanked version remains downloadable but carries warning metadata. Use the manifest response to inspect all versions and SHA-256 checksums before downloading.
         */
        get: operations["getAsset"];
        /**
         * Publish a small tenant asset inline.
         * @description Buffers at most inline_upload_max_bytes from the storage summary, computes the SHA-256 checksum, validates the declared Content-Type and supply-chain policy, and publishes an immutable version. Larger objects must use upload intent -> direct PUT -> commit. Asset hosting requires the tenant plan flag or the assets.host permission. Special case: pushing a static_site asset_type whose body is a ZIP archive publishes a multi-file static-site bundle (unpacked into per-file objects plus a stored manifest and served under /sites/{tenant}/{site}/). Such a publish reads the x-site-public, x-site-spa-fallback, and x-site-cache-control headers for its serving policy and returns a StaticSitePublishResponse body instead of the AssetMutationResponse envelope.
         */
        put: operations["putAsset"];
        post?: never;
        /**
         * Delete a concrete tenant asset variant.
         * @description Deletes the selected variant row and best-effort removes its bucket object. Omit platform for the default variant.
         */
        delete: operations["deleteAsset"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/permissions": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List DB-backed permission actions.
         * @description Lists the DB-backed permission actions visible to the caller. Scoped to the caller (issue #518): a platform-operator key (no organization_id) receives the entire global catalog; a tenant-scoped admin.read key receives only the slice its own tenant can actually reach -- the roles bound to it via /admin/v1/tenant-roles/{tenant_id}, and only the permission keys those roles compose. The RBAC catalog is deliberately not public-to-tenants: it is the platform's authority model.
         */
        get: operations["listPermissions"];
        put?: never;
        /** Create a DB-backed permission action. */
        post: operations["createPermission"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/permissions/{permission_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Get a permission action.
         * @description Scoped per caller (#518): a platform operator sees the whole RBAC catalog; a tenant-scoped credential sees only the entries reachable for its own tenant, resolved through its tenant_role_bindings. An entry outside the caller's scope is refused identically to one that does not exist -- 403 `tenant_scope_denied`, deliberately conflating absent with forbidden -- so this endpoint cannot be walked as an existence oracle to rebuild the catalog the list operations no longer disclose. A platform operator still gets 404 for a genuinely absent id.
         */
        get: operations["getPermission"];
        put?: never;
        post?: never;
        /** Delete a permission action. */
        delete: operations["deletePermission"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/roles": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List DB-backed roles.
         * @description Lists the DB-backed roles visible to the caller. Scoped to the caller (issue #518): a platform-operator key (no organization_id) receives the entire global catalog; a tenant-scoped admin.read key receives only the slice its own tenant can actually reach -- the roles bound to it via /admin/v1/tenant-roles/{tenant_id}, and only the permission keys those roles compose. The RBAC catalog is deliberately not public-to-tenants: it is the platform's authority model.
         */
        get: operations["listRoles"];
        put?: never;
        /** Create a role from permission action keys. */
        post: operations["createRole"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/roles/{role_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Get a DB-backed role.
         * @description Scoped per caller (#518): a platform operator sees the whole RBAC catalog; a tenant-scoped credential sees only the entries reachable for its own tenant, resolved through its tenant_role_bindings. An entry outside the caller's scope is refused identically to one that does not exist -- 403 `tenant_scope_denied`, deliberately conflating absent with forbidden -- so this endpoint cannot be walked as an existence oracle to rebuild the catalog the list operations no longer disclose. A platform operator still gets 404 for a genuinely absent id.
         */
        get: operations["getRole"];
        put?: never;
        post?: never;
        /** Delete a DB-backed role. */
        delete: operations["deleteRole"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tenant-roles/{tenant_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List role bindings for a tenant. */
        get: operations["listTenantRoles"];
        put?: never;
        /** Bind a DB-backed role to a tenant. */
        post: operations["bindTenantRole"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/tenant-roles/{tenant_id}/{role_id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Remove a tenant role binding. */
        delete: operations["unbindTenantRole"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/usage-reports": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Read usage and cost report rows. */
        get: operations["listUsageReports"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/billing-outbox-dead-letters": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List dead-lettered billing outbox records. */
        get: operations["listBillingOutboxDeadLetters"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/billing-outbox-dead-letters/{report_id}/replay": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Replay a dead-lettered billing outbox record. */
        post: operations["replayBillingOutboxDeadLetter"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/virtual-keys/{key_id}/rotate": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Rotate a virtual API key. */
        post: operations["rotateVirtualKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/virtual-keys/{key_id}/enable": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Enable a virtual API key. */
        post: operations["enableVirtualKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/virtual-keys/{key_id}/disable": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Disable a virtual API key. */
        post: operations["disableVirtualKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/virtual-keys/{key_id}/revoke": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Revoke a virtual API key. */
        post: operations["revokeVirtualKeyAction"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agents/{name}/message:send": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Send an A2A message to a configured agent upstream. */
        post: operations["sendAgentMessage"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/agents/{name}/message:stream": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Stream an A2A message from a configured agent upstream. */
        post: operations["streamAgentMessage"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/site-domains": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** List custom-domain bindings for hosted static sites (tenant scoped). */
        get: operations["listSiteDomains"];
        put?: never;
        /** Bind a custom hostname to a hosted static site (audited; TLS rides the existing ACME issuance + graceful-upgrade reload). */
        post: operations["bindSiteDomain"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/site-domains/{hostname}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /** Fetch one custom-domain binding by hostname. */
        get: operations["getSiteDomain"];
        put?: never;
        post?: never;
        /** Unbind a custom hostname from its hosted static site (audited). */
        delete: operations["unbindSiteDomain"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/site-domains/{hostname}/verify": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Redeem the DNS-TXT ownership challenge for a bound custom hostname (audited).
         * @description Resolves _ferrogate-challenge.<hostname> and, only on an exact match with the value issued for this (tenant, hostname), promotes the binding to servable and adds it to the ACME domain set. A resolver that cannot answer returns 503 and never verifies (#488).
         */
        post: operations["verifySiteDomain"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/storage/summary": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Get authoritative tenant asset storage usage and upload limits.
         * @description Reads durable tenant usage and the authenticated key's effective tenant-owned storage quota. The response also tells clients whether to use inline upload or the presigned transfer flow.
         */
        get: operations["getAssetStorageSummary"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/{asset_type}/{name}/manifest": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Get an asset registry manifest.
         * @description Returns every logical version, yank state, platform variant, checksum, size, storage mode, and channel pointer without serializing raw bytes, the internal storage_uri field, bucket endpoint, or credentials.
         */
        get: operations["getAssetManifest"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/{asset_type}/{name}/channels": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List channel pointers for an asset.
         * @description Lists tenant-scoped latest/stable/canary or free-form channel pointers.
         */
        get: operations["listAssetChannels"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/{asset_type}/{name}/channels/{channel}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Create or move an asset channel.
         * @description Validates that the target is an existing, non-yanked concrete version, then durably writes the channel pointer. The validation and upsert are separate repository operations and do not claim race-free target integrity. Asset hosting requires the tenant plan flag or assets.host permission.
         */
        put: operations["putAssetChannel"];
        post?: never;
        /**
         * Delete an asset channel.
         * @description Deletes only the channel pointer; concrete asset versions remain unchanged.
         */
        delete: operations["deleteAssetChannel"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/{asset_type}/{name}/{version}/yank": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Yank every variant of an asset version.
         * @description Marks every variant of the concrete version as yanked. Pinned downloads remain possible with warning headers, while channel moves cannot target a yanked version.
         */
        post: operations["yankAssetVersion"];
        /**
         * Unyank every variant of an asset version.
         * @description Clears yank state on every variant of the concrete version.
         */
        delete: operations["unyankAssetVersion"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/{asset_type}/{name}/{version}/visibility": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Promote a pending_scan asset after an out-of-band scan.
         * @description Flips a withheld pending_scan asset version to visible (clean verdict) or quarantined (flagged verdict) after a completed out-of-band/async scan. The transition is a fail-closed compare-and-swap that fires only from the pending_scan state: an already-visible or already-quarantined asset returns 409 and a missing asset returns 404, so a completed scan can never silently re-promote a terminal asset. An unknown scan_outcome or missing evidence returns 400 and never promotes. Every attempt (committed or rejected) emits a durable audit event linking the promotion to the scan outcome, evidence, asset id, tenant, and request/trace id. Select a platform variant with the platform query parameter. Asset hosting requires the tenant plan flag or assets.host permission.
         */
        post: operations["promoteAssetVisibility"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/presign/upload/{asset_type}/{name}/{version}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Issue a presigned large-asset upload intent.
         * @description Preflights the declared size and tenant quota, allocates a unique staging upload, and returns its opaque upload_id plus a short-lived direct PUT URL. Every intent gets a new staging object, so issuing or replaying an intent cannot overwrite a live immutable asset. key identifies the logical asset only; no storage key is returned as a standalone field, and the upload URL never targets the final key. Existing immutable versions return 409 before a URL is issued.
         */
        post: operations["createAssetUploadIntent"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/presign/commit/{asset_type}/{name}/{version}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Verify and commit a presigned asset upload.
         * @description Resolves the opaque upload_id to its unique staging object, verifies declared size and SHA-256, and then runs the SAME full trust screening the inline push applies over the final verified bytes: detached-signature verification, the cross-tenant approval gate, the configured malware scanner, and the built-in content/type policy (issue #366). Invalid signature, missing approval, and scanner reject/unavailable fail closed with no durable write; a pending/quarantined scan verdict is persisted with an explicit visibility state and withheld from every list, manifest, resolution, and download surface until promoted clean. Verified bytes are copied to an internal immutable final key before the metadata row is created atomically. That key is never a client PUT target and is not serialized in list, manifest, or standalone response fields; an authorized presigned download URL necessarily contains its path. Only the same upload_id with matching metadata is idempotent; a different upload_id for an existing immutable version returns 409 without changing the live asset. Definitive validation, quota, and losing-conflict failures clean only this intent's temporary objects best-effort, while infrastructure outcomes that cannot be proven preserve its staging and final candidates for retry or garbage collection. Successful commits clean staging best-effort. If the atomic metadata create may have committed but its result cannot be proven, the gateway returns 503 asset_commit_outcome_unknown and preserves both objects for a same-upload_id retry or operator reconciliation.
         */
        post: operations["commitAssetUpload"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/presign/abort/{asset_type}/{name}/{version}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Release a presigned upload intent that will not be committed.
         * @description Abort/cancel surface for an unused upload intent (issue #368). Attempts to delete the intent's staging object immediately when one exists, instead of leaving it for the asset-lifecycle GC, and records the release in the audit trail and the ferrogate_asset_presign_* counters. The attempt can fail (bucket 5xx/403), so the response reports what actually happened through staging_reclamation (not_staged / removed / removal_failed) and staging_object_removed is true ONLY for a delete the bucket confirmed; a failed reclamation is audited as aborted_reclaim_failed, counted in ferrogate_asset_presign_abort_reclaim_failed_total, and left to the lifecycle GC. A client whose direct PUT was refused by the bucket may declare reason=bucket_rejected; the gateway corroborates that claim against the server-derived staging key before recording it, and downgrades it to a plain abort when the object turns out to exist. Returns 409 when the upload was already committed: aborting is never a path to deleting a published immutable version. This is a client-reported signal with a server-side consistency check, not an independent observation of the direct PUT, which the gateway is not in the path of.
         */
        post: operations["abortAssetUpload"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/assets/presign/download/{asset_type}/{name}/{version}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Issue a presigned private-bucket download URL.
         * @description Returns a short-lived direct GET URL plus the stored size, content type, and SHA-256 for client verification. Inline assets return 409 and must be fetched through the ordinary asset GET route. URL issuance is audited and metered as egress.
         */
        get: operations["getAssetDownloadUrl"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/observed-agent-activity": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List tenant-scoped Unknown (unattributed virtual-API-key) agent activity.
         * @description Presents traffic authenticated by a durable virtual API key that carries no verified managed-worker session, self-hosted worker/run, or agent-run identity as a third presentation/source category (source=virtual_api_key, identity_status=unattributed, display_name=Unknown). It is NOT a WorkerType and NOT a persistent agent entity: the stable identity is the observed key activity, and it never claims how many real agents share the key. status=running is derived from a recent-activity window (running_ttl_seconds) over already-recorded evidence; expiry reports inactive without fabricating a stop event. When the durable presence read fails (#494) the affected rows report status=unknown with status_basis=presence_feed_unavailable and the list carries presence_feed.rows_may_be_incomplete=true; a read error is never rendered as a confident not-running.
         */
        get: operations["listAdminObservedAgentActivity"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/agent-cost-burn": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List tenant-scoped durable per-agent runtime cost-burn for a billing period.
         * @description Surfaces the durable, accumulating per-agent runtime cost-burn (#428) in the observability/billing admin surface. One row per (tenant_id, agent_key, period): accumulated_usd folds every run of the stable agent key inside the YYYY-MM billing window, so CF-hosted-agent runtime cost is visible per tenant/agent. Optional period=YYYY-MM query selects the billing window (default: the current UTC month). Tenant-isolated: a tenant-scoped admin sees only its own tenant's burn; the platform operator sees all. Read-only surfacing -- enforcement of the per-agent ceiling is a separate concern -- so a durable-store failure is reported as service_unavailable, never a fabricated empty (zero-burn) list.
         */
        get: operations["listAdminAgentCostBurn"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/cost-records": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List per-request cost attribution records, newest first.
         * @description One row per REQUEST, joining the decision (request_logs, #664) to the priced provider attempts (billing_events, #663/#667) so a chargeback question can be answered down to the customer, project, key, model, tag and agent run that produced a charge (#677). It is a JOIN, not a third durable figure: there is no cost_records table, so this surface can never disagree with the billing ledger. Tenant-fenced -- a tenant-scoped admin sees only its own traffic, and the total is computed after the fence, so it cannot leak the existence of another tenant's requests. metered/priced/cost_usd are three separate answers on purpose: cost_usd null with metered false means nothing metered the request (refused, rate-limited), metered true with priced false means some or all attempts could not be priced, and unpriced_attempts names exactly how many -- collapsing any of those into $0 would make a finance total quietly low. The paginated envelope is answered unconditionally, so an un-queried call cannot be mistaken for the whole month.
         */
        get: operations["listAdminCostRecords"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/cost-record-exports": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Download the per-request cost records as CSV, JSONL or Parquet.
         * @description The same fenced, filtered, ordered records as listAdminCostRecords -- both operations run one query, so an export can never see a row the list cannot -- projected onto a downloadable file (#677). `?format=` selects the body: csv (the default, RFC 4180 with a header row), jsonl (one cost record document per line) or parquet (the tabular projection as a Parquet file). The CSV and Parquet column ORDER is part of the contract because chargeback pipelines index it positionally; new columns are appended rather than inserted, and `tags` is one JSON-encoded column rather than a column per key so two months of the same query concatenate. Every response carries content-disposition with a filename. The page is bounded (default 1000 rows, maximum 10000) because a Worker materializes the whole file in memory: pull further pages with `?offset=`.
         */
        get: operations["exportAdminCostRecords"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/x402-spend-policies": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List the operator-declared typed x402 spend policies.
         * @description Returns every x402 spend-policy declaration in the running config (#351), scoped to what the caller may see: a tenant-scoped admin sees only declarations for tenants/projects/workspaces/keys it owns, while run-scoped declarations are visible to the platform operator only (an agent-run id cannot yet be resolved to its tenant). Read-only diagnostics: the response carries allowlists, caps, conversion ratio, approval threshold and revision -- never a signer key, wallet secret, or any credential.
         */
        get: operations["listX402SpendPolicies"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/x402-spend-policies/effective": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Resolve the effective x402 spend policy and revision for a scope chain.
         * @description Resolves the tenant/project/workspace/key/agent-run chain against the operator-declared policies (#351) and returns the single declaration in force, its revision, and the inheritance evidence explaining why it won. The narrowest declared scope wins; a chain with no declaration anywhere returns declared=false with the disabled deny-all default at revision 0 rather than 404, so an unconfigured scope can never be read as unlimited. Exposes decisions and revisions only -- no signer or secret fields.
         */
        get: operations["getEffectiveX402SpendPolicy"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/x402-spend-policies/evaluate": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Dry-run the effective x402 spend policy against a concrete challenge.
         * @description Parses an untrusted x402 PAYMENT-REQUIRED challenge with the frozen x402/SVM wire contract, resolves the effective policy for the scope chain, and returns the pure Allow / ApprovalRequired / Deny decision produced by the SAME policy function the payment path evaluates (#351). Read-only: nothing is paid, signed, or persisted. The response carries the full evidence chain -- stable reason code, policy revision, matched resource rule, ORIGINAL atomic units, computed internal credits, conversion snapshot and challenge hash -- and no signer or secret field. Money math is checked integer arithmetic: overflow or a missing/impossible conversion ratio denies.
         */
        post: operations["evaluateX402SpendPolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/payment-attempts": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List a tenant's durable x402 payment attempts.
         * @description Returns one BOUNDED, tenant-scoped page of durable x402 payment attempts (#352), newest-first, so an operator can inspect a stuck attempt -- notably an outcome_unknown one whose wallet hold is still live and whose stablecoin may already have moved. tenant_id is REQUIRED and applied before ordering and before the cursor; a tenant-scoped admin key may only name its own tenant. Paging is a keyset cursor over (created_at_unix, id) -- the index's own order, with no OFFSET scan -- and limit is clamped server-side, so no request can ask for the whole table. Read-only: nothing is created, released, or captured.
         */
        get: operations["listPaymentAttempts"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/admin/v1/payment-attempts/{id}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Inspect one payment attempt with its wallet reservation and settlement.
         * @description Returns one durable x402 payment attempt joined to its wallet hold and its captured wallet settlement (#352 E2E evidence: attempt, hold, wallet settlement, policy revision, transaction evidence). Tenancy fails closed BOTH ways: a tenant-scoped key is scoped to its own tenant unless it names one, so another tenant's attempt is indistinguishable from a missing one and returns 404 without disclosing that the payment exists; naming a tenant the key does not own is 403. A platform-operator key may omit tenant_id, in which case the owner is resolved from the attempt. Read-only: no hold is released or captured by this call.
         */
        get: operations["getPaymentAttemptLinks"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        HealthResponse: {
            /** @constant */
            status: "ok";
            /** @constant */
            service: "ferrogate";
            version: string;
            /** @constant */
            runtime: "pingora";
        };
        ReadinessResponse: {
            /** @enum {string} */
            status: "ready" | "not_ready";
            /** @constant */
            service: "ferrogate";
            version: string;
            /** @constant */
            runtime: "pingora";
            cluster: components["schemas"]["ClusterStatus"];
        };
        /** @description A read section of the control-plane overview that is either fresh data or an explicit unavailable/error state. `data` is absent whenever `status` is not `ok`, so a failed section is never reported as a fabricated zero. */
        AdminOverviewSection: {
            /** @enum {string} */
            status: "ok" | "unavailable";
            /** @description Where this section was computed from, e.g. `runtime_config` or `control_plane_store`. */
            source: string;
            /** Format: int64 */
            generated_at_unix?: number;
            /** @description Present only when `status` is `unavailable`. */
            error?: string;
            /** @description Section payload; present only when `status` is `ok`. */
            data?: {
                [key: string]: unknown;
            };
        };
        /** @description Scoped control-plane overview (issue #339). Inventory counts and durable token totals are produced by bounded repository aggregation; each section reports its own availability so a partial storage failure never becomes a false zero. */
        AdminOverview: {
            /** @enum {string} */
            object: "control_plane.overview";
            /** Format: int64 */
            generated_at_unix: number;
            /** @description Effective visibility scope. `global` for a platform-operator key; `tenant` (with `tenant_id`) for a tenant-scoped key. */
            scope: {
                /** @enum {string} */
                kind: "global" | "tenant";
                tenant_id?: string;
            };
            runtime: components["schemas"]["AdminOverviewSection"];
            control_plane: components["schemas"]["AdminOverviewSection"];
            usage: components["schemas"]["AdminOverviewSection"];
            alerts: components["schemas"]["AdminOverviewSection"];
        };
        AdminStatus: {
            /** @constant */
            service: "ferrogate";
            version: string;
            /** @constant */
            runtime: "pingora";
            snapshot: string;
            providers: number;
            enabled_providers: number;
            models: number;
            enabled_models: number;
            api_keys: number;
            upstreams: number;
            enabled_upstreams: number;
            routes: number;
            enabled_routes: number;
            plugins: number;
            active_plugins: number;
            extensions: number;
            active_extensions: number;
            tools: number;
            auth_required: boolean;
            storage: components["schemas"]["StorageBackendEvidence"];
            analytics: components["schemas"]["AnalyticsStatus"];
            cluster: components["schemas"]["ClusterStatus"];
            observability: components["schemas"]["ObservabilityStatus"][];
            acme?: components["schemas"]["AdminAcmeStatus"] | null;
            prompt_templates: number;
        };
        StorageBackendEvidence: {
            provider: components["schemas"]["StorageProviderKind"];
            durable: boolean;
            implemented: boolean;
            required: boolean;
            /**
             * @description Configured schema migration posture. Secrets and DSNs are never returned.
             * @enum {string}
             */
            migration_mode: "auto" | "validate_only" | "disabled";
            /**
             * @description Active storage backend health for the running process. Startup fails instead of returning degraded durable storage when storage.required is true.
             * @enum {string}
             */
            health: "ok";
            provider_order: components["schemas"]["StorageProviderKind"][];
            contract_version: number;
            schema: components["schemas"]["StorageSchemaEvidence"] | null;
        };
        StorageSchemaEvidence: {
            engine: string;
            version: number;
            name: string;
            checksum: string;
            validated: boolean;
        };
        /** @enum {string} */
        StorageProviderKind: "memory" | "supabase" | "postgres";
        ClusterStatus: {
            enabled: boolean;
            cluster_id: string;
            node_id: string;
            node_region: string | null;
            node_zone: string | null;
            /**
             * @description Cluster control-plane state backend. local is process-local; file uses a shared JSON file for lightweight multi-node API key and policy propagation.
             * @enum {string}
             */
            state_backend: "local" | "file";
            /**
             * @description Cluster counter backend. local is process-local; redis uses shared atomic counters for cluster-safe request limits and token-budget reservation/settlement.
             * @enum {string}
             */
            counter_backend: "local" | "redis";
            active_revision: string;
            last_sync_at_unix: number | null;
            last_sync_error: string | null;
            stale: boolean;
            ready: boolean;
            /** @enum {string} */
            readiness_reason: "state_loaded" | "stale_state" | "sync_error" | "revision_missing" | "operator_drain";
            draining: boolean;
            accepting_new_requests: boolean;
        };
        AdminAcmeStatus: {
            enabled: boolean;
            domains: string[];
            cert_path: string;
            key_path: string;
            certificate_expires_at_unix?: number | null;
            renewal_window_secs: number;
            renewal_due: boolean;
            last_renewal_status: string;
            last_renewal_at_unix?: number | null;
            last_renewal_error?: string | null;
            next_check_at_unix?: number | null;
            reload_required: boolean;
            reload_mode: string;
        };
        AdminProvider: {
            name: string;
            kind: string;
            /** @enum {string} */
            compatibility: "openai-compatible" | "dedicated";
            base_url: string;
            has_api_key: boolean;
            enabled: boolean;
        };
        AdminProviderModelCatalog: {
            provider: string;
            kind: string;
            base_url: string;
            enabled: boolean;
            /** @enum {string} */
            status: "ok" | "error" | "disabled";
            models: components["schemas"]["AdminProviderModelCandidate"][];
            error: string | null;
        };
        AdminProviderModelCandidate: {
            id: string;
            owned_by: string | null;
            created: number | null;
            context_window: number | null;
            capabilities: string[];
        };
        ProviderHealthCheck: {
            name: string;
            kind: string;
            base_url: string;
            enabled: boolean;
            status: string;
            reachable: boolean;
            circuit_open: boolean;
            consecutive_failures: number;
            checked_at_unix?: number | null;
            error?: string | null;
            routing: components["schemas"]["ProviderRoutingHealth"];
            local_observations: components["schemas"]["ProviderRoutingHealth"];
            /** @description Cluster-wide provider observations when a shared aggregation backend is configured. Null means this node only has local observations. */
            cluster_observations: components["schemas"]["ProviderRoutingHealth"] | null;
        };
        ObservabilityStatus: {
            /** @enum {string} */
            provider: "vector" | "otlp" | "none";
            enabled: boolean;
            active: boolean;
            endpoint: string | null;
            /** @enum {string} */
            endpoint_source: "observability" | "telemetry_legacy" | "none";
            /** @constant */
            protocol: "otlp_http_json";
            signals: components["schemas"]["StringList"];
            prometheus_metrics_path: string;
            export_timeout_secs: number;
            health: string;
            last_success_at_unix: number | null;
            last_export_error: string | null;
            queue_backpressure_events: number;
            dropped_events: number;
        };
        AnalyticsStatus: {
            /** @enum {string} */
            provider: "vector" | "clickhouse" | "none";
            enabled: boolean;
            active: boolean;
            required: boolean;
            /** @enum {string} */
            mode: "pipeline" | "direct_warehouse" | "none";
            sink_configured: boolean;
            signals: components["schemas"]["StringList"];
            export_timeout_secs: number;
            batch_max_events: number;
            flush_interval_millis: number;
            queue_capacity: number;
            request_log_retention_records: number;
            audit_event_retention_records: number;
            billing_event_retention_records: number;
            /** @enum {string} */
            health: "disabled" | "configured" | "not_configured" | "ok" | "degraded";
            last_success_at_unix: number | null;
            last_export_error: string | null;
            contract_version: number;
        };
        AdminPlugin: {
            id: string;
            /** @enum {string} */
            kind: "request_hook" | "tool_provider" | "event_sink";
            /** @description Plugin manifest version. */
            version: string;
            manifest: components["schemas"]["PluginManifest"];
            compatibility: components["schemas"]["PluginCompatibility"];
            source: string;
            approval_policy?: components["schemas"]["ApprovalPolicy"];
            permissions?: components["schemas"]["ExtensionPermissions"];
            config?: {
                [key: string]: unknown;
            };
            capabilities: components["schemas"]["StringList"];
            tools: components["schemas"]["StringList"];
            enabled: boolean;
            active: boolean;
            /** @enum {string} */
            lifecycle: "registered" | "enabled" | "disabled" | "degraded" | "failed" | "version_incompatible";
            /** @enum {string} */
            health: "disabled" | "ok" | "degraded" | "failed" | "version_incompatible" | "unknown";
            order: number;
            last_error: string | null;
        };
        AdminPluginMutation: {
            id?: string;
            /** @enum {string} */
            kind: "request_hook" | "tool_provider" | "event_sink";
            /** @description Plugin manifest version. */
            version?: string;
            manifest?: components["schemas"]["PluginManifest"];
            compatibility?: components["schemas"]["PluginCompatibility"];
            enabled?: boolean;
            source?: string;
            order?: number;
            approval_policy?: components["schemas"]["ApprovalPolicy"];
            permissions?: components["schemas"]["ExtensionPermissions"];
            config?: {
                [key: string]: unknown;
            };
        };
        PluginManifest: {
            name?: string | null;
            description?: string | null;
            capabilities?: components["schemas"]["StringList"];
            required_permissions?: components["schemas"]["ExtensionPermissions"];
            hooks?: components["schemas"]["StringList"];
            config_schema?: {
                [key: string]: unknown;
            } | null;
        };
        PluginCompatibility: {
            min_gateway_version?: string | null;
            max_gateway_version?: string | null;
        };
        /** @enum {string} */
        ApprovalPolicy: "never" | "always";
        ExtensionPermissions: {
            tools?: components["schemas"]["StringList"];
            network?: components["schemas"]["StringList"];
            filesystem?: boolean;
            shell?: boolean;
            tenant_scope?: boolean;
            secrets?: boolean;
            admin_mutation?: boolean;
        };
        AdminPluginMutationResponse: {
            /** @constant */
            object: "plugin";
            plugin: components["schemas"]["AdminPlugin"];
        };
        AdminTool: {
            name: string;
            description: string | null;
            input_schema: Record<string, never>;
            extension_id: string;
            /** @enum {string} */
            approval_policy: "never" | "always";
            tenant_allowlist: components["schemas"]["StringList"];
            api_key_allowlist: components["schemas"]["StringList"];
            route_allowlist: components["schemas"]["StringList"];
        };
        ToolApprovalDecisionRequest: {
            /** @description Required for approve; must match the pending immutable action fingerprint. */
            fingerprint?: string | null;
            reason?: string | null;
        };
        ToolApprovalRecord: {
            id: string;
            request_id: string;
            trace_id: string | null;
            /** @description Issue #305: id of the agent run this approval was requested inside, carried from the tool-governance execution context at approval-creation time. Null/absent on records persisted before the field existed and on approvals created outside an agent run. Never part of the approval fingerprint. */
            agent_run_id?: string | null;
            /** @description Issue #305: id of the workflow whose node requested the tool, when the execution was workflow-bound. */
            workflow_id?: string | null;
            /** @description Issue #305: id of the workflow node that requested the tool, when the execution was workflow-bound. */
            workflow_node_id?: string | null;
            /** @description Issue #306: target-level canonical_target_sha256 action fingerprint of the action this approval gates, carried alongside - never instead of - the invocation-binding `fingerprint`, which stays authoritative for approval verification. Null/absent on legacy records and on tools without a resolvable canonical capability target. Never part of the approval fingerprint input. */
            action_fingerprint?: string | null;
            /**
             * @description Issue #306: stored canonical decision class for the current status, maintained on every status transition via the issue #303 From<ApprovalStatus> mapping (pending=ask, approved=allow, denied/expired=deny). Null/absent only on legacy records.
             * @enum {string|null}
             */
            decision?: "allow" | "deny" | "ask" | null;
            /**
             * @description Issue #306: stored issue #303 decision reason code for the current status (the lossless discriminator: denied vs expired both map to deny). Null/absent only on legacy records.
             * @enum {string|null}
             */
            decision_reason?: "approval_pending" | "approval_approved" | "approval_denied" | "approval_expired" | null;
            tenant: components["schemas"]["TenantContext"];
            actor_api_key_id: string | null;
            tool_name: string;
            server_name: string | null;
            route: string | null;
            /** @enum {string} */
            approval_policy: "never" | "always";
            approval_timeout_secs: number;
            fingerprint: string;
            arguments_summary: string;
            risk_reason: string;
            /** @enum {string} */
            status: "pending" | "approved" | "denied" | "expired";
            reviewer_api_key_id: string | null;
            reviewer_authority: string | null;
            terminal_reason: string | null;
            requested_at_unix: number;
            expires_at_unix: number;
            decided_at_unix: number | null;
        };
        ProviderRoutingHealth: {
            observed_requests: number;
            successful_requests: number;
            failed_requests: number;
            average_latency_ms: number | null;
            failure_rate: number;
            /** @description Lower ranks are preferred by latency-aware and balanced routing. */
            health_rank: number;
            /** @enum {string} */
            health_reason: "disabled" | "circuit_open" | "observed_failure_rate" | "no_observations" | "healthy_observations";
        };
        /** @enum {string} */
        ModelCapability: "chat" | "streaming" | "vision" | "images" | "embeddings" | "tools" | "structured_output";
        Model: {
            name: string;
            provider: string;
            provider_model: string;
            /** @default priority */
            routing_strategy: string;
            fallbacks: components["schemas"]["ModelFallback"][];
            canary?: components["schemas"]["CanaryRoute"] | null;
            visible_organization_ids?: string[];
            visible_project_ids?: string[];
            capabilities?: components["schemas"]["ModelCapability"][];
            context_window?: number | null;
            input_price_per_1m?: number | null;
            output_price_per_1m?: number | null;
            enabled: boolean;
        };
        ModelFallback: {
            provider: string;
            provider_model: string;
            capabilities?: components["schemas"]["ModelCapability"][];
            context_window?: number | null;
            input_price_per_1m?: number | null;
            output_price_per_1m?: number | null;
            priority?: number | null;
            weight?: number | null;
            enabled: boolean;
        };
        CanaryRoute: {
            provider: string;
            provider_model: string;
            capabilities?: components["schemas"]["ModelCapability"][];
            context_window?: number | null;
            /** @default 0 */
            percent: number;
            input_price_per_1m?: number | null;
            output_price_per_1m?: number | null;
            enabled: boolean;
        };
        AdminApiKey: {
            id: string;
            name: string;
            enabled: boolean;
            /**
             * @description Redacted source classification; secret values are never returned.
             * @enum {string}
             */
            key_source: "env" | "hash" | "inline" | "none";
            scopes: string[];
            allowed_models: components["schemas"]["StringList"];
            denied_models: components["schemas"]["StringList"];
            allowed_providers: components["schemas"]["StringList"];
            denied_providers: components["schemas"]["StringList"];
            organization_id?: string | null;
            /** @description What this key DECLARES about platform root (issue #515): true = declared root, false = explicitly refused root, null = declared nothing, so the answer comes from [tenancy] implicit_platform_operator. Nullable because PUT is a full replace with no merge: rendering the null state as false made a read-modify-write persist an explicit refusal on legacy operator keys and lock the admin API out. See effective_platform_operator for the resolved answer. */
            platform_operator?: boolean | null;
            /** @description Whether this key holds unrestricted, cross-tenant platform-root access RIGHT NOW (issue #515) -- platform_operator resolved against organization_id and this deployment's [tenancy] implicit_platform_operator setting. Read-only and derived; it is not accepted on the mutation payload. */
            effective_platform_operator?: boolean;
            team_id?: string | null;
            project_id?: string | null;
            workspace_id?: string | null;
            user_id?: string | null;
            monthly_token_budget?: number | null;
            request_limit_per_minute?: number | null;
            expires_at_unix?: number | null;
            log_bodies: boolean;
            /** @description Optional per-key override for exact-match AI response caching. Null inherits the global/model cache policy. */
            cache_enabled?: boolean | null;
        };
        AdminApiKeyMutation: {
            id?: string;
            name?: string;
            key_env?: string;
            /** @description Plain secret for local development only. Prefer key_env or key_hash. */
            key?: string;
            key_hash?: string;
            /** @default true */
            enabled: boolean;
            scopes?: components["schemas"]["StringList"];
            allowed_models?: components["schemas"]["StringList"];
            denied_models?: components["schemas"]["StringList"];
            allowed_providers?: components["schemas"]["StringList"];
            denied_providers?: components["schemas"]["StringList"];
            organization_id?: string;
            /** @description Explicit platform-root opt-in (issues #515, #540). Mutually exclusive with organization_id. Since #540 one of the two MUST be stated: a body that omits both is refused with 400 invalid_api_key, because an omitted field used to mean unrestricted cross-tenant access. A deployment mid-migration can still set [tenancy] implicit_platform_operator = true to accept an unstated body, which restores the pre-#515 rule for every key that states nothing. */
            platform_operator?: boolean;
            team_id?: string;
            project_id?: string;
            workspace_id?: string;
            user_id?: string;
            monthly_token_budget?: number;
            request_limit_per_minute?: number;
            expires_at_unix?: number;
            log_bodies?: boolean;
            /** @description Optional per-key override for exact-match AI response caching. */
            cache_enabled?: boolean;
        };
        AdminApiKeyMutationResponse: {
            /** @constant */
            object: "api_key";
            key: components["schemas"]["AdminApiKey"];
        };
        PolicyRule: {
            name: string;
            /** @enum {string} */
            effect: "allow" | "deny";
            organization_ids: components["schemas"]["StringList"];
            project_ids: components["schemas"]["StringList"];
            api_key_ids: components["schemas"]["StringList"];
            models: components["schemas"]["StringList"];
            providers: components["schemas"]["StringList"];
            code: string;
            message: string;
            enabled: boolean;
        };
        AdminPolicyMutation: {
            name?: string;
            /**
             * @default deny
             * @enum {string}
             */
            effect: "allow" | "deny";
            organization_ids?: components["schemas"]["StringList"];
            project_ids?: components["schemas"]["StringList"];
            api_key_ids?: components["schemas"]["StringList"];
            models?: components["schemas"]["StringList"];
            providers?: components["schemas"]["StringList"];
            /** @default policy_denied */
            code: string;
            /** @default request denied by policy */
            message: string;
            /** @default true */
            enabled: boolean;
        };
        AdminPolicyMutationResponse: {
            /** @constant */
            object: "policy";
            policy: components["schemas"]["PolicyRule"];
        };
        AdminGatewayConfigMutation: {
            id?: string;
            name?: string;
            /** @default 1 */
            revision: number;
            /** @default true */
            enabled: boolean;
            api_key_ids?: components["schemas"]["StringList"];
            /** @description Optional request-time override for exact-match AI response caching within the global cache policy. */
            cache_enabled?: boolean;
        };
        AdminGatewayConfigMutationResponse: {
            /** @constant */
            object: "gateway_config";
            gateway_config: components["schemas"]["AdminGatewayConfigProfile"];
        };
        AgentWorkflowPolicy: {
            id: string;
            name: string;
            /** @default 1 */
            version: number;
            /** @default true */
            enabled: boolean;
            organization_ids?: components["schemas"]["StringList"];
            project_ids?: components["schemas"]["StringList"];
            api_key_ids?: components["schemas"]["StringList"];
            nodes: components["schemas"]["AgentWorkflowNode"][];
            /** @default [] */
            edges: components["schemas"]["AgentWorkflowEdge"][];
            max_model_calls?: number | null;
            max_tool_calls?: number | null;
            max_parallelism?: number | null;
            max_iterations?: number | null;
            timeout_millis?: number | null;
            token_budget?: number | null;
        };
        AgentWorkflowNode: {
            id: string;
            kind?: components["schemas"]["AgentWorkflowNodeKind"];
            model?: string | null;
            providers?: components["schemas"]["StringList"];
            tool?: string | null;
            max_iterations?: number | null;
            token_budget?: number | null;
        };
        /**
         * @default model
         * @enum {string}
         */
        AgentWorkflowNodeKind: "model" | "tool" | "router" | "human" | "checkpoint";
        AgentWorkflowEdge: {
            from: string;
            to: string;
            condition?: string | null;
        };
        AdminAgentWorkflow: {
            workflow: components["schemas"]["AgentWorkflowPolicy"];
            counters: components["schemas"]["AdminAgentWorkflowCounters"];
        };
        AdminAgentWorkflowCounters: {
            request_count: number;
            error_count: number;
            billing_event_count: number;
            audit_event_count: number;
            estimated_tokens: number;
        };
        AdminAgentWorkflowMutationResponse: {
            /** @constant */
            object: "agent_workflow";
            agent_workflow: components["schemas"]["AdminAgentWorkflow"];
        };
        AdminAgentUpstreamMutation: {
            id?: string;
            name?: string;
            description?: string | null;
            enabled?: boolean;
            protocol?: components["schemas"]["AgentUpstreamProtocol"];
            endpoint?: string;
            auth?: components["schemas"]["AgentUpstreamAuth"];
            tenant_ids?: components["schemas"]["StringList"];
            capabilities?: components["schemas"]["AgentUpstreamCapability"][];
        };
        AdminAgentUpstreamMutationResponse: {
            /** @constant */
            object: "agent_upstream";
            agent_upstream: components["schemas"]["AdminAgentUpstream"];
        };
        AdminAgentUpstream: {
            id: string;
            name: string;
            description?: string | null;
            enabled: boolean;
            protocol: components["schemas"]["AgentUpstreamProtocol"];
            endpoint: string;
            tenant_ids: components["schemas"]["StringList"];
            capabilities: components["schemas"]["AgentUpstreamCapability"][];
        };
        AdminList_AdminAgentUpstream: {
            /** @enum {string} */
            object: "list";
            data: components["schemas"]["AdminAgentUpstream"][];
        };
        DeleteApiKeyResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "api_key";
        };
        DeletePolicyResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "policy";
        };
        DeleteGatewayConfigResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "gateway_config";
        };
        DeleteAgentWorkflowResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "agent_workflow";
        };
        DeleteResponse: {
            object: string;
            id: string;
            /** @constant */
            deleted: true;
        };
        AdminTenantRef: {
            organization_id?: string | null;
            team_id?: string | null;
            project_id?: string | null;
            user_id?: string | null;
            api_key_id: string;
        };
        AdminConfigValidateRequest: {
            config_toml?: string;
            config_yaml?: string;
            config_caddyfile?: string;
            /** @description Used when parsing config_caddyfile. */
            filename?: string;
            /**
             * @description Use source=file to read the config file that started the current process.
             * @enum {string}
             */
            source?: "file";
        };
        AdminConfigValidateResponse: {
            valid: boolean;
            snapshot?: string | null;
            reload_mode?: string | null;
            listener_reload_required: boolean;
            reload_reason?: string | null;
            error?: string | null;
        };
        AdminConfigReloadResponse: {
            valid: boolean;
            committed: boolean;
            mode: string;
            active_snapshot?: string | null;
            candidate_snapshot?: string | null;
            error?: string | null;
        };
        AdminDrainRequest: {
            drain: boolean;
        };
        AdminDrainResponse: {
            /** @constant */
            object: "drain_status";
            draining: boolean;
            accepting_new_requests: boolean;
            /** @enum {string} */
            drain_reason: "not_draining" | "operator_drain";
        };
        RequestLog: {
            request_id?: string;
            trace_id?: string | null;
            agent_run_id?: string | null;
            /** @description Issue #307: fingerprint of the UPSTREAM governed action this row is a downstream effect of (canonical_target_sha256, "sha256:<hex>"), declared via x-ferrogate-parent-action-fingerprint on the A2A ingress. Null/absent when no parent was declared - never fabricated. */
            parent_action_fingerprint?: string | null;
            cluster_id?: string | null;
            node_id?: string | null;
            tenant?: components["schemas"]["TenantContext"];
            route?: string | null;
            provider?: string | null;
            logical_model?: string | null;
            provider_model?: string | null;
            status_code?: number;
            error_code?: string | null;
            prompt_recorded?: boolean;
            response_recorded?: boolean;
            prompt_body?: string | null;
            response_body?: string | null;
            started_at_unix?: number | null;
            completed_at_unix?: number | null;
            gateway_config_id?: string | null;
            gateway_config_revision?: number | null;
        };
        StoredUsageAggregate: {
            id: string;
            organization_id?: string | null;
            project_id?: string | null;
            api_key_id?: string | null;
            logical_model: string;
            provider: string;
            usage: components["schemas"]["TokenUsage"];
        };
        MeteringExportStatus: {
            request_id: string;
            trace_id?: string | null;
            /** @enum {string} */
            provider: "legacy" | "openmeter";
            endpoint: string;
            success: boolean;
            /** @enum {string} */
            status: "exported" | "failed";
            error?: string | null;
            occurred_at_unix: number;
        };
        StoredAuditEvent: {
            id: string;
            request_id: string;
            trace_id?: string | null;
            agent_run_id?: string | null;
            cluster_id?: string | null;
            node_id?: string | null;
            actor_api_key_id?: string | null;
            tenant?: components["schemas"]["TenantContext"];
            action: string;
            target: string;
            outcome: string;
            message: string;
            occurred_at_unix: number | null;
            /** @description Issue #307: fingerprint of the UPSTREAM governed action this row is a downstream effect of (canonical_target_sha256, "sha256:<hex>"), declared via x-ferrogate-parent-action-fingerprint on the A2A ingress. Null/absent when no parent was declared - never fabricated. */
            parent_action_fingerprint?: string | null;
            /** @description Issue #304: target-level action fingerprint under the canonical_target_sha256 contract ("sha256:<hex>"). Null/absent on rows recorded before migration 045 or without capability evidence. */
            action_fingerprint?: string | null;
            /** @description Issue #304: canonical governance decision class: allow | deny | ask | degrade. */
            decision?: string | null;
            /** @description Issue #304: stable decision reason code, e.g. capability_allowed, audit_redacted, guardrail:fail:block:enforced. */
            decision_reason?: string | null;
            /** @description Issue #304: what happened to the action's output: returned | redacted | withheld | errored. */
            output_disposition?: string | null;
        };
        TenantContext: {
            organization_id?: string | null;
            team_id?: string | null;
            project_id?: string | null;
            user_id?: string | null;
            api_key_id?: string | null;
        };
        TokenUsage: {
            prompt_tokens: number;
            completion_tokens: number;
            total_tokens: number;
        };
        StringList: string[];
        AdminProviderList: components["schemas"]["AdminList_AdminProvider"];
        ProviderHealthList: components["schemas"]["AdminList_ProviderHealthCheck"];
        AdminProviderModelCatalogList: components["schemas"]["AdminList_AdminProviderModelCatalog"];
        /** @description One registered BYOK provider credential, REDACTED. Derived from `TenantProviderCredentialSummary` in `packages/storage/src/d1/provider-credential-d1.ts`, which is the type the list handler is handed: it has no `iv` and no `ciphertext` field at all, so this API cannot serve the sealed material even by accident. `last4` is the only part of the credential any response carries, and it is the empty string for credentials shorter than eight characters (for a six-character value "the last four" is most of it). The field names are camelCase here because the store's summary object is serialized directly; the PUT response, built by hand in the handler, uses snake_case for the same values. */
        AdminProviderCredentialSummary: {
            /** @description Tenant-chosen name the `byok://` reference resolves through. */
            alias: string;
            /** @description `[[providers]].name` this credential authenticates against. */
            provider: string;
            /** @description Last four characters of the credential, or "" when it was shorter than eight. */
            last4: string;
            /**
             * Format: int64
             * @description Fleet keyring version the credential was sealed under.
             */
            keyVersion: number;
            /** Format: int64 */
            createdAtUnix: number;
            /**
             * Format: int64
             * @description Last successful PUT under this alias; equals createdAtUnix until the first rotation.
             */
            rotatedAtUnix: number;
            /**
             * Format: int64
             * @description Omitted entirely while the alias is live; set once DELETE has revoked it. Revoked aliases stay in the listing.
             */
            revokedAtUnix?: number;
        };
        /** @description The un-paginated `{ object: "list", data }` envelope; this listing takes no pagination query. */
        AdminProviderCredentialList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminProviderCredentialSummary"][];
        };
        /** @description Register or rotate one alias. `PUT` is idempotent-by-key: the first call registers, every later call rotates, so there is no `POST` and no create/update split a client could guess wrong. The tenant is taken from the CALLER's scope and is deliberately neither a body field nor a path segment. */
        AdminProviderCredentialMutation: {
            /** @description `[[providers]].name` this credential authenticates against. Required with no default: a guessed provider could present the key to an upstream the tenant never negotiated with. */
            provider: string;
            /**
             * Format: password
             * @description The provider API key. Sealed with the fleet keyring before it reaches storage and never returned by any operation on this API — deliberately carries no example.
             */
            value: string;
        };
        /** @description Read back from the store rather than echoed from the request, so it structurally cannot carry the key the caller just sent. Note the snake_case field names: this object is assembled in the handler, unlike the camelCase list rows. */
        AdminProviderCredentialMutationResponse: {
            /** @constant */
            object: "provider_credential";
            alias: string;
            provider: string;
            last4: string;
            /** Format: int64 */
            key_version: number;
            /**
             * Format: int64
             * @description Preserved across a rotation. `null` only if the row could not be read back after the write.
             */
            created_at_unix: number | null;
            /** Format: int64 */
            rotated_at_unix: number | null;
        };
        /** @description Keyed by `alias`, not by `id` — this surface has no opaque record id, so it is not the shared `DeleteResponse` shape. */
        AdminProviderCredentialRevokeResponse: {
            /** @constant */
            object: "provider_credential";
            alias: string;
            /** @constant */
            deleted: true;
        };
        ObservabilityStatusList: components["schemas"]["AdminList_ObservabilityStatus"];
        AdminExtensionList: components["schemas"]["AdminPluginList"];
        AdminPluginList: components["schemas"]["AdminList_AdminPlugin"];
        AdminToolList: components["schemas"]["AdminList_AdminTool"];
        ToolApprovalList: components["schemas"]["AdminList_ToolApprovalRecord"];
        AdminModelList: components["schemas"]["AdminList_Model"];
        AdminApiKeyList: components["schemas"]["AdminList_AdminApiKey"];
        AdminTenantRefList: components["schemas"]["AdminList_AdminTenantRef"];
        PolicyRuleList: components["schemas"]["AdminList_PolicyRule"];
        RequestLogList: components["schemas"]["PaginatedList_RequestLog"];
        AgentRunList: components["schemas"]["PaginatedList_AgentRunSummary"];
        RequestLogExportRecord: {
            /** @constant */
            object: "request_log_export";
            request_id: string;
            trace_id?: string | null;
            agent_run_id?: string | null;
            tenant: components["schemas"]["TenantContext"];
            route?: string | null;
            logical_model?: string | null;
            provider?: string | null;
            provider_model?: string | null;
            status_code: number;
            error_code?: string | null;
            usage?: components["schemas"]["TokenUsage"] | null;
            latency_ms?: number | null;
            prompt_recorded: boolean;
            response_recorded: boolean;
            prompt_body?: string | null;
            response_body?: string | null;
            started_at_unix?: number | null;
            completed_at_unix?: number | null;
        };
        AgentRunSummary: {
            /** @constant */
            object: "agent_run";
            id: string;
            tenant: components["schemas"]["TenantContext"];
            /** @enum {string} */
            status: "completed" | "blocked" | "failed";
            request_count: number;
            billing_event_count: number;
            audit_event_count: number;
            agent_event_count: number;
            first_seen_unix?: number | null;
            last_seen_unix?: number | null;
        };
        AgentRunTimeline: {
            /** @constant */
            object: "agent_run_timeline";
            id: string;
            run: components["schemas"]["StoredAgentRun"] | null;
            summary: components["schemas"]["AgentRunSummary"];
            agent_events: components["schemas"]["StoredAgentRunEvent"][];
            requests: components["schemas"]["RequestLog"][];
            billing_events: components["schemas"]["TokenMeteringEvent"][];
            audit_events: components["schemas"]["StoredAuditEvent"][];
        };
        StoredAgentRun: {
            id: string;
            request_id: string;
            trace_id?: string | null;
            tenant: components["schemas"]["TenantContext"];
            status: string;
            provider: string;
            turns_executed: number;
            output_recorded: boolean;
            started_at_unix?: number | null;
            completed_at_unix?: number | null;
        };
        StoredAgentRunEvent: {
            id: string;
            run_id: string;
            request_id: string;
            trace_id?: string | null;
            tenant: components["schemas"]["TenantContext"];
            turn: number;
            kind: string;
            target: string;
            outcome: string;
            tool_call_id?: string | null;
            message?: string | null;
            occurred_at_unix?: number | null;
            /** @description Issue #304: target-level action fingerprint under the canonical_target_sha256 contract ("sha256:<hex>"). Null/absent on rows recorded before migration 045 or without capability evidence. */
            action_fingerprint?: string | null;
            /** @description Issue #304: canonical governance decision class: allow | deny | ask | degrade. */
            decision?: string | null;
            /** @description Issue #304: stable decision reason code, e.g. capability_allowed, audit_redacted, guardrail:fail:block:enforced. */
            decision_reason?: string | null;
            /** @description Issue #304: what happened to the action's output: returned | redacted | withheld | errored. */
            output_disposition?: string | null;
        };
        UsageAggregateList: components["schemas"]["AdminList_StoredUsageAggregate"];
        MeteringExportStatusList: components["schemas"]["AdminList_MeteringExportStatus"];
        AuditEventList: components["schemas"]["PaginatedList_StoredAuditEvent"];
        AdminList_AdminProvider: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminProvider"][];
            total?: number;
            offset?: number;
            limit?: number;
        };
        AdminList_ProviderHealthCheck: {
            /** @constant */
            object: "list";
            data: components["schemas"]["ProviderHealthCheck"][];
        };
        AdminList_AdminProviderModelCatalog: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminProviderModelCatalog"][];
        };
        AdminList_ObservabilityStatus: {
            /** @constant */
            object: "list";
            data: components["schemas"]["ObservabilityStatus"][];
        };
        AdminList_AdminPlugin: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminPlugin"][];
        };
        AdminList_AdminTool: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminTool"][];
        };
        AdminList_ToolApprovalRecord: {
            /** @constant */
            object: "list";
            data: components["schemas"]["ToolApprovalRecord"][];
            total?: number | null;
            offset?: number | null;
            limit?: number | null;
        };
        AdminList_Model: {
            /** @constant */
            object: "list";
            data: components["schemas"]["Model"][];
            total?: number;
            offset?: number;
            limit?: number;
        };
        AdminList_AdminApiKey: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminApiKey"][];
        };
        AdminList_AdminTenantRef: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminTenantRef"][];
        };
        AdminList_PolicyRule: {
            /** @constant */
            object: "list";
            data: components["schemas"]["PolicyRule"][];
        };
        AdminList_StoredUsageAggregate: {
            /** @constant */
            object: "list";
            data: components["schemas"]["StoredUsageAggregate"][];
        };
        AdminList_MeteringExportStatus: {
            /** @constant */
            object: "list";
            data: components["schemas"]["MeteringExportStatus"][];
        };
        PaginatedList_RequestLog: {
            /** @constant */
            object: "list";
            data: components["schemas"]["RequestLog"][];
            total: number;
            offset: number;
            limit: number;
        };
        PaginatedList_AgentRunSummary: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AgentRunSummary"][];
            total: number;
            offset: number;
            limit: number;
        };
        PaginatedList_StoredAuditEvent: {
            /** @constant */
            object: "list";
            data: components["schemas"]["StoredAuditEvent"][];
            total: number;
            offset: number;
            limit: number;
        };
        ErrorResponse: {
            error: {
                message: string;
                /** @constant */
                type: "ferrogate_error";
                code: string;
                request_id: string | null;
            };
        };
        AgentRunCreateRequest: {
            input: string;
            /** @description Optional stable run id. The x-ferrogate-agent-run-id header takes precedence when both are present. */
            run_id?: string | null;
            max_turns?: number | null;
            timeout_millis?: number | null;
            /** @default [] */
            tool_calls: components["schemas"]["AgentRunToolCallRequest"][];
        };
        AgentRunToolCallRequest: {
            name: string;
            /** @default {} */
            arguments: Record<string, never>;
            route?: string | null;
            session_id?: string | null;
        };
        AgentRunCreateResponse: {
            /** @constant */
            object: "agent_run";
            id: string;
            /** @enum {string} */
            status: "completed" | "max_turns_exceeded" | "cancelled" | "timed_out" | "failed";
            turns_executed: number;
            output: string | null;
            tool_results: components["schemas"]["ToolResult"][];
            request_id: string;
        };
        AgentJobSubmitRequest: {
            /** @description The long-running task handed to the agent runtime. */
            input: string;
            /** @description Explicit idempotency key. The Idempotency-Key header takes precedence when both are present. A retried submit with the same key returns the original run_id. */
            idempotency_key?: string | null;
            /** @description Runtime framework adapter to lease this job to. Defaults to native-harness. */
            framework_adapter?: string | null;
            /** @description Capabilities a worker must advertise to lease this job. Optional: omit or send [] to let the runtime apply its own default (currently ["shell"]). */
            required_capabilities?: string[];
            /** @description Opaque workload reference handed to the runtime. Defaults to agent-job://{run_id}. */
            workload_ref?: string | null;
        };
        AgentJobSubmitResponse: {
            /** @constant */
            object: "agent_job";
            /** @description The durable job id. Retries of the same idempotency key return this same value. */
            run_id: string;
            status: string;
            /** @description The effective key this job is keyed on. */
            idempotency_key: string;
            /** @enum {string} */
            idempotency_key_source: "header" | "body" | "request_id";
            /** @description true when this submit matched an existing job and started nothing new. */
            deduplicated: boolean;
            terminal: boolean;
            submitted_at_unix: number | null;
            status_url: string;
            events_url: string;
            result_url: string;
            request_id: string;
        };
        AgentJobStatus: {
            /** @constant */
            object: "agent_job";
            run_id: string;
            status: string;
            terminal: boolean;
            provider: string | null;
            turns_executed: number;
            output_recorded: boolean;
            event_count: number;
            started_at_unix: number | null;
            completed_at_unix: number | null;
            first_seen_unix: number | null;
            last_seen_unix: number | null;
            /** @description Latest lifecycle state the runtime itself reported for this run, from the self-hosted run timeline. */
            runtime_reported_state: string | null;
            runtime_reported_event_count: number;
            request_id: string;
        };
        AgentJobEventPage: {
            /** @constant */
            object: "agent_job_event_page";
            run_id: string;
            data: components["schemas"]["StoredAgentRunEvent"][];
            limit: number;
            after_event_id: string | null;
            next_after_event_id: string | null;
            has_more: boolean;
            /** @description True when after_event_id could not be located in the run's current event set (retention pruned it, or the caller invented it) and this page therefore restarts from the run's oldest retained event. The caller may re-see events; a poll loop is never left with a permanently unusable cursor. */
            cursor_reset: boolean;
            request_id: string;
        };
        AgentJobArtifact: {
            id: string;
            worker_id: string;
            occurred_at_unix: number | null;
            event_json: string;
        };
        AgentJobResult: {
            /** @constant */
            object: "agent_job_result";
            run_id: string;
            status: string;
            terminal: boolean;
            turns_executed: number;
            output_recorded: boolean;
            output: string | null;
            /** @description Artifact evidence the runtime reported for this run. */
            artifacts: components["schemas"]["AgentJobArtifact"][];
            /** @description Coding-agent work products (#472) decoded from this run's artifact events and re-verified against the run_id in the path. Empty for non-coding jobs. */
            work_products: components["schemas"]["CodingAgentWorkProduct"][];
            request_count: number;
            billing_event_count: number;
            completed_at_unix: number | null;
            request_id: string;
        };
        AgentJobCancelResponse: {
            /** @constant */
            object: "agent_job_cancel";
            run_id: string;
            status: string;
            terminal: boolean;
            /** @description true when this call terminalized the run; false when it was already terminal. */
            cancelled: boolean;
            /** @description true when a cancel_run dispatch was handed to the runtime transport, which happens whenever a StartRun dispatch exists but the serving node could not withdraw an unleased copy from its own queue: a local worker had already leased the job, or only a durable copy was visible because another replica may hold the runnable work. false means no cancel_run was emitted: the serving node withdrew its local unleased copy, no StartRun dispatch existed, or the run was already terminal. A local withdrawal does not prove that copy was unique; when this call terminalizes an unleased run, separate settled-run cleanup reclaims its shared StartRun row and the terminal run row prevents peers from leasing copies they restored earlier. The field reports WHICH runtime remedy ran, never whether the cancel took effect -- `cancelled` reports whether this call terminalized the run. */
            runtime_cancel_dispatched: boolean;
            cancelled_at_unix: number | null;
            request_id: string;
        };
        ToolResult: {
            tool_call_id: string;
            content: unknown;
            is_error: boolean;
        };
        TokenMeteringEvent: {
            request_id: string;
            trace_id?: string | null;
            agent_run_id?: string | null;
            cluster_id?: string | null;
            node_id?: string | null;
            tenant?: components["schemas"]["TenantContext"];
            logical_model: string;
            provider: string;
            provider_model: string;
            usage: components["schemas"]["TokenUsage"];
            /** @enum {string} */
            usage_source: "provider_usage" | "gateway_estimate";
            status_code: number;
            occurred_at_unix?: number | null;
        };
        TokenMeteringEventList: components["schemas"]["PaginatedList_TokenMeteringEvent"];
        PaginatedList_TokenMeteringEvent: {
            /** @constant */
            object: "list";
            data: components["schemas"]["TokenMeteringEvent"][];
            total: number;
            offset: number;
            limit: number;
        };
        McpJsonRpcRequest: {
            /** @default 2.0 */
            jsonrpc: string;
            id?: unknown;
            /** @enum {string} */
            method: "initialize" | "notifications/initialized" | "ping" | "resources/list" | "resources/read" | "server/discover" | "tools/list" | "tools/call";
            /** @default {} */
            params: Record<string, never>;
        } & {
            [key: string]: unknown;
        };
        McpJsonRpcResponse: {
            /** @constant */
            jsonrpc: "2.0";
            id?: unknown;
            result?: unknown;
            error?: components["schemas"]["McpJsonRpcError"];
        };
        McpJsonRpcError: {
            code: number;
            message: string;
            /** @description Protocol-defined structured error data, including requested/supported versions for -32022. */
            data?: {
                [key: string]: unknown;
            };
        };
        ToolExecutionRequest: {
            /** @description Namespaced tool name, for MCP using serverName-toolName. */
            name: string;
            /** @default {} */
            arguments: Record<string, never>;
            route?: string | null;
            session_id?: string | null;
        };
        ToolExecutionResponse: {
            /** @constant */
            object: "tool_execution";
            name: string;
            content: unknown;
            is_error: boolean;
            request_id: string;
            session_id: string | null;
            latency_ms: number;
        };
        McpServerStatus: {
            name: string;
            /** @enum {string} */
            transport: "streamable_http" | "sse" | "stdio";
            connected: boolean;
            /** @enum {string} */
            health: "ok" | "degraded";
            tools: number;
            reconnect_attempts: number;
            last_error: string | null;
            last_connected_at_unix: number | null;
            next_reconnect_backoff_secs: number;
            /** @enum {string|null} */
            protocol_version?: "2026-07-28" | "2025-11-25" | "2025-06-18" | null;
            /** @enum {string|null} */
            protocol_mode?: "modern" | "legacy" | null;
            /** @enum {string|null} */
            protocol_downgrade_reason?: "http400_unrecognized_response" | "http404_unrecognized_response" | "http405_unrecognized_response" | "stdio_method_not_found" | null;
        };
        AdminList_McpServerStatus: {
            /** @constant */
            object: "list";
            data: components["schemas"]["McpServerStatus"][];
            total?: number;
            offset?: number;
            limit?: number;
        };
        McpServerStatusList: components["schemas"]["AdminList_McpServerStatus"];
        AdminMcpServerMutation: components["schemas"]["McpServerConfig"];
        AdminMcpServerMutationResponse: {
            /** @constant */
            object: "mcp_server";
            server: components["schemas"]["McpServerStatus"];
        };
        /** @description MCP server status response. */
        AdminMcpServerStatusOk: unknown;
        /** @description MCP server mutation response. */
        AdminMcpServerMutationOk: unknown;
        DeleteMcpServerResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "mcp_server";
        };
        AdminGatewayConfigProfile: {
            id: string;
            name: string;
            revision: number;
            enabled: boolean;
            api_key_ids: components["schemas"]["StringList"];
            /** @description Optional request-time override for exact-match AI response caching within the global cache policy. */
            cache_enabled?: boolean | null;
        };
        AdminGatewayConfigProfileList: components["schemas"]["AdminList_AdminGatewayConfigProfile"];
        AdminList_AdminGatewayConfigProfile: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminGatewayConfigProfile"][];
        };
        AdminAgentWorkflowList: components["schemas"]["AdminList_AdminAgentWorkflow"];
        AdminList_AdminAgentWorkflow: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminAgentWorkflow"][];
        };
        AdminAgentSchedule: {
            /** @constant */
            object: "agent_schedule";
            id: string;
            tenant_id: string;
            workspace_id: string;
            name: string;
            enabled: boolean;
            /** @enum {string} */
            spec_kind: "cron" | "interval";
            cron_expr?: string | null;
            timezone: string;
            /** Format: int64 */
            interval_secs?: number | null;
            /** @enum {string} */
            target_kind: "self_hosted_dispatch" | "agent_run";
            target: unknown;
            /** @enum {string} */
            overlap_policy: "skip" | "allow";
            /** @enum {string} */
            catchup_policy: "skip_missed" | "fire_once";
            /** Format: int64 */
            jitter_secs: number;
            /** Format: int64 */
            next_fire_at_unix?: number | null;
            /** Format: int64 */
            last_fire_at_unix?: number | null;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
            /** Format: int64 */
            revision: number;
        };
        AdminAgentScheduleFire: {
            /** @constant */
            object: "agent_schedule_fire";
            fire_id: string;
            schedule_id: string;
            /** Format: int64 */
            scheduled_fire_at_unix: number;
            /** Format: int64 */
            fired_at_unix: number;
            node_id?: string | null;
            /** @enum {string} */
            outcome: "dispatched" | "skipped_overlap" | "skipped_disabled" | "error";
            dispatch_id?: string | null;
            run_id?: string | null;
            detail?: string | null;
        };
        AdminAgentScheduleMutation: {
            id?: string;
            tenant_id?: string;
            workspace_id?: string;
            name?: string;
            enabled?: boolean;
            /** @enum {string} */
            spec_kind?: "cron" | "interval";
            cron_expr?: string;
            timezone?: string;
            /** Format: int64 */
            interval_secs?: number;
            /** @enum {string} */
            target_kind?: "self_hosted_dispatch" | "agent_run";
            target?: Record<string, never>;
            /** @enum {string} */
            overlap_policy?: "skip" | "allow";
            /** @enum {string} */
            catchup_policy?: "skip_missed" | "fire_once";
            /** Format: int64 */
            jitter_secs?: number;
        };
        AdminAgentScheduleMutationResponse: {
            /** @constant */
            object: "agent_schedule";
            agent_schedule: components["schemas"]["AdminAgentSchedule"];
        };
        AdminAgentScheduleRunNowResponse: {
            /** @constant */
            object: "agent_schedule_fire";
            fire: components["schemas"]["AdminAgentScheduleFire"];
        };
        AdminAgentScheduleList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminAgentSchedule"][];
        };
        AdminAgentScheduleFireList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminAgentScheduleFire"][];
        };
        /** @enum {string} */
        PromptTemplateStatus: "draft" | "active" | "archived";
        /** @enum {string} */
        PromptTemplateTarget: "chat_completions" | "responses";
        McpServerConfig: {
            name: string;
            /** @enum {string} */
            transport: "streamable_http" | "sse" | "stdio";
            url?: string | null;
            command?: string | null;
            args?: components["schemas"]["StringList"];
            auth_type?: components["schemas"]["McpAuthType"];
            headers?: components["schemas"]["McpHeaderConfig"][];
            oauth?: components["schemas"]["McpOauthConfig"] | null;
            signed_jwt_audience?: string | null;
            tools_to_execute: components["schemas"]["StringList"];
            tools_to_auto_execute?: components["schemas"]["StringList"];
            approval_policy?: components["schemas"]["ApprovalPolicy"];
            tool_include?: components["schemas"]["StringList"];
            tool_regex?: components["schemas"]["StringList"];
            tls?: components["schemas"]["McpTlsConfig"];
            timeout_ms?: number;
            health_ping_interval_secs?: number;
            max_reconnect_attempts?: number;
            min_reconnect_backoff_secs?: number;
            max_reconnect_backoff_secs?: number;
        };
        /** @enum {string} */
        McpAuthType: "none" | "shared_headers" | "oauth" | "per_user_oauth" | "per_user_headers" | "original_bearer" | "ferrogate_signed_jwt";
        McpOauthConfig: {
            /** Format: uri */
            issuer: string;
            client_id: string;
            /** @description Secret resolver reference; resolved value is never returned by the API. */
            client_secret_ref?: string | null;
            /** Format: uri */
            redirect_uri?: string | null;
            scopes?: components["schemas"]["StringList"];
            audience?: string | null;
            /** @default false */
            allow_insecure_http: boolean;
        };
        McpOauthAuthorizeResponse: {
            /** @constant */
            object: "mcp_oauth_authorization";
            server_name: string;
            /** Format: uri */
            authorize_url: string;
            state: string;
            expires_at_unix: number;
        };
        McpIdentityStatus: {
            /** @constant */
            object: "mcp_identity";
            server_name: string;
            auth_type: components["schemas"]["McpAuthType"];
            connected: boolean;
            credential_source: string;
            subject?: string | null;
            expires_at_unix?: number | null;
            revoked_at_unix?: number | null;
            last_refresh_outcome?: string | null;
            last_revocation_outcome?: string | null;
        };
        McpHeaderConfig: {
            name: string;
            value?: string | null;
            value_env?: string | null;
        };
        McpTlsConfig: {
            /** @default false */
            insecure_skip_verify: boolean;
            ca_cert_path?: string | null;
        };
        /** @enum {string} */
        PromptTemplateVersionStatus: "draft" | "active" | "archived";
        PromptTemplateVariable: {
            name: string;
            /** @default true */
            required: boolean;
            default?: string | null;
            description?: string | null;
        };
        PromptTemplateMessage: {
            /** @enum {string} */
            role: "system" | "developer" | "user" | "assistant" | "tool";
            content: string;
        };
        PromptTemplateVersion: {
            /** @default 1 */
            revision: number;
            status?: components["schemas"]["PromptTemplateVersionStatus"];
            messages: components["schemas"]["PromptTemplateMessage"][];
            temperature?: number | null;
            top_p?: number | null;
            max_tokens?: number | null;
        };
        AdminPromptTemplate: {
            id: string;
            name: string;
            status: components["schemas"]["PromptTemplateStatus"];
            target: components["schemas"]["PromptTemplateTarget"];
            model: string;
            variables: components["schemas"]["PromptTemplateVariable"][];
            active_revision?: number | null;
            versions: components["schemas"]["PromptTemplateVersion"][];
        };
        AdminPromptTemplateMutation: {
            id?: string;
            name?: string;
            status?: components["schemas"]["PromptTemplateStatus"];
            target?: components["schemas"]["PromptTemplateTarget"];
            model?: string;
            variables?: components["schemas"]["PromptTemplateVariable"][];
            version?: components["schemas"]["PromptTemplateVersion"];
            versions?: components["schemas"]["PromptTemplateVersion"][];
        };
        AdminPromptTemplateMutationResponse: {
            /** @constant */
            object: "prompt_template";
            prompt_template: components["schemas"]["AdminPromptTemplate"];
        };
        DeletePromptTemplateResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "prompt_template";
        };
        AdminPromptTemplateList: components["schemas"]["AdminList_AdminPromptTemplate"];
        /** @description A deployment label: a mutable name pointing at one prompt template revision. Stored twice on purpose — as this admin document (listable, tenant-fenced) and as the KV pointer the gateway reads on the inference hot path. `tenant_id` is taken from the TEMPLATE, not from the caller, so a platform operator acting on a tenant's template writes into that tenant's key space. */
        AdminPromptTemplateLabel: {
            /** @description Store id, `{template_id}::{label}`. */
            id: string;
            template_id: string;
            /** @description Normalized label name. A segment outside the label grammar is 400 `invalid_prompt_label`, not 404. */
            label: string;
            /** @description Template revision this label points at. Existence is NOT validated here; the gateway refuses an unresolvable pointer with 404 `prompt_template_version_not_found`. */
            revision: number;
            /** Format: int64 */
            updated_at_unix: number;
            /** @description Authenticated subject that last moved the label, or null. */
            updated_by?: string | null;
            tenant_id?: string | null;
        };
        /** @description The un-paginated `{ object: "list", data }` envelope, sorted by label. The handler forces the template filter and ignores pagination, so `total`/`offset`/`limit` are never sent. */
        AdminPromptTemplateLabelList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminPromptTemplateLabel"][];
        };
        /** @description The whole body: a label carries exactly one field. */
        AdminPromptTemplateLabelMutation: {
            /** @description Template revision the label should point at. Must be a whole number, 1 or greater. */
            revision: number;
        };
        AdminPromptTemplateLabelMutationResponse: {
            /** @constant */
            object: "prompt_template_label";
            prompt_template_label: components["schemas"]["AdminPromptTemplateLabel"];
        };
        /** @description `id` carries the LABEL name here, not the `{template_id}::{label}` store id. */
        DeletePromptTemplateLabelResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "prompt_template_label";
        };
        AdminList_AdminPromptTemplate: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminPromptTemplate"][];
        };
        PromptTemplateRenderRequest: {
            variables?: {
                [key: string]: unknown;
            };
            revision?: number | null;
        };
        RenderedPromptRequest: {
            model: string;
            messages?: components["schemas"]["PromptTemplateMessage"][];
            input?: components["schemas"]["PromptTemplateMessage"][];
            temperature?: number;
            top_p?: number;
            max_tokens?: number;
        } & {
            [key: string]: unknown;
        };
        /** @enum {string} */
        SkillPackageCapabilityKind: "plugin" | "tool" | "mcp_server" | "mcp_tool" | "prompt_template" | "agent_workflow";
        SkillPackageCapability: {
            kind: components["schemas"]["SkillPackageCapabilityKind"];
            id: string;
            description?: string | null;
        };
        SkillPackageCompatibility: {
            min_gateway_version?: string | null;
            agent_runtimes?: components["schemas"]["StringList"];
        };
        SkillPackagePluginConfig: {
            id: string;
            /** @enum {string} */
            kind: "request_hook" | "tool_provider" | "event_sink";
            version?: string;
            manifest?: components["schemas"]["PluginManifest"];
            compatibility?: components["schemas"]["PluginCompatibility"];
            /** @default true */
            enabled: boolean;
            source?: string;
            order?: number;
            approval_policy?: components["schemas"]["ApprovalPolicy"];
            permissions?: components["schemas"]["ExtensionPermissions"];
            config?: {
                [key: string]: unknown;
            };
        };
        SkillPackagePromptTemplate: {
            id: string;
            name: string;
            /** @enum {string} */
            status?: "draft" | "active" | "archived";
            /** @enum {string} */
            target?: "chat_completions" | "responses";
            model: string;
            variables?: {
                [key: string]: unknown;
            }[];
            versions: {
                [key: string]: unknown;
            }[];
        };
        SkillPackageResources: {
            /** @default [] */
            plugins: components["schemas"]["SkillPackagePluginConfig"][];
            /** @default [] */
            mcp_servers: components["schemas"]["McpServerConfig"][];
            /** @default [] */
            prompt_templates: components["schemas"]["SkillPackagePromptTemplate"][];
            /** @default [] */
            agent_workflows: components["schemas"]["AgentWorkflowPolicy"][];
        };
        SkillPackage: {
            id: string;
            name: string;
            /** @default 0.1.0 */
            version: string;
            description?: string | null;
            /** @default true */
            enabled: boolean;
            compatibility?: components["schemas"]["SkillPackageCompatibility"];
            permissions?: components["schemas"]["ExtensionPermissions"];
            capabilities: components["schemas"]["SkillPackageCapability"][];
            resources?: components["schemas"]["SkillPackageResources"];
            api_key_ids?: components["schemas"]["StringList"];
            metadata?: {
                [key: string]: unknown;
            };
        };
        AdminSkillPackage: components["schemas"]["SkillPackage"];
        AgentUpstreamDiscovery: {
            /** @constant */
            object: "agent_upstream";
            id: string;
            name: string;
            description?: string | null;
            protocol: components["schemas"]["AgentUpstreamProtocol"];
            endpoint: string;
            capabilities: components["schemas"]["AgentUpstreamCapability"][];
        };
        AgentUpstreamDiscoveryList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AgentUpstreamDiscovery"][];
        };
        /** @enum {string} */
        AgentUpstreamProtocol: "a2a";
        /** @enum {string} */
        AgentUpstreamCapability: "invoke" | "read" | "stream" | "discover";
        AgentUpstreamAuth: {
            none: Record<string, never> | null;
        } | {
            bearer: {
                token?: string | null;
            };
        } | {
            header: {
                name: string;
                value?: string | null;
            };
        };
        AgentSkillPackage: {
            id: string;
            name: string;
            version: string;
            description?: string | null;
            capabilities: components["schemas"]["SkillPackageCapability"][];
            compatibility: components["schemas"]["SkillPackageCompatibility"];
        };
        AdminSkillPackageMutationResponse: {
            /** @enum {string} */
            object: "skill_package";
            skill_package: components["schemas"]["AdminSkillPackage"];
        };
        DeleteSkillPackageResponse: components["schemas"]["DeleteResponse"];
        AdminSkillPackageList: components["schemas"]["AdminList_AdminSkillPackage"];
        AgentSkillPackageList: components["schemas"]["AdminList_AgentSkillPackage"];
        AdminAgentUpstreamList: components["schemas"]["AdminList_AdminAgentUpstream"];
        AdminList_AdminSkillPackage: {
            /** @enum {string} */
            object: "list";
            data: components["schemas"]["AdminSkillPackage"][];
        };
        AdminList_AgentSkillPackage: {
            /** @enum {string} */
            object: "list";
            data: components["schemas"]["AgentSkillPackage"][];
        };
        AdminManagedWorkerRuntimeList: components["schemas"]["AdminList_AdminManagedWorkerRuntime"];
        AdminList_AdminManagedWorkerRuntime: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminManagedWorkerRuntime"][];
        };
        AdminManagedWorkerRuntime: {
            id: string;
            /** @enum {string} */
            status: "contract_ready";
            /** @constant */
            process_name: "agent-worker";
            /** @constant */
            process_boundary: "external_process";
            gateway_role: string;
            /** @constant */
            agent_worker_role: "microvm_lifecycle_controller";
            lifecycle_actions: ("prepare" | "start" | "exec_or_attach" | "stop" | "snapshot_or_checkpoint" | "collect_logs" | "collect_artifacts" | "cleanup")[];
            isolation_backends: components["schemas"]["AdminManagedWorkerIsolationBackend"][];
            /** @constant */
            capability_boundary: "gateway_mediated";
            capability_policy: components["schemas"]["AdminManagedWorkerCapabilityPolicy"];
            persistence: components["schemas"]["AdminManagedWorkerPersistence"];
        };
        AdminManagedWorkerCapabilityPolicy: {
            revision: string;
            /** @enum {string} */
            class_only_policy_mode: "deny" | "legacy_class_wide";
            /** @description False while explicit legacy_class_wide migration mode permits class-only grants. */
            target_level_enforced: boolean;
            /**
             * @description Stable fingerprint emitted for approval and Guardrail consumers.
             * @constant
             */
            action_fingerprint_contract: "canonical_target_sha256";
            /**
             * @description False until the #200 exact-action approval workflow consumes and rechecks this fingerprint.
             * @constant
             */
            exact_action_approval_enforced: false;
            target_grants: components["schemas"]["AdminManagedWorkerTargetGrant"][];
        };
        AdminManagedWorkerTargetGrant: {
            selector_id: string;
            /** @description DB-backed RBAC action key required by this target grant; role names are not interpreted. */
            permission_key: string;
            /** @enum {string} */
            action: "mcp.tool" | "filesystem" | "rest" | "network.egress" | "secret" | "cli";
            selector: components["schemas"]["CapabilityTargetSelector"];
        };
        CapabilityTargetSelector: {
            /** @constant */
            kind: "mcp";
            server: string;
            tool: string;
            /** @enum {string} */
            risk: "read" | "write";
            argument_schema: components["schemas"]["JsonShape"];
            allow_extra_arguments: boolean;
        } | {
            /** @constant */
            kind: "filesystem";
            workspace_root: string;
            path_glob: string;
            operations: ("read" | "write" | "delete" | "execute")[];
        } | {
            /** @constant */
            kind: "network";
            /** @enum {string} */
            scheme: "http" | "https" | "tcp" | "tls";
            host: string;
            port: number;
            method: string | null;
            path_glob: string;
            allowed_ips: string[];
            allow_redirects: boolean;
        } | {
            /** @constant */
            kind: "secret";
            reference_namespace: string;
            reference_name: string;
            destination_adapter: string;
            destination_action: string;
        } | {
            /** @constant */
            kind: "cli";
            executable: string;
            argv: string[];
            /** @description Must be empty; managed CLI execution clears the inherited environment. */
            environment: Record<string, never>;
            cwd_glob: string;
            max_timeout_millis: number;
            max_stdout_bytes: number;
            max_stderr_bytes: number;
        };
        JsonShape: {
            /** @constant */
            kind: "null";
        } | {
            /** @constant */
            kind: "boolean";
        } | {
            /** @constant */
            kind: "number";
        } | {
            /** @constant */
            kind: "string";
        } | {
            /** @constant */
            kind: "array";
            items: components["schemas"]["JsonShape"];
        } | {
            /** @constant */
            kind: "object";
            fields: {
                [key: string]: components["schemas"]["JsonShape"];
            };
        };
        AdminManagedWorkerIsolationBackend: {
            /** @enum {string} */
            kind: "firecracker_microvm" | "kata_containers" | "gvisor" | "rootless_docker";
            backend_name: string;
            commercial_preference: number;
            /** @constant */
            host_lifecycle_owner: "agent-worker";
            /** @constant */
            gateway_controls_backend: false;
        };
        AdminManagedWorkerPersistence: {
            provider: components["schemas"]["StorageProviderKind"];
            durable: boolean;
            /** @description False until managed worker template, session lifecycle, agent-worker transport, and evidence records are all persisted through the Supabase-backed control-plane storage path. */
            implemented: boolean;
            /** @description True when managed run/timeline evidence can use the existing agent_runs and agent_run_events persistence path. */
            timeline_evidence_implemented: boolean;
            /** @description True when the active Supabase/Postgres schema contains the managed worker template, agent-worker instance, session, and lifecycle event tables. */
            session_lifecycle_schema_ready: boolean;
            /** @description True only after managed worker scheduler/create/cancel paths write session lifecycle records through durable storage. */
            session_lifecycle_implemented: boolean;
            /** @description True only after the gateway has a real agent-worker control transport rather than a contract-only boundary. */
            agent_worker_transport_implemented: boolean;
            contract_version: number;
        };
        AdminFrameworkAdapterRuntimeList: components["schemas"]["AdminList_AdminFrameworkAdapterRuntime"];
        AdminList_AdminFrameworkAdapterRuntime: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminFrameworkAdapterRuntime"][];
        };
        AdminFrameworkAdapterRuntime: {
            id: string;
            /** @enum {string} */
            framework: "claude_code" | "codex" | "hermes" | "native_harness";
            adapter_name: string;
            adapter_version: string;
            enabled: boolean;
            /** @enum {string} */
            integration_status: "contract_ready" | "process_shim_contract_ready" | "shim_pending";
            modes: ("managed" | "self_hosted")[];
            capabilities: ("tools" | "mcp" | "memory.read" | "memory.write" | "checkpoint" | "artifacts" | "subagents" | "filesystem" | "shell" | "browser" | "rest.egress" | "secrets.read" | "streaming")[];
            /** @constant */
            event_schema: "normalized_worker_event";
            /** @constant */
            managed_capability_boundary: "gateway_mediated";
            /** @constant */
            self_hosted_trust_level: "reported_by_self_hosted_worker";
            /** @constant */
            public_api_exposes_framework_details: false;
            persistence: components["schemas"]["AdminFrameworkAdapterPersistence"];
        };
        AdminFrameworkAdapterPersistence: {
            /** @constant */
            implemented: true;
            /** @constant */
            provider: "supabase_postgres";
            /** @constant */
            session_table: "managed_worker_sessions";
            /** @constant */
            lifecycle_event_table: "managed_worker_lifecycle_events";
            /** @constant */
            normalized_event_table: "agent_run_events";
            /** @constant */
            session_records_implemented: true;
            /** @constant */
            lifecycle_event_records_implemented: true;
            /** @constant */
            normalized_event_records_implemented: true;
        };
        AdminSelfHostedWorkerRuntimeList: components["schemas"]["AdminList_AdminSelfHostedWorkerRuntime"];
        AdminList_AdminSelfHostedWorkerRuntime: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminSelfHostedWorkerRuntime"][];
        };
        AdminSelfHostedWorkerRuntime: {
            id: string;
            /** @enum {string} */
            status: "contract_ready";
            /** @constant */
            execution_owner: "customer";
            /** @constant */
            enforcement_boundary: "customer_owned_host";
            /** @constant */
            trust_level: "reported_by_self_hosted_worker";
            identity_scope: ("tenant_id" | "workspace_id" | "worker_id")[];
            transport_actions: ("register_worker" | "probe_worker" | "heartbeat" | "start_run" | "cancel_run" | "resume_run" | "close_session" | "poll_run" | "ack_run" | "stream_events" | "upload_artifact" | "fetch_checkpoint")[];
            telemetry_kinds: ("lifecycle" | "log" | "tool_call" | "mcp_call" | "cli_command" | "skill_invocation" | "artifact" | "checkpoint" | "usage")[];
            dispatch_contract: components["schemas"]["AdminSelfHostedWorkerDispatchContract"];
            registration_api: components["schemas"]["AdminSelfHostedWorkerSurface"];
            persistence: components["schemas"]["AdminSelfHostedWorkerPersistence"];
        };
        AdminSelfHostedWorkerDispatchContract: {
            /** @description True when the runner-style poll/lease/ack runtime contract is implemented. */
            implemented: boolean;
            /**
             * @description Remote workers initiate the transport session and poll for leased work instead of requiring inbound customer-host reachability.
             * @enum {string}
             */
            transport_shape: "worker_initiated_outbound_polling";
            /** @description Current self-hosted worker poll/ack wire protocol version accepted by the gateway. */
            current_protocol_version: number;
            /** @description Minimum self-hosted worker poll/ack wire protocol version accepted by the gateway. */
            minimum_supported_protocol_version: number;
            /** @description True when the runtime contract supports leased dispatch and worker acknowledgements. */
            lease_ack_implemented: boolean;
            /** @description False for the primary self-hosted worker transport; FerroGate should not require inbound access to customer hosts. */
            inbound_customer_host_required: boolean;
            /** @description True only after the production mutually authenticated HTTP worker transport is implemented. */
            production_mtls_transport_implemented: boolean;
            actions: ("start_run" | "cancel_run" | "resume_run" | "close_session" | "poll_run" | "ack_run")[];
        };
        AdminSelfHostedWorkerSurface: {
            implemented: boolean;
            planned_paths: string[];
        };
        AdminSelfHostedWorkerPersistence: {
            provider: components["schemas"]["StorageProviderKind"];
            durable: boolean;
            /** @description Overall self-hosted worker persistence/transport completion. Remains false until durable registration, heartbeat, telemetry, artifact, checkpoint, rotation, stale visibility, and real worker transport are all implemented. */
            implemented: boolean;
            contract_version: number;
            /** @description True when POST /admin/v1/self-hosted-workers writes durable self-hosted worker registrations. */
            registration_implemented: boolean;
            /** @description True when GET /admin/v1/self-hosted-workers/{id} reads storage-backed worker details. */
            detail_implemented: boolean;
            /** @description True when heartbeat/status ingestion writes durable heartbeat records. */
            heartbeat_implemented: boolean;
            /** @description True when telemetry event ingestion writes durable event records. */
            telemetry_event_implemented: boolean;
            /** @description True when artifact metadata ingestion writes durable artifact metadata records. */
            artifact_metadata_implemented: boolean;
            /** @description True when checkpoint metadata ingestion writes durable checkpoint metadata records. */
            checkpoint_metadata_implemented: boolean;
            /** @description True when Admin rotation updates the durable identity fingerprint projection. */
            identity_fingerprint_rotation_implemented: boolean;
            /** @description True when Admin records expose stale visibility derived from durable heartbeat evidence. */
            stale_visibility_implemented: boolean;
            /** @description True when the worker-facing HTTP wire contract paths are implemented. This does not imply production mTLS is implemented; check dispatch_contract.production_mtls_transport_implemented for that boundary. */
            worker_transport_implemented: boolean;
            /** @description Worker-facing transport paths currently implemented behind the local HTTP wire contract marker. */
            worker_transport_paths: ("/v1/self-hosted-workers/heartbeat" | "/v1/self-hosted-workers/events" | "/v1/self-hosted-workers/artifacts" | "/v1/self-hosted-workers/checkpoints" | "/v1/self-hosted-workers/runs/poll" | "/v1/self-hosted-workers/runs/ack")[];
        };
        AdminSelfHostedWorkerRecordList: components["schemas"]["AdminList_AdminSelfHostedWorkerRecord"];
        AdminSelfHostedWorkerRegistrationRequest: {
            tenant: components["schemas"]["TenantContext"];
            workspace_id: string;
            worker_name: string;
            identity_fingerprint: string;
            /** @description Optional Unix timestamp after which the self-hosted worker identity is rejected by worker-facing transport APIs. */
            identity_expires_at_unix?: number | null;
            /** @default false */
            orchestration_enabled: boolean;
            /** @description Optional worker capability envelope document encoded as JSON. It remains customer-reported metadata for self-hosted workers. */
            capability_envelope_json?: string | null;
        };
        AdminSelfHostedWorkerRegistrationResponse: {
            /** @constant */
            object: "self_hosted_worker";
            worker: components["schemas"]["AdminSelfHostedWorkerRecord"];
        };
        AdminSelfHostedWorkerHeartbeatRequest: {
            status: string;
            reported_at_unix?: number | null;
            /** @description Optional customer-reported heartbeat document encoded as JSON. */
            heartbeat_json?: string | null;
        };
        AdminSelfHostedWorkerHeartbeatResponse: {
            /** @constant */
            object: "self_hosted_worker_heartbeat";
            worker: components["schemas"]["AdminSelfHostedWorkerRecord"];
            heartbeat: components["schemas"]["AdminSelfHostedWorkerHeartbeat"];
        };
        AdminSelfHostedWorkerTelemetryEventRequest: {
            session_id: string;
            run_id: string;
            /** @enum {string} */
            kind: "lifecycle" | "log" | "tool_call" | "mcp_call" | "cli_command" | "skill_invocation" | "artifact" | "checkpoint" | "usage";
            occurred_at_unix?: number | null;
            /** @description Optional customer-reported telemetry event document encoded as JSON. */
            event_json?: string | null;
        };
        AdminSelfHostedWorkerTelemetryEventResponse: {
            /** @constant */
            object: "self_hosted_worker_event";
            worker: components["schemas"]["AdminSelfHostedWorkerRecord"];
            event: components["schemas"]["AdminSelfHostedWorkerTelemetryEvent"];
        };
        AdminSelfHostedWorkerTelemetryEvent: {
            id: string;
            worker_id: string;
            session_id?: string | null;
            run_id?: string | null;
            kind: string;
            /** @constant */
            trust_level: "reported_by_self_hosted_worker";
            occurred_at_unix?: number | null;
            ingested_at_unix?: number | null;
        };
        AdminList_AdminSelfHostedWorkerRecord: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminSelfHostedWorkerRecord"][];
            total: number;
            offset: number;
            limit: number;
        };
        AdminSelfHostedWorkerRecord: {
            id: string;
            tenant: components["schemas"]["TenantContext"];
            workspace_id: string;
            worker_name: string;
            status: string;
            identity_fingerprint: string;
            /** @description Unix timestamp after which the self-hosted worker identity is rejected by worker-facing transport APIs. Null means no gateway-enforced identity expiry. */
            identity_expires_at_unix: number | null;
            orchestration_enabled: boolean;
            registered_at_unix?: number | null;
            last_seen_at_unix?: number | null;
            /** @constant */
            trust_level: "reported_by_self_hosted_worker";
            latest_heartbeat: components["schemas"]["AdminSelfHostedWorkerHeartbeat"] | null;
            telemetry_event_count: number;
            artifact_count: number;
            checkpoint_count: number;
            latest_event_at_unix?: number | null;
            latest_artifact_at_unix?: number | null;
            latest_checkpoint_at_unix?: number | null;
            /** @description True when the latest observed self-hosted worker heartbeat is older than the stale threshold. This is control-plane visibility only, not transport enforcement. */
            stale: boolean;
            /** @description Unix timestamp after which the worker is considered stale based on last_seen_at_unix and stale_threshold_secs. Null until a heartbeat has been observed. */
            stale_after_unix: number | null;
            /** @constant */
            stale_threshold_secs: 300;
        };
        AdminSelfHostedWorkerHeartbeat: {
            id: string;
            status: string;
            reported_at_unix?: number | null;
            observed_at_unix?: number | null;
        };
        AdminSelfHostedRunEvent: {
            id: string;
            worker_id: string;
            session_id?: string | null;
            run_id?: string | null;
            kind: string;
            /** @constant */
            trust_level: "reported_by_self_hosted_worker";
            occurred_at_unix?: number | null;
            ingested_at_unix?: number | null;
            /** @description Customer-reported telemetry event document encoded as JSON. This is reported evidence, not managed-worker enforcement proof. */
            event_json: string;
        };
        AdminSelfHostedWorkerEventStream: {
            /** @constant */
            object: "self_hosted_worker_event_stream";
            worker_id: string;
            /** @constant */
            trust_level: "reported_by_self_hosted_worker";
            data: components["schemas"]["AdminSelfHostedRunEvent"][];
            total: number;
            limit: number;
            after_event_id: string | null;
            /** @description Use this value as after_event_id to request the next page. Null means this page is empty. */
            next_after_event_id: string | null;
        };
        AdminSelfHostedRunTimeline: {
            /** @constant */
            object: "self_hosted_run_timeline";
            run_id: string;
            session_ids: string[];
            worker_ids: string[];
            /** @constant */
            trust_level: "reported_by_self_hosted_worker";
            reported_event_count: number;
            lifecycle_event_count: number;
            first_seen_unix?: number | null;
            last_seen_unix?: number | null;
            latest_lifecycle_state?: string | null;
            events: components["schemas"]["AdminSelfHostedRunEvent"][];
        };
        AdminManagedWorkerSessionList: components["schemas"]["AdminList_AdminManagedWorkerSession"];
        AdminList_AdminManagedWorkerSession: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminManagedWorkerSession"][];
            total: number;
            offset: number;
            limit: number;
        };
        AdminManagedWorkerSession: {
            id: string;
            run_id: string;
            tenant: components["schemas"]["TenantContext"];
            workspace_id: string;
            worker_template_id: string;
            agent_worker_instance_id?: string | null;
            /** @enum {string} */
            status: "running" | "completed" | "cancelled" | "failed" | "cleaned_up";
            /** @enum {string} */
            isolation_backend_kind: "firecracker_microvm" | "kata_containers" | "gvisor" | "rootless_docker";
            microvm_id?: string | null;
            capability_envelope_id: string;
            requested_at_unix?: number | null;
            started_at_unix?: number | null;
            completed_at_unix?: number | null;
            cleanup_completed_at_unix?: number | null;
            lifecycle_events: components["schemas"]["AdminManagedWorkerLifecycleEvent"][];
        };
        AdminManagedWorkerLifecycleEvent: {
            id: string;
            session_id: string;
            run_id: string;
            /** @enum {string} */
            status: "running" | "completed" | "cancelled" | "failed" | "cleaned_up";
            /** @enum {string} */
            action: "exec_or_attach" | "stop" | "cleanup" | "failure";
            outcome: string;
            occurred_at_unix?: number | null;
            agent_worker_instance_id?: string | null;
        };
        AdminSelfHostedWorkerArtifactRequest: {
            artifact_id: string;
            session_id: string;
            run_id: string;
            artifact_name: string;
            content_type?: string | null;
            size_bytes: number;
            created_at_unix?: number | null;
            /** @description Optional customer-reported artifact metadata document encoded as JSON. Binary artifact upload is not implemented by this metadata endpoint. */
            artifact_json?: string | null;
        };
        AdminSelfHostedWorkerArtifactResponse: {
            /** @constant */
            object: "self_hosted_worker_artifact";
            worker: components["schemas"]["AdminSelfHostedWorkerRecord"];
            artifact: components["schemas"]["AdminSelfHostedWorkerArtifact"];
        };
        AdminSelfHostedWorkerArtifact: {
            id: string;
            worker_id: string;
            session_id: string;
            run_id: string;
            artifact_name: string;
            content_type?: string | null;
            size_bytes: number;
            /** @constant */
            trust_level: "reported_by_self_hosted_worker";
            created_at_unix?: number | null;
        };
        AdminSelfHostedWorkerCheckpointRequest: {
            checkpoint_id: string;
            session_id: string;
            run_id: string;
            checkpoint_name: string;
            size_bytes: number;
            created_at_unix?: number | null;
            /** @description Optional customer-reported checkpoint metadata document encoded as JSON. Checkpoint fetch/restore payload transfer is not implemented by this metadata endpoint. */
            checkpoint_json?: string | null;
        };
        AdminSelfHostedWorkerCheckpointResponse: {
            /** @constant */
            object: "self_hosted_worker_checkpoint";
            worker: components["schemas"]["AdminSelfHostedWorkerRecord"];
            checkpoint: components["schemas"]["AdminSelfHostedWorkerCheckpoint"];
        };
        AdminSelfHostedWorkerCheckpoint: {
            id: string;
            worker_id: string;
            session_id: string;
            run_id: string;
            checkpoint_name: string;
            size_bytes: number;
            /** @constant */
            trust_level: "reported_by_self_hosted_worker";
            created_at_unix?: number | null;
        };
        AdminSelfHostedWorkerRotateRequest: {
            identity_fingerprint: string;
            /** @description Optional replacement expiry for the rotated identity. Null clears gateway-enforced identity expiry. */
            identity_expires_at_unix?: number | null;
        };
        GuardrailPolicyScope: {
            /** @default [] */
            tenant_ids: string[];
            /** @default [] */
            organization_ids: string[];
            /** @default [] */
            project_ids: string[];
            /** @default [] */
            workspace_ids: string[];
            /** @default [] */
            api_key_ids: string[];
            /** @default [] */
            service_account_ids: string[];
            /** @default [] */
            gateway_config_ids: string[];
            /** @default [] */
            models: string[];
            /** @default [] */
            providers: string[];
        };
        GuardrailJsonConstraints: {
            /** @description JSON Schema compiled when the policy revision is activated. */
            schema?: Record<string, never> | boolean | null;
            /** @default [] */
            required_keys: string[];
            /** @default [] */
            forbidden_keys: string[];
        };
        GuardrailRequestConstraints: {
            /** @default [] */
            allowed_endpoints: ("chat_completions" | "responses")[];
            /** @default [] */
            allowed_models: string[];
            /** @default [] */
            forbidden_models: string[];
            /** @default [] */
            allowed_providers: string[];
            /** @default [] */
            forbidden_providers: string[];
            metadata?: components["schemas"]["GuardrailJsonConstraints"] | null;
            tool_parameters?: components["schemas"]["GuardrailJsonConstraints"] | null;
        };
        GuardrailLocalDetector: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            kind: "GuardrailLocalDetector";
            /** @default [] */
            keywords: string[];
            /** @default [] */
            regex: string[];
            max_input_bytes?: number | null;
            json?: components["schemas"]["GuardrailJsonConstraints"] | null;
            request?: components["schemas"]["GuardrailRequestConstraints"] | null;
            /**
             * @description Versioned high-confidence pattern set; requires fingerprint_secret_ref.
             * @default []
             */
            secret_patterns: ("open_ai_api_key" | "github_token" | "aws_access_key_id")[];
            /** @description Secret reference resolved at activation for HMAC-SHA256 finding fingerprints. */
            fingerprint_secret_ref?: string | null;
        } | unknown | unknown | unknown | unknown | unknown | unknown;
        GuardrailCustomHttpDetector: {
            /**
             * @description discriminator enum property added by openapi-typescript
             * @enum {string}
             */
            kind: "GuardrailCustomHttpDetector";
            /** Format: uri */
            endpoint: string;
            /** @default 2000 */
            timeout_ms: number;
            /** @default 16 */
            max_concurrency: number;
            /** @default 3 */
            circuit_failure_threshold: number;
            /** @default 30000 */
            circuit_cooldown_ms: number;
            /** @default 0 */
            max_retries: number;
            /** @default 1048576 */
            max_payload_bytes: number;
            /** @default 262144 */
            max_response_bytes: number;
            /** @default false */
            allow_private_network: boolean;
            /** @description Secret reference resolved during activation/reload; raw bearer tokens are not accepted. */
            secret_ref?: string | null;
        };
        GuardrailDetectorDefinition: components["schemas"]["GuardrailLocalDetector"] | components["schemas"]["GuardrailCustomHttpDetector"];
        GuardrailCheckBinding: {
            id: string;
            /** @default true */
            enabled: boolean;
            /** @enum {string} */
            stage: "request" | "response";
            detector: components["schemas"]["GuardrailDetectorDefinition"];
            fallback_detector?: components["schemas"]["GuardrailLocalDetector"] | null;
            /** @description Content sources visible to this detector. Omitted values default to all supported sources for compatibility. */
            sources?: ("system" | "developer" | "user" | "assistant" | "tool_schema" | "tool_arguments" | "tool_result" | "metadata" | "text_attachment" | "unknown")[];
        };
        GuardrailPolicyAction: {
            /** @enum {string} */
            kind: "allow" | "block" | "redact" | "record";
            /** @description Required for block and redact actions. */
            code?: string | null;
            /** @description Required for block and redact actions; must not echo detector input. */
            message?: string | null;
        };
        GuardrailPolicyAggregation: {
            /** @constant */
            type: "all";
        } | {
            /** @constant */
            type: "any";
        } | {
            /** @constant */
            type: "threshold";
            minimum: number;
        };
        GuardrailPolicyDefinition: {
            /** @description Required on the collection endpoint; optional when the path supplies policy_id. */
            policy_id?: string;
            name: string;
            description?: string | null;
            /** @default true */
            enforced: boolean;
            scope?: components["schemas"]["GuardrailPolicyScope"];
            checks: components["schemas"]["GuardrailCheckBinding"][];
            aggregation?: components["schemas"]["GuardrailPolicyAggregation"];
            /**
             * @default sequential
             * @enum {string}
             */
            execution: "sequential" | "parallel";
            /**
             * @default enforce
             * @enum {string}
             */
            mode: "enforce" | "shadow";
            /**
             * @default buffer_and_enforce
             * @enum {string}
             */
            streaming: "buffer_and_enforce" | "shadow_after_complete" | "reject_streaming";
            on_pass: components["schemas"]["GuardrailPolicyAction"][];
            on_fail: components["schemas"]["GuardrailPolicyAction"][];
            on_error: components["schemas"]["GuardrailPolicyAction"][];
            /** @default 2000 */
            deadline_ms: number;
        };
        GuardrailPolicyRevisionMutation: components["schemas"]["GuardrailPolicyDefinition"];
        GuardrailPolicyRevisionView: components["schemas"]["GuardrailPolicyDefinition"] & {
            revision: number;
            created_at_unix: number;
            created_by: string;
            /** @enum {string} */
            status: "draft" | "active" | "archived";
        };
        GuardrailPolicyRevisionResponse: {
            /** @constant */
            object: "guardrail_policy_revision";
            policy: components["schemas"]["GuardrailPolicyRevisionView"];
        };
        GuardrailPolicyRevisionList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["GuardrailPolicyRevisionView"][];
        };
        GuardrailPolicyRevisionSelection: {
            revision: number;
        };
        GuardrailPolicyRollbackSelection: {
            revision?: number | null;
        };
        GuardrailPolicyBindingResponse: {
            /** @constant */
            object: "guardrail_policy_binding";
            policy_id: string;
            active_revision: number;
            previous_active_revision: number | null;
            rollback: boolean;
            reload: Record<string, never>;
            /** @description Id of the admin audit-event row this activation/rollback wrote (action guardrail.policy_activate / guardrail.policy_rollback). A mutation receipt follows this id to locate the audit row. Null only when no audit row was recorded. */
            audit_id?: string | null;
        };
        GuardrailPolicyDryRunRequest: {
            revision?: number | null;
            /** @enum {string} */
            stage: "request" | "response";
            organization_id?: string | null;
            project_id?: string | null;
            workspace_id?: string | null;
            api_key_id?: string | null;
            service_account_id?: string | null;
            gateway_config_id?: string | null;
            model?: string | null;
            provider?: string | null;
            /**
             * @description Used only by local deterministic checks and never persisted in dry-run evidence.
             * @default
             */
            text: string;
        };
        GuardrailPolicyDryRunCheck: {
            id: string;
            /** @enum {string} */
            detector: "local" | "custom_http";
            /** @enum {string} */
            result: "pass" | "fail" | "disabled" | "not_executed";
        };
        GuardrailPolicyDryRunResponse: {
            /** @constant */
            object: "guardrail_policy_dry_run";
            /** @description Exact immutable policy_id@revision. */
            policy_revision: string;
            selected: boolean;
            /** @constant */
            result: "planned";
            checks: components["schemas"]["GuardrailPolicyDryRunCheck"][];
            on_pass: components["schemas"]["GuardrailPolicyAction"][];
            on_fail: components["schemas"]["GuardrailPolicyAction"][];
            on_error: components["schemas"]["GuardrailPolicyAction"][];
            /** @constant */
            provider_dispatched: false;
            /** @constant */
            external_action_dispatched: false;
        };
        /** @description Sanitized category-to-count map. Matched text is never included. */
        GuardrailFindingCategoryCounts: {
            [key: string]: number;
        };
        GuardrailCheckEvaluation: {
            id: string;
            evaluation_id: string;
            check_id: string;
            detector_id: string;
            detector_version: string;
            config_digest: string;
            /** @enum {string} */
            verdict: "pass" | "fail" | "error" | "skipped";
            /** @enum {string} */
            action: "allow" | "block" | "redact" | "record";
            /** @enum {string} */
            enforcement_status: "enforced" | "shadow_only" | "not_enforced";
            latency_ms: number;
            finding_category_counts: components["schemas"]["GuardrailFindingCategoryCounts"];
            finding_count: number;
            transformed: boolean;
            used_fallback: boolean;
            error_kind?: string | null;
        };
        GuardrailEvaluation: {
            id: string;
            request_id: string;
            trace_id?: string | null;
            agent_run_id?: string | null;
            subject_id?: string | null;
            tenant: components["schemas"]["TenantContext"];
            /** @enum {string} */
            scope_type: "global" | "tenant" | "project" | "workspace" | "api_key";
            scope_id: string;
            target: string;
            /** @enum {string} */
            protocol: "chat_completions" | "responses";
            /** @enum {string} */
            stage: "request" | "response";
            /** @enum {string} */
            mode: "enforce" | "shadow";
            policy_id: string;
            policy_revision: number;
            /** @enum {string} */
            verdict: "pass" | "fail" | "error";
            /** @enum {string} */
            action: "allow" | "block" | "redact" | "record";
            /** @enum {string} */
            enforcement_status: "enforced" | "shadow_only" | "not_enforced";
            latency_ms: number;
            finding_category_counts: components["schemas"]["GuardrailFindingCategoryCounts"];
            finding_count: number;
            transformed: boolean;
            input_fingerprint: string;
            /** @description Issue #306: target-level action fingerprint under the canonical_target_sha256 contract ("sha256:<hex>"), the SAME value carried by timeline/audit rows of the same action, so guardrail evidence, approvals, timeline and audit evidence join by fingerprint. Null/absent on records persisted before the field existed and on actions without a resolvable canonical capability target (e.g. model-content guardrail evaluations, Extension/Builtin tools). Distinct from the guardrail HMAC input_fingerprint (content identity) and from the invocation-binding tool-approval fingerprint, which both keep their semantics. */
            action_fingerprint?: string | null;
            /**
             * @description Issue #306: canonical decision class STORED AT WRITE TIME via the issue #303 GuardrailOutcome mapping over the recorded verdict/action/enforcement_status triple. Null/absent on rows persisted before migration 047; readers fall back to the verdict/action/enforcement heuristic only for those legacy rows.
             * @enum {string|null}
             */
            decision?: "allow" | "deny" | "ask" | "degrade" | null;
            /** @description Issue #306: lossless issue #303 reason code carrying the full recorded triple: guardrail:<verdict>:<action>:<enforcement_status>. Null/absent on legacy rows. */
            decision_reason?: string | null;
            occurred_at_unix: number;
        };
        GuardrailEvaluationView: components["schemas"]["GuardrailEvaluation"] & {
            checks: components["schemas"]["GuardrailCheckEvaluation"][];
        };
        GuardrailEvaluationList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["GuardrailEvaluationView"][];
            total: number;
            offset: number;
            limit: number;
        };
        InvestigationRequestEvidence: {
            request_id: string;
            trace_id?: string | null;
            agent_run_id?: string | null;
            workflow_id?: string | null;
            workflow_version?: number | null;
            workflow_node_id?: string | null;
            /** @description Issue #307: fingerprint of the UPSTREAM governed action this request is a downstream effect of (canonical_target_sha256). Present only when the child declared a parent; omitted for absent-parent rows - never fabricated. Join key for walking child -> parent. */
            parent_action_fingerprint?: string | null;
            tenant: components["schemas"]["TenantContext"];
            route?: string | null;
            provider?: string | null;
            logical_model?: string | null;
            provider_model?: string | null;
            status_code: number;
            error_code?: string | null;
            cache_status?: string | null;
            started_at_unix?: number | null;
            completed_at_unix?: number | null;
        };
        InvestigationApprovalEvidence: {
            id: string;
            request_id: string;
            trace_id?: string | null;
            /** @description Issue #305: id of the agent run this approval was requested inside, carried from the tool-governance execution context at approval-creation time. Null/absent on records persisted before the field existed and on approvals created outside an agent run. Never part of the approval fingerprint. */
            agent_run_id?: string | null;
            /** @description Issue #305: id of the workflow whose node requested the tool, when the execution was workflow-bound. */
            workflow_id?: string | null;
            /** @description Issue #305: id of the workflow node that requested the tool, when the execution was workflow-bound. */
            workflow_node_id?: string | null;
            /** @description Issue #306: target-level canonical_target_sha256 action fingerprint of the action this approval gates, carried alongside - never instead of - the invocation-binding `fingerprint`, which stays authoritative for approval verification. Null/absent on legacy records and on tools without a resolvable canonical capability target. Never part of the approval fingerprint input. */
            action_fingerprint?: string | null;
            /**
             * @description Issue #306: stored canonical decision class for the current status, maintained on every status transition via the issue #303 From<ApprovalStatus> mapping (pending=ask, approved=allow, denied/expired=deny). Null/absent only on legacy records.
             * @enum {string|null}
             */
            decision?: "allow" | "deny" | "ask" | null;
            /**
             * @description Issue #306: stored issue #303 decision reason code for the current status (the lossless discriminator: denied vs expired both map to deny). Null/absent only on legacy records.
             * @enum {string|null}
             */
            decision_reason?: "approval_pending" | "approval_approved" | "approval_denied" | "approval_expired" | null;
            tenant: components["schemas"]["TenantContext"];
            actor_api_key_id?: string | null;
            tool_name: string;
            server_name?: string | null;
            route?: string | null;
            status: string;
            reviewer_api_key_id?: string | null;
            reviewer_authority?: string | null;
            terminal_reason?: string | null;
            requested_at_unix: number;
            expires_at_unix: number;
            decided_at_unix?: number | null;
        };
        InvestigationBillingEvidence: {
            request_id: string;
            trace_id?: string | null;
            agent_run_id?: string | null;
            tenant: components["schemas"]["TenantContext"];
            logical_model: string;
            provider: string;
            provider_model: string;
            usage: components["schemas"]["TokenUsage"];
            status_code: number;
            occurred_at_unix?: number | null;
            cost_usd?: number | null;
            latency_ms?: number | null;
            wallet_delta_credits?: number | null;
            wallet_balance_after_credits?: number | null;
        };
        GuardrailInvestigationTimeline: {
            /** @constant */
            object: "guardrail_investigation";
            selector: string;
            identity?: components["schemas"]["TenantContext"] | null;
            agent_runs: components["schemas"]["StoredAgentRun"][];
            agent_events: components["schemas"]["StoredAgentRunEvent"][];
            requests: components["schemas"]["InvestigationRequestEvidence"][];
            guardrail_evaluations: components["schemas"]["GuardrailEvaluationView"][];
            audit_events: components["schemas"]["StoredAuditEvent"][];
            approvals: components["schemas"]["InvestigationApprovalEvidence"][];
            billing_events: components["schemas"]["InvestigationBillingEvidence"][];
            /** @description Issue #306: fingerprint-based joining across the evidence arrays above. Empty when no row in the investigation carries an action fingerprint. Additive-optional: absent on responses from older gateways. */
            action_correlations?: components["schemas"]["InvestigationActionCorrelation"][];
            total_cost_usd: number;
            /** @enum {string} */
            final_outcome: "blocked" | "failed" | "succeeded" | "decision_only";
        };
        /** @description Issue #306: one shared-action-identity join group inside an investigation. Every evidence row (guardrail evaluations, approvals, timeline events, audit events) carrying the SAME target-level canonical_target_sha256 fingerprint describes the SAME external action. */
        InvestigationActionCorrelation: {
            /** @description The shared "sha256:<hex>" action fingerprint (action_fingerprint_contract: canonical_target_sha256). */
            action_fingerprint: string;
            /** @description Ids of guardrail evaluation rows carrying the fingerprint. */
            guardrail_evaluation_ids: string[];
            /** @description Ids of approval records carrying the fingerprint. */
            approval_ids: string[];
            /** @description Ids of timeline (agent-run event) rows carrying the fingerprint. */
            agent_event_ids: string[];
            /** @description Ids of audit-event rows carrying the fingerprint. */
            audit_event_ids: string[];
            /** @description Issue #307 parent -> child traversal: request ids of request-log rows (e.g. child A2A exchanges) that declared THIS fingerprint as their parent_action_fingerprint. Omitted when empty. */
            child_request_ids?: string[];
            /** @description Issue #307 parent -> child traversal: dispatch ids of self-hosted run dispatches created as downstream effects of this action (self_hosted_run_dispatches.parent_action_fingerprint, migration 048). Omitted when empty. */
            child_dispatch_ids?: string[];
        };
        AdminSelfHostedWorkerRotateResponse: {
            /** @constant */
            object: "self_hosted_worker_identity_rotation";
            worker: components["schemas"]["AdminSelfHostedWorkerRecord"];
            previous_identity_fingerprint: string;
            previous_identity_expires_at_unix: number | null;
            rotated_at_unix: number | null;
        };
        AiRequest: {
            model: string;
            stream?: boolean;
            metadata?: {
                [key: string]: string;
            };
        };
        EmbeddingsRequest: {
            model: string;
            input: string | string[];
            encoding_format?: string;
            dimensions?: number;
            user?: string;
            metadata?: {
                [key: string]: string;
            };
        };
        RerankRequest: {
            model: string;
            query: string;
            documents: (string | {
                text: string;
            })[];
            top_n?: number;
            return_documents?: boolean;
            metadata?: {
                [key: string]: string;
            };
        };
        MachineTransportRequest: {
            [key: string]: unknown;
        };
        OpenAiModel: {
            id: string;
            /** @constant */
            object: "model";
            /** Format: int64 */
            created: number;
            owned_by: string;
            capabilities?: ("chat" | "streaming" | "vision" | "images" | "embeddings" | "tools" | "structured_output")[];
            /** Format: int64 */
            context_window?: number | null;
            modalities?: {
                input: ("text" | "image" | "embedding")[];
                output: ("text" | "image" | "embedding")[];
            };
            pricing?: {
                /** @constant */
                currency: "USD";
                /** @constant */
                unit: "per_1m_tokens";
                input: number | null;
                output: number | null;
            };
        };
        OpenAiModelList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["OpenAiModel"][];
        };
        AdminTenantAccount: {
            id: string;
            name: string;
            slug: string;
            status: string;
            plan_id: string;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminTenantAccountCreateRequest: {
            id?: string | null;
            name?: string | null;
            slug?: string | null;
            status?: string | null;
            plan_id?: string | null;
        };
        AdminTenantAccountMutationResponse: {
            /** @constant */
            object: "tenant_account";
            tenant: components["schemas"]["AdminTenantAccount"];
        };
        AdminTenantPlanAssignmentRequest: {
            plan_id: string;
        };
        AdminTenantResolvedDefaults: {
            object: string;
            tenant_id: string;
            plan_id: string;
            model_allowlist?: string[] | null;
            /** Format: int64 */
            rpm_limit?: number | null;
            /** Format: int64 */
            tpm_limit?: number | null;
            /** Format: double */
            monthly_budget_usd?: number | null;
            mcp_enabled: boolean;
            extension_tools_enabled: boolean;
            self_hosted_workers_enabled: boolean;
            asset_hosting_enabled: boolean;
            /** Format: int64 */
            default_asset_storage_quota_bytes?: number | null;
            /**
             * Format: int64
             * @description The plan's per-object (not cumulative) default asset byte ceiling (issue #259), independent of default_asset_storage_quota_bytes.
             */
            default_asset_max_object_bytes?: number | null;
            /**
             * Format: double
             * @description The plan's default monthly USD ceiling on CF-hosted-agent runtime cost (issue #428); money that mirrors default_monthly_budget_usd (min-across-the-chain, not tenant-only).
             */
            default_agent_cost_budget_usd?: number | null;
        };
        AdminProject: {
            id: string;
            tenant_id: string;
            name: string;
            slug: string;
            status: string;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminProjectCreateRequest: {
            id?: string | null;
            tenant_id?: string | null;
            name?: string | null;
            slug?: string | null;
            status?: string | null;
        };
        AdminProjectMutationResponse: {
            /** @constant */
            object: "project";
            project: components["schemas"]["AdminProject"];
        };
        AdminWorkspace: {
            id: string;
            project_id: string;
            tenant_id: string;
            name: string;
            slug: string;
            environment: string;
            status: string;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminWorkspaceCreateRequest: {
            id?: string | null;
            project_id?: string | null;
            name?: string | null;
            slug?: string | null;
            environment?: string | null;
            status?: string | null;
        };
        AdminWorkspaceMutationResponse: {
            /** @constant */
            object: "workspace";
            workspace: components["schemas"]["AdminWorkspace"];
        };
        AdminVirtualApiKey: {
            id: string;
            workspace_id: string;
            tenant_id: string;
            project_id: string;
            name: string;
            key_prefix: string;
            last4: string;
            enabled: boolean;
            scopes: string[];
            allowed_models: string[];
            allowed_providers: string[];
            /** Format: int64 */
            monthly_token_budget?: number | null;
            /** Format: int64 */
            request_limit_per_minute?: number | null;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
            /** Format: int64 */
            rotated_at_unix?: number | null;
            /** Format: int64 */
            expires_at_unix?: number | null;
            /** Format: int64 */
            revoked_at_unix?: number | null;
        };
        AdminVirtualApiKeyCreateRequest: {
            id?: string | null;
            name?: string | null;
            workspace_id?: string | null;
            scopes?: string[] | null;
            allowed_models?: string[] | null;
            allowed_providers?: string[] | null;
            /** Format: int64 */
            monthly_token_budget?: number | null;
            /** Format: int64 */
            request_limit_per_minute?: number | null;
            /** Format: int64 */
            expires_at_unix?: number | null;
        };
        AdminVirtualApiKeyMutationResponse: {
            /** @constant */
            object: "virtual_api_key";
            key: components["schemas"]["AdminVirtualApiKey"];
            secret?: string | null;
        };
        AdminQuotaPolicy: {
            id: string;
            /** @enum {string} */
            scope_type: "tenant" | "project" | "workspace" | "key";
            scope_id: string;
            model_allowlist: string[];
            /** Format: int64 */
            rpm_limit?: number | null;
            /** Format: int64 */
            tpm_limit?: number | null;
            /** Format: double */
            monthly_budget_usd?: number | null;
            /**
             * Format: int64
             * @description Tenant-only override of the plan asset storage quota; must be null for project, workspace, and key scopes.
             */
            asset_storage_quota_bytes?: number | null;
            /**
             * Format: int64
             * @description Tenant-only override of the plan per-object asset byte ceiling (issue #259); a per-object (not cumulative) cap, independent of asset_storage_quota_bytes; must be null for project, workspace, and key scopes.
             */
            asset_max_object_bytes?: number | null;
            /**
             * Format: double
             * @description Per-scope monthly USD ceiling on CF-hosted-agent runtime cost (issue #428); money merged min-across-the-chain like monthly_budget_usd (settable at any scope, not tenant-only).
             */
            agent_cost_budget_usd?: number | null;
            alert_threshold_pcts: number[];
            enabled: boolean;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminQuotaPolicyMutation: {
            scope_type?: string | null;
            scope_id?: string | null;
            model_allowlist?: string[] | null;
            /** Format: int64 */
            rpm_limit?: number | null;
            /** Format: int64 */
            tpm_limit?: number | null;
            /** Format: double */
            monthly_budget_usd?: number | null;
            /**
             * Format: int64
             * @description Tenant-only override of the plan asset storage quota; rejected for project, workspace, and key scopes.
             */
            asset_storage_quota_bytes?: number | null;
            /**
             * Format: int64
             * @description Tenant-only override of the plan per-object asset byte ceiling (issue #259); a per-object (not cumulative) cap, independent of asset_storage_quota_bytes; rejected for project, workspace, and key scopes.
             */
            asset_max_object_bytes?: number | null;
            /**
             * Format: double
             * @description Per-scope monthly USD ceiling on CF-hosted-agent runtime cost (issue #428); money merged min-across-the-chain like monthly_budget_usd (settable at any scope, not tenant-only).
             */
            agent_cost_budget_usd?: number | null;
            alert_threshold_pcts?: number[] | null;
            enabled?: boolean | null;
        };
        AdminQuotaPolicyMutationResponse: {
            /** @constant */
            object: "quota_policy";
            policy: components["schemas"]["AdminQuotaPolicy"];
        };
        /** @description The stored response-cache policy document. Every governed column is a TRI-STATE: `null` means "inherit the deployment value", which is why this family has `PUT` and deliberately no `PATCH` — a JSON merge patch cannot express "set this field back to inherit". The document schema derives from `adminRecordSchema.extend(...)` in `apps/control-plane/src/routes/admin_semantic_cache.ts`, which is a Zod `passthrough()`, so operator fields beyond those named here are accepted and stored rather than silently dropped. */
        AdminSemanticCachePolicy: {
            /** @description `{scope_type}:{scope_id}` — the composite key flattened to the store's string id. */
            id: string;
            /** @description Owning tenant, stamped by the store from the caller's scope; never taken from the body for a tenant-scoped caller. */
            tenant_id?: string | null;
            name?: string;
            /**
             * @description Scope kind this policy governs. `tenant` is the only kind today; an unknown kind names no resource and answers 404.
             * @enum {string}
             */
            scope_type?: "tenant";
            scope_id?: string;
            /** @description Tri-state. `null` means "inherit the deployment value", which is a different thing from `false`. */
            enabled?: boolean | null;
            /**
             * @description Tri-state; `null` inherits the deployment value.
             * @enum {string|null}
             */
            mode?: "exact_match" | "semantic" | null;
            /**
             * Format: double
             * @description Semantic-mode match floor, in (0, 1]. Tri-state; `null` inherits.
             */
            similarity_threshold?: number | null;
            /**
             * Format: int64
             * @description Cached-entry lifetime. Tri-state; `null` inherits.
             */
            ttl_seconds?: number | null;
            /** @description Narrow caching to these models. Tri-state; `null` inherits. */
            scoped_models?: string[] | null;
        } & {
            [key: string]: unknown;
        };
        /** @description `POST` requires `scope_type` and `scope_id` in the body (they identify the policy being created). On `PUT` they come from the path and any body copy is overwritten by it. `PUT` states the WHOLE governed policy: an omitted field returns to inherit. */
        AdminSemanticCachePolicyMutation: {
            id?: string;
            name?: string;
            tenant_id?: string | null;
            /**
             * @description Scope kind this policy governs. `tenant` is the only kind today; an unknown kind names no resource and answers 404.
             * @enum {string}
             */
            scope_type?: "tenant";
            scope_id?: string;
            /** @description Tri-state. `null` means "inherit the deployment value", which is a different thing from `false`. */
            enabled?: boolean | null;
            /**
             * @description Tri-state; `null` inherits the deployment value.
             * @enum {string|null}
             */
            mode?: "exact_match" | "semantic" | null;
            /**
             * Format: double
             * @description Semantic-mode match floor, in (0, 1]. Tri-state; `null` inherits.
             */
            similarity_threshold?: number | null;
            /**
             * Format: int64
             * @description Cached-entry lifetime. Tri-state; `null` inherits.
             */
            ttl_seconds?: number | null;
            /** @description Narrow caching to these models. Tri-state; `null` inherits. */
            scoped_models?: string[] | null;
        } & {
            [key: string]: unknown;
        };
        AdminSemanticCachePolicyList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminSemanticCachePolicy"][];
            total?: number;
            offset?: number;
            limit?: number;
        };
        AdminSemanticCachePolicyMutationResponse: {
            /** @constant */
            object: "semantic_cache_policy";
            semantic_cache_policy: components["schemas"]["AdminSemanticCachePolicy"];
        };
        /** @description `id` is the `{scope_type}:{scope_id}` composite key. */
        DeleteSemanticCachePolicyResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "semantic_cache_policy";
        };
        /** @description The epoch now in force. Every cache key is a function of it, so a bumped epoch IS the purge — this is the number an operator correlates with the misses they expect. */
        AdminSemanticCacheInvalidationResponse: {
            /** @constant */
            object: "semantic_cache_invalidation";
            /** @enum {string} */
            scope_type: "tenant";
            scope_id: string;
            /**
             * Format: int64
             * @description Monotonic; never moved by an ordinary edit.
             */
            invalidation_epoch: number;
        };
        AdminPlan: {
            id: string;
            name: string;
            slug: string;
            mcp_enabled: boolean;
            self_hosted_workers_enabled: boolean;
            /** Format: int64 */
            admin_console_seats?: number | null;
            default_model_allowlist: string[];
            /** Format: int64 */
            default_rpm_limit?: number | null;
            /** Format: int64 */
            default_tpm_limit?: number | null;
            /** Format: double */
            default_monthly_budget_usd?: number | null;
            asset_hosting_enabled: boolean;
            /** Format: int64 */
            default_asset_storage_quota_bytes?: number | null;
            /**
             * Format: int64
             * @description Per-object (not cumulative) default asset byte ceiling (issue #259), independent of default_asset_storage_quota_bytes.
             */
            default_asset_max_object_bytes?: number | null;
            /**
             * Format: double
             * @description Default monthly USD ceiling on CF-hosted-agent runtime cost (issue #428); money that mirrors default_monthly_budget_usd (min-across-the-chain, not tenant-only).
             */
            default_agent_cost_budget_usd?: number | null;
            extension_tools_enabled: boolean;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminPlanMutation: {
            id?: string | null;
            name?: string | null;
            slug?: string | null;
            mcp_enabled?: boolean | null;
            self_hosted_workers_enabled?: boolean | null;
            /** Format: int64 */
            admin_console_seats?: number | null;
            default_model_allowlist?: string[] | null;
            /** Format: int64 */
            default_rpm_limit?: number | null;
            /** Format: int64 */
            default_tpm_limit?: number | null;
            /** Format: double */
            default_monthly_budget_usd?: number | null;
            asset_hosting_enabled?: boolean | null;
            /** Format: int64 */
            default_asset_storage_quota_bytes?: number | null;
            /**
             * Format: int64
             * @description Per-object (not cumulative) default asset byte ceiling (issue #259), independent of default_asset_storage_quota_bytes.
             */
            default_asset_max_object_bytes?: number | null;
            /**
             * Format: double
             * @description Default monthly USD ceiling on CF-hosted-agent runtime cost (issue #428); money that mirrors default_monthly_budget_usd (min-across-the-chain, not tenant-only).
             */
            default_agent_cost_budget_usd?: number | null;
            extension_tools_enabled?: boolean | null;
        };
        AdminPlanMutationResponse: {
            /** @constant */
            object: "plan";
            plan: components["schemas"]["AdminPlan"];
        };
        AdminBillingOutboxReplayResponse: {
            /** @constant */
            object: "billing_outbox_dead_letter_replay";
            id: string;
            replayed: boolean;
            dead_lettered: boolean;
            /** Format: int64 */
            attempts: number;
            /** Format: int64 */
            next_attempt_unix: number;
        };
        AdminWallet: {
            tenant_id: string;
            /** Format: int64 */
            balance_credits: number;
            /** Format: int64 */
            auto_recharge_threshold_credits?: number | null;
            /** Format: int64 */
            auto_recharge_amount_credits?: number | null;
            dunning: boolean;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminWalletMutation: {
            tenant_id?: string | null;
            /** Format: int64 */
            auto_recharge_threshold_credits?: number | null;
            /** Format: int64 */
            auto_recharge_amount_credits?: number | null;
        };
        AdminWalletAdjustRequest: {
            /** Format: int64 */
            delta_credits: number;
        };
        AdminWalletChargeRequest: {
            payment_method_id?: string | null;
            /** Format: int64 */
            amount_usd_cents?: number | null;
        };
        AdminWalletMutationResponse: {
            /** @constant */
            object: "wallet";
            wallet: components["schemas"]["AdminWallet"];
        };
        AdminWalletChargeResponse: {
            /** @constant */
            object: "wallet_charge";
            succeeded: boolean;
            provider_charge_id: string;
            decline_reason?: string | null;
            wallet: components["schemas"]["AdminWallet"];
        };
        AdminPaymentMethod: {
            id: string;
            tenant_id: string;
            provider: string;
            provider_customer_id: string;
            provider_payment_method_id: string;
            is_default: boolean;
            /** Format: int64 */
            created_at_unix: number;
        };
        AdminPaymentMethodCreateRequest: {
            tenant_id?: string | null;
            provider?: string | null;
            provider_customer_id?: string | null;
            provider_payment_method_id?: string | null;
            is_default?: boolean;
        };
        AdminPaymentMethodMutationResponse: {
            /** @constant */
            object: "payment_method";
            payment_method: components["schemas"]["AdminPaymentMethod"];
        };
        AssetSummary: {
            id: string;
            asset_type: string;
            name: string;
            version: string;
            content_type: string;
            /** @description Lowercase SHA-256 digest of the exact stored bytes. */
            content_hash: string;
            /**
             * Format: int64
             * @description Authoritative stored byte count.
             */
            size_bytes: number;
            /** @description True for private bucket storage. Internal storage_uri, bucket endpoint, and credentials are not serialized. */
            storage_backed: boolean;
            /**
             * @description Durable trust-screening state of the row (#366). Only visible is downloadable: pending_scan (deferred or out-of-band scan not yet completed) and quarantined (the scanner or a fail-closed-unavailable policy flagged it) are stored but WITHHELD from every read surface and omitted from consumer listings until promoted. Read this rather than inferring publication from a 2xx.
             * @enum {string}
             */
            visibility: "visible" | "pending_scan" | "quarantined";
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        /** @description One withheld (pending_scan/quarantined) asset for the operator-only inspection surface (issue #379). Carries the ordinary AssetSummary metadata plus the durable visibility state and the screening evidence recorded at push/commit time (#366). */
        WithheldAssetSummary: {
            id: string;
            asset_type: string;
            name: string;
            version: string;
            content_type: string;
            /** @description Lowercase SHA-256 digest of the exact stored bytes. */
            content_hash: string;
            /**
             * Format: int64
             * @description Authoritative stored byte count.
             */
            size_bytes: number;
            /** @description True for private bucket storage. Internal storage_uri, bucket endpoint, and credentials are not serialized. */
            storage_backed: boolean;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
            /**
             * @description Durable trust-screening state on the asset row. A visible asset is never listed here.
             * @enum {string}
             */
            visibility: "pending_scan" | "quarantined";
            /** @description Screening evidence detail (scan outcome, signature status, cross-tenant approval state, verification manifest) captured on the asset's push/commit audit event. Omitted when that audit row is no longer retained. */
            screening_evidence?: string;
        };
        AssetMutationResponse: {
            /** @constant */
            object: "asset";
            asset: components["schemas"]["AssetSummary"];
        };
        /** @description Body returned when a static_site asset is pushed as a ZIP archive and published as a multi-file bundle (PUT /v1/assets/static_site/{site}/{version}). Distinct from the AssetMutationResponse envelope a single-blob asset push returns. */
        StaticSitePublishResponse: {
            /** @constant */
            object: "static_site";
            /** @description Owning tenant id. */
            tenant: string;
            /** @description Site slug (the asset name segment of the publish path). */
            site: string;
            /** @description Immutable, retained bundle version (the version segment of the publish path). */
            bundle_version: string;
            /** @description Whether the published site is served with public (unauthenticated) access, resolved from x-site-public. */
            public: boolean;
            /** @description Whether index.html is served for unmatched paths, resolved from x-site-spa-fallback. */
            spa_fallback: boolean;
            /** @description Number of files unpacked from the bundle. */
            file_count: number;
            /**
             * Format: int64
             * @description Total unpacked byte count across every file in the bundle.
             */
            size_bytes: number;
            /** @description Bundle-relative paths of every published file. */
            files: string[];
        };
        AdminPermission: {
            id: string;
            key: string;
            name: string;
            description: string;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminPermissionMutation: {
            id?: string | null;
            key?: string | null;
            name?: string | null;
            description?: string | null;
        };
        AdminPermissionMutationResponse: {
            /** @constant */
            object: "permission";
            permission: components["schemas"]["AdminPermission"];
        };
        AdminRole: {
            id: string;
            name: string;
            slug: string;
            description: string;
            permission_keys: string[];
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminRoleMutation: {
            id?: string | null;
            name?: string | null;
            slug?: string | null;
            description?: string | null;
            permission_keys?: string[] | null;
        };
        AdminRoleMutationResponse: {
            /** @constant */
            object: "role";
            role: components["schemas"]["AdminRole"];
        };
        AdminTenantRoleBinding: {
            id: string;
            tenant_id: string;
            role_id: string;
            /** Format: int64 */
            created_at_unix: number;
        };
        AdminTenantRoleBindingRequest: {
            role_id: string;
        };
        AdminUsageReportRow: {
            period_month?: string | null;
            scope_type?: string | null;
            scope_id?: string | null;
            metadata_key?: string | null;
            metadata_value?: string | null;
            /** Format: int64 */
            prompt_tokens: number;
            /** Format: int64 */
            completion_tokens: number;
            /** Format: int64 */
            total_tokens: number;
            /** Format: double */
            cost_usd: number;
            /** Format: int64 */
            request_count: number;
            /** Format: int64 */
            error_count: number;
        };
        AdminSiteDomain: {
            /** @constant */
            object: "site_domain";
            /** @description Normalized (lowercase, portless) bound hostname. */
            hostname: string;
            tenant_id: string;
            site: string;
            /** @description Equivalent path-based serve prefix, /sites/{tenant}/{site}/. */
            serve_path: string;
            /**
             * @description DNS ownership state as of now, expiry applied (#488). no_verification means no proof record exists. Only verified and grandfathered serve.
             * @enum {string}
             */
            verification_state?: "no_verification" | "pending_verification" | "verified" | "grandfathered" | "expired";
            /** @description Whether a request arriving on this hostname is actually served. False for every state without a live DNS ownership proof (#488). */
            serving?: boolean;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AdminSiteDomainAcme: {
            /** @description Whether ACME issuance is enabled on this gateway. */
            enabled: boolean;
            /** @description Whether this change triggered the listener-level graceful upgrade that re-issues the certificate with the updated domain set. */
            reload_triggered: boolean;
        };
        AdminSiteDomainMutation: {
            /** @description Fully qualified hostname to bind (no wildcard, no IP literal). */
            hostname: string;
            tenant_id: string;
            /** @description Published static_site name under the tenant (#258). */
            site: string;
        };
        AdminSiteDomainResponse: {
            /** @constant */
            object: "site_domain";
            site_domain: components["schemas"]["AdminSiteDomain"];
            acme: components["schemas"]["AdminSiteDomainAcme"];
            verification?: components["schemas"]["AdminSiteDomainVerification"];
        };
        AdminSiteDomainList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminSiteDomain"][];
        };
        /**
         * @description Stable asset lifecycle error code.
         * @enum {string}
         */
        AssetErrorCode: "asset_not_found" | "asset_variant_not_found" | "asset_variant_required" | "asset_version_immutable" | "asset_storage_quota_exceeded" | "asset_hosting_disabled" | "tenant_required" | "channel_target_required" | "channel_target_not_found" | "channel_not_found" | "asset_not_bucket_backed" | "asset_bucket_unavailable" | "storage_unavailable" | "payload_too_large" | "invalid_upload_intent" | "invalid_commit" | "invalid_request_body" | "asset_not_uploaded" | "asset_commit_size_mismatch" | "asset_commit_hash_mismatch" | "asset_rejected" | "asset_signature_required" | "cross_tenant_publish_denied" | "asset_scan_rejected" | "asset_egress_quota_exceeded" | "asset_download_rate_limit_exceeded" | "governance_counter_unavailable" | "asset_integrity_check_failed" | "asset_commit_outcome_unknown";
        AssetErrorResponse: {
            error: {
                message: string;
                /** @constant */
                type: "ferrogate_error";
                code: components["schemas"]["AssetErrorCode"];
                request_id: string | null;
            };
        };
        /** @description One channel pointer and its concrete version target. */
        AssetChannelSummary: {
            channel: string;
            version: string;
            /** Format: int64 */
            updated_at_unix: number;
        };
        AssetChannelListResponse: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AssetChannelSummary"][];
        };
        AssetChannelMutationResponse: {
            /** @constant */
            object: "asset_channel";
            asset_type: string;
            name: string;
            channel: components["schemas"]["AssetChannelSummary"];
        };
        /** @description One platform/architecture artifact of a logical version. */
        AssetManifestVariant: {
            /** @description Platform variant; an empty string is the default variant. */
            variant: string;
            content_type: string;
            /** @description Lowercase SHA-256 digest of the exact variant bytes. */
            content_hash: string;
            /** Format: int64 */
            size_bytes: number;
            /** @description True for private bucket storage. Internal storage_uri, bucket endpoint, and credentials are not serialized. */
            storage_backed: boolean;
        };
        AssetManifestVersion: {
            version: string;
            yanked: boolean;
            variants: components["schemas"]["AssetManifestVariant"][];
        };
        AssetManifest: {
            /** @constant */
            object: "asset_manifest";
            asset_type: string;
            name: string;
            channels: components["schemas"]["AssetChannelSummary"][];
            versions: components["schemas"]["AssetManifestVersion"][];
        };
        /** @description Asset summaries for every variant updated by yank or unyank. */
        AssetYankMutationResponse: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AssetSummary"][];
        };
        /** @description Completed out-of-band scan result driving a pending_scan promotion (issue #378). */
        AssetVisibilityPromotionRequest: {
            /**
             * @description Terminal scan verdict. clean (or visible) promotes to visible; quarantined (or quarantine/infected) withholds permanently. Any other value is rejected fail-closed and never promotes.
             * @enum {string}
             */
            scan_outcome: "clean" | "visible" | "quarantined" | "quarantine" | "infected";
            /** @description Durable, human-readable justification (scanner id, verdict detail, ticket) recorded verbatim in the promotion audit event. Required and non-empty. */
            evidence: string;
            /** @description Optional identifier of the out-of-band scanner/backend that produced the verdict, echoed into the audit evidence. */
            scanner?: string;
        };
        /** @description Result of a completed-scan visibility promotion (issue #378). */
        AssetVisibilityPromotionResponse: {
            /** @constant */
            object: "asset.visibility_promotion";
            /** @description Variant-aware stored asset id that was promoted. */
            id: string;
            /**
             * @description The durable terminal visibility the gateway persisted.
             * @enum {string}
             */
            visibility: "visible" | "quarantined";
            /**
             * @description The terminal target the supplied scan_outcome resolved to.
             * @enum {string}
             */
            scan_outcome: "visible" | "quarantined";
            asset: components["schemas"]["AssetSummary"];
        };
        AssetPresignUploadIntentRequest: {
            /** Format: int64 */
            size_bytes: number;
            /** @description Hex SHA-256 of the bytes that will be uploaded. */
            sha256: string;
        };
        AssetPresignCommitRequest: {
            /** @description Opaque staging-upload identity returned by createAssetUploadIntent. */
            upload_id: string;
            /** Format: int64 */
            size_bytes: number;
            /** @description Registered hex SHA-256; comparison is case-insensitive. */
            sha256: string;
            /** @description Stored content type; null or omission defaults to application/octet-stream. */
            content_type?: string | null;
            /** @description Detached minisign file or base64 Ed25519 signature material over the uploaded bytes. Screened identically to the inline x-asset-signature header; omission is an unsigned publish (rejected only when the tenant requires signatures). */
            signature?: string | null;
            /**
             * @description Detached signature encoding; defaults to minisign when a signature is present without a format.
             * @enum {string|null}
             */
            signature_format?: "minisign" | "ed25519" | "cosign" | null;
            /** @description Publisher key hint for bare Ed25519 signatures. */
            signature_key_id?: string | null;
            /**
             * @description Publish visibility. Cross-tenant values require a durable approval record. Omission defaults to tenant-private.
             * @enum {string|null}
             */
            visibility?: "private" | "tenant" | "tenant_private" | "shared" | "cross_tenant" | "cross_tenant_shared" | "public" | null;
            /** @description Durable tool-approval record used for cross-tenant publication; its status is read server-side, never client-asserted. */
            approval_id?: string | null;
        };
        /** @description Release a presigned upload intent that will not be committed (issue #368). The staging key is derived server-side from the asset identity plus upload_id, size_bytes and sha256, so all three declarations from the intent are required to name it. */
        AssetPresignAbortRequest: {
            /** @description The opaque upload_id the intent returned. */
            upload_id: string;
            /**
             * Format: int64
             * @description The size the intent registered.
             */
            size_bytes: number;
            /** @description The SHA-256 the intent registered. */
            sha256: string;
            /**
             * @description Why the intent is being released. bucket_rejected asserts the direct PUT was refused by the bucket; the gateway records that class ONLY after corroborating it (no object under the staging key). An absent or unrecognized value is treated as abandoned.
             * @enum {string}
             */
            reason?: "bucket_rejected" | "abandoned";
        };
        AssetPresignAbortResponse: {
            /** @constant */
            object: "asset_upload_abort";
            upload_id: string;
            /** @description True ONLY when a staging object existed AND the bucket confirmed its deletion, reclaiming its bytes immediately instead of leaving them to the lifecycle GC. A delete the bucket refused reports false here with staging_reclamation=removal_failed: the bytes still occupy the tenant's quota. */
            staging_object_removed: boolean;
            /**
             * @description What this abort actually did to the staging object, which the staging_object_removed boolean cannot express: not_staged (no object existed), removed (the bucket confirmed the delete), or removal_failed (an object existed and the delete was refused, so the bytes remain until the asset-lifecycle GC collects them).
             * @enum {string}
             */
            staging_reclamation: "not_staged" | "removed" | "removal_failed";
            /**
             * @description The outcome actually recorded in the audit trail and metrics, which is NOT an echo of the requested reason: a bucket_rejected claim the gateway contradicts (bytes ARE staged, so the bucket accepted the PUT) is downgraded to aborted, and an abort whose staging delete the bucket refused is aborted_reclaim_failed.
             * @enum {string}
             */
            outcome: "rejected_bucket" | "aborted" | "aborted_reclaim_failed";
        };
        AssetPresignUploadIntentResponse: {
            /** @constant */
            object: "asset_upload_intent";
            /** @description Logical tenant-scoped asset identity. This is never a staging or final bucket key. */
            key: string;
            /** @description Opaque identity of this unique staging upload; submit it unchanged when committing. */
            upload_id: string;
            /** Format: uri */
            upload_url: string;
            /** @constant */
            method: "PUT";
            /** Format: int64 */
            expires_in_seconds: number;
            /** Format: int64 */
            size_bytes: number;
            sha256: string;
            /** @description Header name -> value the client MUST send verbatim on the direct PUT. These are SigV4 signed headers of upload_url, so omitting or changing any of them invalidates the upload at the bucket boundary (403). Browser clients must NOT set content-length explicitly (the Fetch spec forbids it); sending exactly size_bytes of body makes the browser emit the signed value. A browser also requires the bucket's CORS Access-Control-Allow-Headers to include x-amz-content-sha256. */
            required_headers: {
                [key: string]: string;
            };
            /**
             * Format: int64
             * @description Per-object ceiling this intent was admitted against: the tightest of the operator's presign_max_object_bytes, the tenant's per-object ceiling, and the tenant's cumulative asset storage quota. Echoed so a client can fail fast without a rejected round-trip.
             */
            max_object_bytes: number;
            /**
             * @description Wire protocol for the direct upload. Always single_put: the whole object goes in ONE PUT bound to required_headers. Multipart is not supported because S3 signs each part separately, so one presigned authorization cannot bind the whole object's length and checksum.
             * @enum {string}
             */
            upload_protocol: "single_put";
        };
        AssetPresignDownloadResponse: {
            /** @constant */
            object: "asset_download_url";
            /** Format: uri */
            download_url: string;
            /** @constant */
            method: "GET";
            /** Format: int64 */
            expires_in_seconds: number;
            sha256: string;
            /** Format: int64 */
            size_bytes: number;
            content_type: string;
        };
        /** @description Current direct-to-bucket upload capability and limits. */
        AssetPresignedUploadConstraints: {
            enabled: boolean;
            /** Format: int64 */
            max_object_bytes: number | null;
            /** Format: int64 */
            url_ttl_seconds: number | null;
        };
        /** @description Authoritative tenant usage, effective quota, and upload-path constraints. */
        AssetStorageSummary: {
            /** @constant */
            object: "asset_storage_summary";
            /** Format: int64 */
            used_bytes: number;
            /** Format: int64 */
            quota_bytes: number | null;
            /**
             * Format: int64
             * @description Saturating quota minus usage, or null when storage is unlimited.
             */
            remaining_bytes: number | null;
            /** Format: int64 */
            inline_upload_max_bytes: number;
            presigned_upload: components["schemas"]["AssetPresignedUploadConstraints"];
        };
        DeleteProjectResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "project";
        };
        DeleteWorkspaceResponse: components["schemas"]["DeleteResponse"] & {
            /** @constant */
            object?: "workspace";
        };
        /** @description The paginated envelope listAdminCostRecords answers UNCONDITIONALLY. The three paging fields are always present, unlike the collection reads that omit them for an un-queried call: on a spend report, a first page that looks like the whole set is a wrong number rather than a missing one. */
        PaginatedList_CostRecord: {
            /** @constant */
            object: "list";
            data: components["schemas"]["CostRecord"][];
            /** @description Records matching the fence and the filters, computed in the same statement as the page so the two cannot disagree under a concurrent write. */
            total: number;
            offset: number;
            limit: number;
        };
        /** @description The cost of ONE request: the authenticated attribution and resolved model from the request log, joined to the priced provider attempts from the metering events (#677). The chargeback unit is the request, so `id` is the request id and every other evidence surface (request-logs, investigations, audit-events) is addressable by the same key. */
        CostRecord: {
            /** @constant */
            object: "cost_record";
            /** @description The request id -- the same value as request_id, repeated because every admin document carries an id. */
            id: string;
            request_id: string;
            trace_id: string | null;
            /** @description The agent run this request belongs to, when one declared itself. */
            agent_run_id: string | null;
            tenant_id: string | null;
            project_id: string | null;
            workspace_id: string | null;
            /** @description The key that authenticated the request -- the identifier, never the key material. */
            api_key_id: string | null;
            provider: string | null;
            /** @description The model name the caller asked for. */
            logical_model: string | null;
            /** @description The model the provider was actually asked for, which for a dated snapshot differs from logical_model. */
            provider_model: string | null;
            route: string | null;
            status_code: number | null;
            error_code: string | null;
            cache_status: string | null;
            streamed: boolean;
            latency_ms: number | null;
            /** @description Whole seconds. The list is ordered by this descending, with request_id ascending as the tiebreaker, so a page boundary inside a busy second cannot re-serve one row and skip another. */
            started_at_unix: number;
            completed_at_unix: number | null;
            prompt_tokens: number | null;
            completion_tokens: number | null;
            total_tokens: number | null;
            /** @description Null rather than 0 when nothing metered the request: 0 would assert the provider reported no cached tokens, which nobody ever asked it. */
            cached_input_tokens: number | null;
            /** @description Null when the request was never metered; see cached_input_tokens. */
            cache_write_tokens: number | null;
            /** @description Null when the request was never metered; see cached_input_tokens. */
            reasoning_tokens: number | null;
            /** @description Summed cost of the priced attempts, or null when nothing could be priced. Read it together with metered and priced: null with metered false means nothing metered the request at all (refused, rate-limited), and a number with priced false means SOME attempts were priced and others could not be. */
            cost_usd: number | null;
            /** @description True when at least one provider attempt was metered (billed_attempts > 0). */
            metered: boolean;
            /** @description True only when the request was metered AND every metered attempt carried a price. False with a non-null cost_usd means the figure is incomplete. */
            priced: boolean;
            /** @description Provider attempts that produced a metering event. */
            billed_attempts: number;
            /** @description Metered attempts with no price-book entry. Non-zero means a re-price would change this row, which is how an operator finds the rows a low total came from. */
            unpriced_attempts: number;
            /** @description The request's attribution metadata, bounded to string values at ingress. `{}` when there is none. */
            tags: {
                [key: string]: string;
            };
        };
        AdminAgentCostBurnList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminAgentCostBurn"][];
            total: number;
            offset: number;
            limit: number;
        };
        /** @description One durable per-agent cost-burn row: the runtime cost accumulated by a stable agent key within a billing period. */
        AdminAgentCostBurn: {
            /** @description Owning tenant. A tenant-scoped admin only ever sees its own tenant's rows. */
            tenant_id: string;
            /** @description Stable per-agent identity (the agent/deployment key), NOT a per-run id: every run of this agent inside the period folds into this row's total. */
            agent_key: string;
            /** @description Billing window this accumulated total covers, as YYYY-MM (UTC). */
            period: string;
            /**
             * Format: double
             * @description Total USD runtime cost accumulated for this (tenant, agent, period).
             */
            accumulated_usd: number;
            /**
             * Format: int64
             * @description Unix seconds of the most recent burn increment folded into this row.
             */
            updated_at_unix: number;
        };
        AdminObservedAgentActivityList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminObservedAgentActivity"][];
            total: number;
            offset: number;
            limit: number;
            presence_feed: components["schemas"]["AdminObservedAgentPresenceFeed"];
        };
        /** @description One tenant/key-scoped Unknown activity row observed through a durable virtual API key. */
        AdminObservedAgentActivity: {
            /** @description Stable identity of the observed activity: observed:<tenant_id>:<api_key_id>. Not a per-process/agent UUID. */
            id: string;
            /** @constant */
            source: "virtual_api_key";
            /** @constant */
            identity_status: "unattributed";
            /** @constant */
            display_name: "Unknown";
            /**
             * @description running | inactive | unknown. unknown (#494) is emitted when the durable presence feed did not answer and the request-log evidence alone cannot decide; it is NEVER collapsed into inactive, because an operator who reads inactive for a process that is in fact running may restart it.
             * @enum {string}
             */
            status: "running" | "inactive" | "unknown";
            /**
             * @description Which question the status answers: recent_api_key_activity (the evidence decided it) or presence_feed_unavailable (the durable presence read failed, so it could not be decided).
             * @enum {string}
             */
            status_basis: "recent_api_key_activity" | "presence_feed_unavailable";
            tenant_id: string;
            project_id?: string;
            workspace_id?: string;
            api_key_id: string;
            /** @description Redacted operator hint (the virtual key's name) the tenant may already view. Never the inferred agent name. */
            credential_name?: string;
            first_seen_at_unix: number;
            /** @description Freshest recency actually observed. A LOWER BOUND when evidence.presence_feed_status is unavailable: the unread presence feed could only ever have reported something fresher. */
            last_seen_at_unix: number;
            running_ttl_seconds: number;
            evidence: components["schemas"]["AdminObservedAgentActivityEvidence"];
        };
        /** @description Inspectable evidence justifying the row's derived status. */
        AdminObservedAgentActivityEvidence: {
            /** @constant */
            evidence_source: "request_logs";
            request_count: number;
            /** @description Age of last_seen_at_unix. An UPPER BOUND when presence_feed_status is unavailable. */
            seconds_since_last_seen: number;
            running_ttl_seconds: number;
            /** @description null (#494) when the presence feed did not answer and the request-log evidence alone is outside the window - the one case where the missing feed is exactly the signal that could have flipped the answer. */
            within_running_window: boolean | null;
            /** @description Whether the durable observed-agent presence store contributed the recency used for the running decision (its last-seen was at least as fresh as the request-log evidence). null (#494) when the presence read failed - false would claim the store was consulted and lost. */
            durable_presence_backed: boolean | null;
            /**
             * @description Whether the durable presence read succeeded for this derivation pass.
             * @enum {string}
             */
            presence_feed_status: "available" | "unavailable";
            /** @description The named storage condition when the presence read failed; null when it succeeded. Always serialized, so an unknown status is never unexplained. */
            presence_unavailable_reason: string | null;
            prompt_tokens?: number;
            completion_tokens?: number;
            total_tokens?: number;
            cost_usd?: number;
            usage_evidence_available: boolean;
            reason: string;
        };
        /**
         * @description A recognised Solana network, encoded as its canonical CAIP-2 identifier. A mainnet pin is always explicit and self-describing on the wire; a token symbol such as USDC is never accepted as a network.
         * @example solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1
         * @enum {string}
         */
        X402PolicyNetwork: "solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp" | "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1";
        /**
         * @description Rounding direction applied when an atomic->credits conversion does not divide evenly. 'up' (ceil) is the safe default for cap enforcement because it never under-counts a spend.
         * @enum {string}
         */
        X402Rounding: "down" | "up";
        /** @description Checked-integer conversion from atomic token units to internal credits: credits = round(atomic * numerator / denominator). A zero numerator or denominator is an impossible ratio and is rejected by config validation. */
        X402ConversionRule: {
            /** Format: int64 */
            numerator: number;
            /** Format: int64 */
            denominator: number;
            rounding: components["schemas"]["X402Rounding"];
            /** @description Opaque operator-set version tag for this ratio, persisted into every conversion snapshot. */
            version: string;
            /**
             * Format: int64
             * @description Unix second at which this ratio stops being usable. Absent means the rate never expires on its own. A decision made at or after it -- or one whose caller supplied no clock -- denies with x402_conversion_expired: stale money data is not a smaller cap, it is no cap.
             */
            expires_at_unix?: number | null;
        };
        /** @description One explicitly allowlisted (network, mint) pair. The mint is a canonical base58 SPL mint address, never a token symbol. */
        X402AllowedAsset: {
            network: components["schemas"]["X402PolicyNetwork"];
            /** @description Canonical base58 SPL mint address (validated 32 bytes). */
            mint: string;
        };
        /** @description A canonicalised resource rule: which HTTPS origin + path prefix a payment may unlock. Both the challenge resource and the already-authorized egress URL must match one of these. */
        X402ResourceRule: {
            /** @description Canonical origin scheme://host[:port]. Must be https unless allow_insecure_local_resources is set. */
            origin: string;
            /** @description Path prefix that gates the origin. '/' allows the whole origin. */
            path_prefix: string;
        };
        /** @description Spend caps, denominated in internal credits except the two atomic bounds which act directly on the on-chain amount. null means uncapped for that dimension; an explicit 0 is rejected by validation. */
        X402SpendCaps: {
            /** Format: int64 */
            max_credits_per_payment?: number | null;
            /** Format: int64 */
            max_credits_per_run?: number | null;
            /** Format: int64 */
            max_credits_per_window?: number | null;
            /** Format: int64 */
            window_seconds?: number | null;
            /** Format: int64 */
            max_atomic_per_payment?: number | null;
            /** Format: int64 */
            min_atomic_per_payment?: number | null;
        };
        /** @description Approval-mode policy: payments whose computed credits exceed the threshold require explicit out-of-band approval instead of auto-allow. null never requires approval. */
        X402ApprovalPolicy: {
            /** Format: int64 */
            threshold_credits?: number | null;
        };
        /**
         * @description The typed, disabled-by-default x402 spend policy an operator declares through FerroGate's structured config (issue #351). Parsed and validated into a runtime-evaluable policy; x402 spending is off unless enabled is explicitly true. This is config-in shape only; the runtime never evaluates a policy that failed validation.
         * @example {
         *       "enabled": true,
         *       "revision": 7,
         *       "allowed_networks": [
         *         "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1"
         *       ],
         *       "allowed_assets": [
         *         {
         *           "network": "solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1",
         *           "mint": "4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU"
         *         }
         *       ],
         *       "allowed_recipients": [
         *         "9WzDXwBbmkg8ZTbNMqUxvQRAyrZzDsGYdLVL9zYtAWWM"
         *       ],
         *       "allowed_resources": [
         *         {
         *           "origin": "https://api.example.com",
         *           "path_prefix": "/paid"
         *         }
         *       ],
         *       "caps": {
         *         "max_credits_per_payment": 1000,
         *         "max_credits_per_run": 5000,
         *         "max_credits_per_window": 10000,
         *         "window_seconds": 3600,
         *         "max_atomic_per_payment": 2000000,
         *         "min_atomic_per_payment": 10
         *       },
         *       "conversion": {
         *         "numerator": 1,
         *         "denominator": 1000,
         *         "rounding": "up",
         *         "version": "usdc-devnet-v1"
         *       },
         *       "approval": {
         *         "threshold_credits": 500
         *       },
         *       "allow_insecure_local_resources": false
         *     }
         */
        X402SpendPolicy: {
            /** @description Master switch. x402 spending is OFF unless explicitly true. */
            enabled: boolean;
            /**
             * Format: int64
             * @description Monotonic policy revision echoed into every decision for audit/diagnostics.
             */
            revision: number;
            allowed_networks: components["schemas"]["X402PolicyNetwork"][];
            allowed_assets: components["schemas"]["X402AllowedAsset"][];
            /** @description Explicit recipient (payTo) allowlist, canonical base58. Empty => nothing payable. */
            allowed_recipients: string[];
            allowed_resources: components["schemas"]["X402ResourceRule"][];
            caps: components["schemas"]["X402SpendCaps"];
            conversion: components["schemas"]["X402ConversionRule"];
            approval: components["schemas"]["X402ApprovalPolicy"];
            /** @description Test-only escape hatch permitting http:// resource origins. MUST be false in production. */
            allow_insecure_local_resources?: boolean;
        };
        /** @description The DNS-TXT ownership proof (or in-flight challenge) for one (tenant, hostname) pair (#488). A bound hostname only serves while this resolves to verified or grandfathered. */
        AdminSiteDomainVerification: {
            /** @constant */
            object: "site_domain_verification";
            /**
             * @description State as of now, with token/re-verification expiry applied.
             * @enum {string}
             */
            state: "pending_verification" | "verified" | "grandfathered" | "expired";
            serves: boolean;
            tenant_id: string;
            hostname: string;
            site: string;
            /** @description The DNS name to publish the TXT record at, _ferrogate-challenge.<hostname>. */
            challenge_record_name: string;
            /** @constant */
            challenge_record_type: "TXT";
            /** @description The exact TXT value to publish: a digest bound to this tenant, hostname, and issued token. */
            challenge_record_value: string;
            /** Format: int64 */
            issued_at_unix: number;
            /** Format: int64 */
            token_expires_at_unix: number;
            /** Format: int64 */
            verified_at_unix?: number | null;
            /** Format: int64 */
            verification_expires_at_unix?: number | null;
            /** Format: int64 */
            last_checked_at_unix?: number | null;
            last_failure_reason?: string | null;
            /** Format: int64 */
            attempt_count: number;
        };
        /** @description One reviewable diff produced by a coding run (#472), projected from the run timeline. attribution_verified/repo_verified are re-derived from the content-addressed product id rather than copied from the worker-reported record. */
        CodingAgentWorkProduct: {
            /** @constant */
            object: "coding_agent_work_product";
            /** @description sha256(tenant|run|repo|base_commit|diff_digest). */
            product_id: string;
            run_id: string;
            tenant_id: string;
            /** @description provider:host/namespace/name. */
            repo_id: string;
            base_commit: string;
            branch?: string;
            head_commit?: string;
            diff_digest: string;
            diff_byte_len: number;
            /**
             * @description Where the patch bytes are. They are never inlined into this projection.
             * @enum {string}
             */
            diff_carrier: "inline" | "artifact";
            diff_artifact_ref?: string;
            stats: components["schemas"]["CodingAgentDiffStats"];
            /** @description Advisory agent prose. Never a substitute for reading the diff. */
            summary?: string;
            extracted_at_unix: number;
            /** @description True only when the derived product id matches this run. False means the record was relabelled after extraction. */
            attribution_verified: boolean;
            repo_verified: boolean;
            published?: components["schemas"]["CodingAgentPublishedChange"];
        };
        CodingAgentDiffStats: {
            files_changed: number;
            insertions: number;
            deletions: number;
        };
        /** @description What left the platform for this work product, and whether the receipt actually describes publishing it. */
        CodingAgentPublishedChange: {
            /** @enum {string} */
            operation: "push_branch" | "open_pull_request" | "update_pull_request" | "push_tag";
            branch: string;
            head_commit: string;
            /** @enum {string} */
            outcome: "completed" | "refused" | "failed";
            review_reference?: string;
            grant_id: string;
            /** @description Target-level fingerprint of the vcs.write_back ActionIdentity, for the audit join. */
            action_fingerprint: string;
            completed_at_unix: number;
            /** @description True only when the receipt's tenant, run, repo, branch and head commit all match the work product it names. */
            matches_work_product: boolean;
        };
        /**
         * @description One level of the tenancy chain an x402 spend policy may be declared at, from broadest to narrowest. Precedence is by specificity: the narrowest declared scope in a request's chain is the one in force.
         * @enum {string}
         */
        X402PolicyScopeKind: "tenant" | "project" | "workspace" | "key" | "run";
        /** @description A reference to the scope whose declaration is in force. */
        X402PolicyScopeRef: {
            scope_type: components["schemas"]["X402PolicyScopeKind"];
            scope_id: string;
        };
        /** @description The tenant/project/workspace/key/agent-run chain a policy lookup or dry-run evaluation is made at. tenant_id is required; narrower levels are optional. */
        X402ScopeChain: {
            tenant_id: string;
            project_id?: string | null;
            workspace_id?: string | null;
            key_id?: string | null;
            run_id?: string | null;
        };
        /** @description One operator-declared x402 spend policy, pinned to a scope. Carries no secret or signer material: the policy model is an allowlist/caps/conversion document only. */
        X402DeclaredSpendPolicy: {
            /** @constant */
            object: "x402_spend_policy";
            scope_type: components["schemas"]["X402PolicyScopeKind"];
            scope_id: string;
            enabled: boolean;
            /** Format: int64 */
            revision: number;
            policy: components["schemas"]["X402SpendPolicy"];
        };
        X402DeclaredSpendPolicyList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["X402DeclaredSpendPolicy"][];
        };
        /** @description One level of the resolved inheritance chain, broadest first: whether a declaration exists there and whether it is the one in force. This is the 'why did this policy win' evidence. */
        X402PolicyInheritanceLevel: {
            scope_type: components["schemas"]["X402PolicyScopeKind"];
            scope_id: string;
            declared: boolean;
            effective: boolean;
        };
        /** @description The effective x402 spend policy for a scope chain. Always defined: a chain with no declaration anywhere reports declared=false with the disabled deny-all default at revision 0, so 'no policy' can never read as 'no limit'. Exposes decisions/revisions only -- no signer key, secret, or wallet material. */
        X402EffectiveSpendPolicy: {
            /** @constant */
            object: "x402_effective_spend_policy";
            scope: components["schemas"]["X402ScopeChain"];
            declared: boolean;
            resolved_scope: components["schemas"]["X402PolicyScopeRef"] | null;
            /** Format: int64 */
            policy_revision: number;
            policy: components["schemas"]["X402SpendPolicy"];
            inheritance: components["schemas"]["X402PolicyInheritanceLevel"][];
        };
        /** @description The atomic->credits conversion in effect for one decision. Integer-only; computed_credits is null (never 0) when the conversion overflowed or the ratio was impossible, which denies. */
        X402ConversionSnapshot: {
            /** Format: int64 */
            numerator: number;
            /** Format: int64 */
            denominator: number;
            rounding: components["schemas"]["X402Rounding"];
            version: string;
            /**
             * Format: int64
             * @description Unix second at which this ratio stops being usable. Absent means the rate never expires on its own. A decision made at or after it -- or one whose caller supplied no clock -- denies with x402_conversion_expired: stale money data is not a smaller cap, it is no cap.
             */
            expires_at_unix: number | null;
            /** Format: int64 */
            computed_credits: number | null;
        };
        /** @description The immutable decision evidence for one payment authorization: three-valued decision, stable reason code, policy revision, matched rule, ORIGINAL atomic units, computed internal credits, conversion snapshot, and the deterministic challenge hash. No signer, secret, or transaction material. */
        X402PaymentDecision: {
            /** @enum {string} */
            decision: "allow" | "approval_required" | "deny";
            /** @description Stable x402_* reason code (e.g. x402_allowed, x402_disabled, x402_network_not_allowed, x402_mint_not_allowed, x402_recipient_not_allowed, x402_resource_mismatch, x402_resource_not_allowed, x402_amount_below_min, x402_atomic_cap_exceeded, x402_conversion_unavailable, x402_over_per_payment_cap, x402_over_run_cap, x402_over_window_cap, x402_approval_required, x402_intent_mismatch, x402_conversion_expired). */
            reason_code: string;
            message: string;
            /** Format: int64 */
            approval_threshold_credits: number | null;
            /** Format: int64 */
            policy_revision: number;
            network_caip2: components["schemas"]["X402PolicyNetwork"];
            mint: string;
            recipient: string;
            resource_url: string;
            authorized_resource_url: string;
            /** @description HTTP method of the already-authorized request this decision is bound to. Together with request_body_sha256_hex this is what stops an authorization for a GET being reused for a POST of a different body to the same URL. */
            http_method: string;
            /** @description SHA-256 of the already-authorized request body, lowercase hex. A bodyless request hashes the empty byte string, which is a concrete value rather than a wildcard. */
            request_body_sha256_hex: string;
            /** @description Deterministic hash of the immutable payment intent this decision authorized. */
            intent_hash_hex: string;
            /** @description Deterministic seal over the decision's load-bearing content (outcome, reason code, revision, payment terms, intent, priced credits). Recomputable at an audit sink to prove the decision stored is the decision policy made. The human-readable message is deliberately outside the seal. */
            decision_hash_hex: string;
            challenge_hash_hex: string;
            /**
             * Format: int64
             * @description Original on-chain atomic token amount, preserved losslessly as an integer.
             */
            atomic_amount: number;
            /** Format: int64 */
            computed_credits: number | null;
            conversion: components["schemas"]["X402ConversionSnapshot"];
            matched_resource: components["schemas"]["X402ResourceRule"] | null;
        };
        /** @description A dry-run payment-authorization request. payment_required is the untrusted merchant PAYMENT-REQUIRED header exactly as received (base64), parsed by the frozen x402/SVM wire contract -- display text and token symbols are never trusted. authorized_resource_url is the egress URL FerroGate already authorized; a challenge whose own resource differs is a payment-redirect attempt and denies. The decision is bound to a whole payment intent -- method, request-body hash and canonical URL -- not just a URL, so an authorized GET and a POST of a different body to the same URL are two different authorizations. */
        X402SpendPolicyEvaluationRequest: {
            scope: components["schemas"]["X402ScopeChain"];
            payment_required: string;
            authorized_resource_url: string;
            /**
             * @description HTTP method of the already-authorized request. Defaults to GET, the only method that is bodyless by definition, so an omitted method is never read as "any method".
             * @default GET
             */
            authorized_method: string;
            /** @description Lowercase-hex SHA-256 of the already-authorized request body. Absent means the bodyless hash. An unparseable value is a 400, never a silent fallback. */
            authorized_request_body_sha256_hex?: string | null;
            /** @description Already-committed spend read from the durable ledger, so per-run/per-window caps can be dry-run realistically. Defaults to a fresh run/window. */
            spent?: {
                /** Format: int64 */
                run_spent_credits?: number;
                /** Format: int64 */
                window_spent_credits?: number;
                /**
                 * Format: int64
                 * @description The caller's clock, used only to test the conversion rule's validity window. Absent means a policy whose conversion declares an expiry denies.
                 */
                now_unix?: number | null;
            };
        };
        /** @description The result of a dry-run evaluation: which declaration was in force, at which revision, and the decision it produced. */
        X402SpendPolicyEvaluation: {
            /** @constant */
            object: "x402_spend_policy_evaluation";
            scope: components["schemas"]["X402ScopeChain"];
            declared: boolean;
            resolved_scope: components["schemas"]["X402PolicyScopeRef"] | null;
            /** Format: int64 */
            policy_revision: number;
            /** @description The ledger figures the dry run actually used, and whether the caller supplied them. An omitted `spent` makes a dry run answer for a FRESH run/window, where the real run could deny x402_over_run_cap, so the assumption is reported rather than left implicit. */
            spent: {
                /** Format: int64 */
                run_spent_credits: number;
                /** Format: int64 */
                window_spent_credits: number;
                /** Format: int64 */
                now_unix: number | null;
                /** @description False means the caller sent no spent object and these are assumed zeroes. */
                supplied: boolean;
            };
            decision: components["schemas"]["X402PaymentDecision"];
        };
        /** @description One durable x402 payment attempt (#352): the complete immutable audit tuple (wire-contract identity + #351 policy decision) plus the mutable state/evidence tail. Money keeps its stored representation exactly: atomic_amount and settled_atomic_amount are canonical decimal STRINGS of the on-chain u64 atomic units (a u64 does not survive JSON's double), and credits_amount is the integer internal wallet-credit hold. Nothing here is rounded or recomputed. */
        AdminPaymentAttempt: {
            /** @constant */
            object: "payment_attempt";
            /** @description Attempt / idempotency id. */
            id: string;
            tenant_id: string;
            project_id?: string | null;
            workspace_id?: string | null;
            run_id?: string | null;
            worker_id?: string | null;
            request_id?: string | null;
            trace_id?: string | null;
            /** @description HTTP method of the egress request the payment unlocks. */
            method: string;
            /** @description Canonical resource URL FerroGate authorized egress to. */
            resource_url: string;
            /** @description Hex SHA-256 of the authorized request body. */
            request_body_hash?: string | null;
            /** @description Deterministic hex challenge hash from the frozen wire contract. */
            challenge_hash: string;
            /** Format: int64 */
            x402_version: number;
            scheme: string;
            /** @description CAIP-2 network identifier. */
            network_caip2: string;
            /** @description SPL token mint (base58). */
            mint: string;
            /** @description ORIGINAL on-chain atomic amount, canonical decimal u64 string. */
            atomic_amount: string;
            /** @description payTo recipient (base58). */
            recipient: string;
            /**
             * Format: int64
             * @description Internal wallet credits held for this payment.
             */
            credits_amount?: number | null;
            /** @description Conversion-rule version tag in effect at decision time (#351). */
            conversion_version?: string | null;
            /** Format: int64 */
            policy_revision: number;
            /** @enum {string} */
            decision: "allow" | "approval_required" | "deny";
            /** @description Stable x402_* policy reason code. */
            reason_code: string;
            /** @description Linked wallet reservation (hold) id. Absent on a denied attempt. */
            hold_id?: string | null;
            /**
             * @description Attempt state. outcome_unknown is NON-TERMINAL and RETAINS the wallet hold: after proof submission a timeout or RPC ambiguity is not proof the money did not move.
             * @enum {string}
             */
            state: "challenged" | "authorized" | "submitted" | "settled" | "denied" | "released" | "failed" | "outcome_unknown";
            /**
             * Format: int64
             * @description Monotonic CAS operation token, bumped by every applied transition.
             */
            generation: number;
            /** Format: int64 */
            submitted_at_unix?: number | null;
            /** @description Base58 on-chain transaction signature recorded on settle. */
            transaction_signature?: string | null;
            settled_atomic_amount?: string | null;
            /** @description Raw settlement evidence blob, when captured. */
            settlement_response?: string | null;
            failure_code?: string | null;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        /** @description The wallet hold (#281 reservation) this attempt reserved. status is reported as STORED; available balance separately ignores a hold past expires_at_unix, so an active row with an elapsed expiry no longer counts against the wallet. */
        AdminPaymentAttemptReservation: {
            /** @constant */
            object: "wallet_reservation";
            id: string;
            tenant_id: string;
            /** Format: int64 */
            amount_credits: number;
            /** @enum {string} */
            status: "active" | "settled" | "released";
            /** Format: int64 */
            expires_at_unix: number;
            settlement_id?: string | null;
            /** Format: int64 */
            created_at_unix: number;
            /** Format: int64 */
            updated_at_unix: number;
        };
        /** @description The captured wallet settlement (the ledger charge the hold produced). */
        AdminPaymentAttemptSettlement: {
            /** @constant */
            object: "wallet_settlement";
            id: string;
            tenant_id: string;
            /** Format: int64 */
            delta_credits: number;
            /** Format: int64 */
            balance_after_credits?: number | null;
            /** Format: int64 */
            created_at_unix: number;
        };
        /** @description An attempt joined to its wallet hold and captured settlement -- the #352 E2E evidence chain (attempt, hold, wallet settlement, policy revision, transaction evidence) in one read. */
        AdminPaymentAttemptLinks: {
            /** @constant */
            object: "payment_attempt_links";
            attempt: components["schemas"]["AdminPaymentAttempt"];
            /** @description Absent when the attempt records no hold (denied) or the hold row is gone. */
            reservation?: components["schemas"]["AdminPaymentAttemptReservation"] | null;
            /** @description Absent until the hold is captured. */
            settlement?: components["schemas"]["AdminPaymentAttemptSettlement"] | null;
        };
        /** @description One BOUNDED page of a tenant's attempts, newest-first. payment_attempts grows one row per paid egress request, so there is no unbounded listing: limit is clamped server-side and paging is a keyset cursor over (created_at_unix, id), never an OFFSET scan. */
        AdminPaymentAttemptList: {
            /** @constant */
            object: "list";
            data: components["schemas"]["AdminPaymentAttempt"][];
            /** @description The page size actually applied, after clamping. */
            limit: number;
            /** @description Opaque cursor to pass back as ?cursor= for the next page. Null at the definitive end of the listing. */
            next_cursor?: string | null;
        };
        /** @description Availability of the durable observed-agent presence feed for this read (#494). An EMPTY data array is itself an answer, and a failed presence read makes that answer unsound; rows_may_be_incomplete says so instead of letting an empty list read as 'nothing is running'. */
        AdminObservedAgentPresenceFeed: {
            /** @enum {string} */
            status: "available" | "unavailable";
            /** @description The storage diagnostic when status is unavailable; null otherwise. Rendered as an explicit null so the field's presence is not itself a signal. */
            unavailable_reason: string | null;
            /** @description true when the feed did not answer, so absent or inactive rows are NOT evidence that nothing is running. */
            rows_may_be_incomplete: boolean;
        };
    };
    responses: {
        /** @description API key mutation response. */
        AdminApiKeyMutationOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AdminApiKeyMutationResponse"];
            };
        };
        /** @description Policy mutation response. */
        AdminPolicyMutationOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AdminPolicyMutationResponse"];
            };
        };
        /** @description Gateway config mutation response. */
        AdminGatewayConfigMutationOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AdminGatewayConfigMutationResponse"];
            };
        };
        /** @description Agent workflow mutation response. */
        AdminAgentWorkflowMutationOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AdminAgentWorkflowMutationResponse"];
            };
        };
        /** @description MCP server mutation response. */
        AdminMcpServerMutationOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AdminMcpServerMutationResponse"];
            };
        };
        /** @description MCP server status response. */
        AdminMcpServerStatusOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["McpServerStatus"];
            };
        };
        /** @description API key delete response. */
        DeleteApiKeyOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeleteApiKeyResponse"];
            };
        };
        /** @description Policy delete response. */
        DeletePolicyOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeletePolicyResponse"];
            };
        };
        /** @description Gateway config delete response. */
        DeleteGatewayConfigOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeleteGatewayConfigResponse"];
            };
        };
        /** @description Agent workflow delete response. */
        DeleteAgentWorkflowOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeleteAgentWorkflowResponse"];
            };
        };
        /** @description MCP server delete response. */
        DeleteMcpServerResponse: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeleteMcpServerResponse"];
            };
        };
        /** @description Invalid request payload or candidate config. */
        BadRequest: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Missing or invalid bearer token when API keys are configured. */
        Unauthorized: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Bearer token lacks the required scope or is disallowed by tenant/provider/model constraints. */
        Forbidden: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Requested API key or policy was not found. */
        NotFound: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Runtime rejected the candidate process-local reload. */
        Conflict: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Request body exceeded the endpoint limit. */
        PayloadTooLarge: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Identity provider, credential storage, secret resolution, or encryption key is unavailable. */
        ServiceUnavailable: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Prompt template mutation response. */
        AdminPromptTemplateMutationOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AdminPromptTemplateMutationResponse"];
            };
        };
        /** @description Prompt template archive response. */
        DeletePromptTemplateOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeletePromptTemplateResponse"];
            };
        };
        /** @description Skill package mutation response. */
        AdminSkillPackageMutationOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AdminSkillPackageMutationResponse"];
            };
        };
        /** @description Agent upstream mutation response. */
        AdminAgentUpstreamMutationOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AdminAgentUpstreamMutationResponse"];
            };
        };
        /** @description Skill package delete response. */
        DeleteSkillPackageResponse: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeleteResponse"];
            };
        };
        /** @description Agent upstream delete response. */
        DeleteAgentUpstreamResponse: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeleteResponse"];
            };
        };
        /** @description The fixed route exists but does not support this HTTP method. */
        MethodNotAllowed: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Asset reference, variant, channel target, checksum, or JSON control body is invalid. */
        AssetBadRequest: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AssetErrorResponse"];
            };
        };
        /** @description The requested asset, version, variant, channel, or uploaded object does not exist. */
        AssetNotFound: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AssetErrorResponse"];
            };
        };
        /** @description The operation conflicts with immutable version state or the selected transfer mode. */
        AssetConflict: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AssetErrorResponse"];
            };
        };
        /** @description The inline body, control body, or declared object exceeds its configured limit. */
        AssetPayloadTooLarge: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AssetErrorResponse"];
            };
        };
        /** @description Uploaded bytes failed checksum, size, content, signature, or malware validation and are not published. */
        AssetUnprocessableEntity: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AssetErrorResponse"];
            };
        };
        /** @description Asset storage is unavailable or a commit outcome cannot be proven. asset_bucket_unavailable covers final-key generation or object-write failure and preserves staging plus any final candidate. asset_commit_outcome_unknown means the atomic metadata create may already be durable; retry with the same upload_id and do not delete staging or final objects. */
        AssetServiceUnavailable: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AssetErrorResponse"];
            };
        };
        /** @description The resolved asset download exceeds the effective egress byte budget or download request rate. */
        AssetTooManyRequests: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AssetErrorResponse"];
            };
        };
        /** @description Stored asset bytes failed their recorded SHA-256 integrity check. */
        AssetInternalServerError: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AssetErrorResponse"];
            };
        };
        /** @description Project delete response. */
        DeleteProjectOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeleteProjectResponse"];
            };
        };
        /** @description Workspace delete response. */
        DeleteWorkspaceOk: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["DeleteWorkspaceResponse"];
            };
        };
        /** @description Too many requests. */
        TooManyRequests: {
            headers: {
                "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
    };
    parameters: {
        ApiKeyId: string;
        PolicyName: string;
        GuardrailPolicyId: string;
        GuardrailPolicyRevision: number;
        GatewayConfigId: string;
        AgentWorkflowId: string;
        Offset: number;
        /** @description Clamped by storage.admin_list_max_limit. */
        Limit: number;
        /** @description Gateway plugin id. */
        PluginId: string;
        /** @description Stable prompt template id. */
        PromptTemplateId: string;
        /** @description Optional skill package id that must be visible to the authenticated API key. When set, tool/MCP execution is allowed only if the package declares the requested capability, and audit evidence is linked to the package. */
        SkillPackageHeader: string;
        /** @description Optional skill package version. Requires x-ferrogate-skill-package and must match the registered package version. */
        SkillPackageVersionHeader: string;
        /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
        ClientActionIdHeader: string;
        /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
        ClientFingerprintHeader: string;
        /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
        ClientClockUnverifiedHeader: string;
        /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
        ClientTimeTokenHeader: string;
        /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
        ClientReportedIpHeader: string;
    };
    requestBodies: {
        AdminApiKeyMutation: {
            content: {
                "application/json": components["schemas"]["AdminApiKeyMutation"];
            };
        };
        AdminPolicyMutation: {
            content: {
                "application/json": components["schemas"]["AdminPolicyMutation"];
            };
        };
        ToolApprovalDecisionRequest: {
            content: {
                "application/json": components["schemas"]["ToolApprovalDecisionRequest"];
            };
        };
        AdminGatewayConfigMutation: {
            content: {
                "application/json": components["schemas"]["AdminGatewayConfigMutation"];
            };
        };
        AdminAgentWorkflowMutation: {
            content: {
                "application/json": components["schemas"]["AgentWorkflowPolicy"];
            };
        };
        AdminConfigValidateRequest: {
            content: {
                "application/json": components["schemas"]["AdminConfigValidateRequest"];
            };
        };
        AdminDrainRequest: {
            content: {
                "application/json": components["schemas"]["AdminDrainRequest"];
            };
        };
        AdminPromptTemplateMutation: {
            content: {
                "application/json": components["schemas"]["AdminPromptTemplateMutation"];
            };
        };
        AdminMcpServerMutation: {
            content: {
                "application/json": components["schemas"]["AdminMcpServerMutation"];
            };
        };
        PromptTemplateRenderRequest: {
            content: {
                "application/json": components["schemas"]["PromptTemplateRenderRequest"];
            };
        };
        AdminAgentUpstreamMutationRequest: {
            content: {
                "application/json": components["schemas"]["AdminAgentUpstreamMutation"];
            };
        };
        AdminSkillPackageMutation: {
            content: {
                "application/json": components["schemas"]["SkillPackage"];
            };
        };
    };
    headers: {
        /** @description Short-lived, server-issued time token (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. When a request presents x-ferrogate-action-id, FerroGate signs this response header at the downstream response boundary. A client echoes it byte-for-byte on the next request of the same action and reports issued_at as the authoritative client_sent_at. The server validates the HMAC signature, action binding and TTL against its own receive clock; the client clock remains unverified and is never the authority. Tokens last seconds, not minutes, to bound pre-fetch and replay. */
        ClientTimeTokenResponseHeader: string;
    };
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getHealthz: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Gateway health response. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthResponse"];
                };
            };
        };
    };
    getReadyz: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Gateway is ready to serve traffic with a loaded state revision. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReadinessResponse"];
                };
            };
            /** @description Gateway is not ready to serve traffic. */
            503: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getAdminDashboard: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Admin dashboard HTML. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "text/html": string;
                };
            };
        };
    };
    mcpJsonRpc: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
                /** @description Required for modern candidate requests and must match params._meta.io.modelcontextprotocol/protocolVersion; legacy initialize-based requests retain their revision-specific behavior. */
                "MCP-Protocol-Version"?: string;
                /** @description Required for modern candidate requests and must match the JSON-RPC method. */
                "Mcp-Method"?: string;
                /** @description Required for modern tools/call, resources/read, and prompts/get requests; Base64 sentinel values are decoded before body comparison. */
                "Mcp-Name"?: string;
                /** @description Optional skill package id that must be visible to the authenticated API key. When set, tool/MCP execution is allowed only if the package declares the requested capability, and audit evidence is linked to the package. */
                "x-ferrogate-skill-package"?: components["parameters"]["SkillPackageHeader"];
                /** @description Optional skill package version. Requires x-ferrogate-skill-package and must match the registered package version. */
                "x-ferrogate-skill-package-version"?: components["parameters"]["SkillPackageVersionHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["McpJsonRpcRequest"];
            };
        };
        responses: {
            /** @description MCP JSON-RPC response. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpJsonRpcResponse"];
                };
            };
            /** @description Modern MCP metadata/header mismatch or unsupported protocol version. */
            400: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpJsonRpcResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description Unknown modern MCP JSON-RPC method. */
            404: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpJsonRpcResponse"];
                };
            };
        };
    };
    authorizeMcpIdentity: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                server: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Authorization URL and one-time state. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpOauthAuthorizeResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    getMcpIdentity: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                server: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description MCP identity metadata. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpIdentityStatus"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    revokeMcpIdentity: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                server: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revoked MCP identity metadata. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpIdentityStatus"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    completeMcpIdentityOauth: {
        parameters: {
            query: {
                code: string;
                state: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Connected MCP identity metadata. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpIdentityStatus"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    createAgentRun: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AgentRunCreateRequest"];
            };
        };
        responses: {
            /** @description Agent run completed immediately through the default bounded harness. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentRunCreateResponse"];
                };
            };
            /** @description Agent run stopped before completion due to configured bounds. */
            202: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentRunCreateResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    submitAgentJob: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
                /** @description Explicit idempotency key. Takes precedence over the body's idempotency_key. */
                "Idempotency-Key"?: string;
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AgentJobSubmitRequest"];
            };
        };
        responses: {
            /** @description Duplicate submission: the ORIGINAL run_id is returned and no second run is started. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentJobSubmitResponse"];
                };
            };
            /** @description Job accepted; the durable run_id can be observed and collected. */
            202: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentJobSubmitResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            429: components["responses"]["TooManyRequests"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    getAgentJob: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Durable agent job id returned by submitAgentJob (the agent run id). */
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent job status. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentJobStatus"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            405: components["responses"]["MethodNotAllowed"];
        };
    };
    listAgentJobEvents: {
        parameters: {
            query?: {
                /** @description Resume after this cursor. Pass a previous page's next_after_event_id (an opaque "<occurred_at_unix>:<event id>" position token, which stays resolvable after the event it names is pruned); a bare event id copied from data[].id is also accepted. An unresolvable cursor is NOT an error: the page restarts from the oldest retained event with cursor_reset=true. */
                after_event_id?: string;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Durable agent job id returned by submitAgentJob (the agent run id). */
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One page of agent-run timeline events for the job. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentJobEventPage"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            405: components["responses"]["MethodNotAllowed"];
        };
    };
    getAgentJobResult: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Durable agent job id returned by submitAgentJob (the agent run id). */
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Terminal job result. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentJobResult"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["Conflict"];
        };
    };
    cancelAgentJob: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Durable agent job id returned by submitAgentJob (the agent run id). */
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Job is terminal. `cancelled` is false when it was already terminal (cancel is idempotent). */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentJobCancelResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["Conflict"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    executeMcpTool: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
                /** @description Optional skill package id that must be visible to the authenticated API key. When set, tool/MCP execution is allowed only if the package declares the requested capability, and audit evidence is linked to the package. */
                "x-ferrogate-skill-package"?: components["parameters"]["SkillPackageHeader"];
                /** @description Optional skill package version. Requires x-ferrogate-skill-package and must match the registered package version. */
                "x-ferrogate-skill-package-version"?: components["parameters"]["SkillPackageVersionHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ToolExecutionRequest"];
            };
        };
        responses: {
            /** @description Tool execution response. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ToolExecutionResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminOverview: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Control-plane overview. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminOverview"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getAdminStatus: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Gateway status. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminStatus"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    registerAdminSelfHostedWorker: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSelfHostedWorkerRegistrationRequest"];
            };
        };
        responses: {
            /** @description Self-hosted worker registration record. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerRegistrationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminStatusAlias: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Gateway status. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminStatus"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminProviders: {
        parameters: {
            query?: {
                /** @description Case-insensitive match against provider name or kind. */
                search?: string;
                offset?: number;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Provider list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProviderList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminProviderHealth: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Provider health list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProviderHealthList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminProviderModels: {
        parameters: {
            query?: {
                /** @description Optional configured provider name filter. */
                provider?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Provider model catalog list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProviderModelCatalogList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listProviderCredentials: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Redacted provider credential listing. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProviderCredentialList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    putProviderCredential: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description BYOK alias, held to the same grammar the `byok://` reference parser enforces (lowercase alphanumerics plus . _ -, no path separators, 1-64 characters). A segment outside it is 400 `invalid_request_body`, never a lookup. */
                alias: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminProviderCredentialMutation"];
            };
        };
        responses: {
            /** @description Stored credential metadata, read back after sealing. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProviderCredentialMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            413: components["responses"]["PayloadTooLarge"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    revokeProviderCredential: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description BYOK alias, held to the same grammar the `byok://` reference parser enforces (lowercase alphanumerics plus . _ -, no path separators, 1-64 characters). A segment outside it is 400 `invalid_request_body`, never a lookup. */
                alias: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revocation acknowledgement. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProviderCredentialRevokeResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    recordAdminSelfHostedWorkerHeartbeat: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSelfHostedWorkerHeartbeatRequest"];
            };
        };
        responses: {
            /** @description Recorded self-hosted worker heartbeat and updated worker projection. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerHeartbeatResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listAdminSelfHostedWorkerTelemetryEvents: {
        parameters: {
            query?: {
                /** @description Return events after this event id. Unknown cursors replay from the beginning. */
                after_event_id?: string;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Self-hosted worker telemetry event stream page. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerEventStream"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    recordAdminSelfHostedWorkerTelemetryEvent: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSelfHostedWorkerTelemetryEventRequest"];
            };
        };
        responses: {
            /** @description Recorded self-hosted worker telemetry event and updated worker projection. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerTelemetryEventResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listAdminManagedWorkers: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Managed worker runtime contract list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminManagedWorkerRuntimeList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminFrameworkAdapters: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Framework adapter runtime contract list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminFrameworkAdapterRuntimeList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminSelfHostedWorkers: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Self-hosted worker runtime contract list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerRuntimeList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    registerSelfHostedWorker: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSelfHostedWorkerRegistrationRequest"];
            };
        };
        responses: {
            /** @description Registered self-hosted worker. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerRegistrationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getAdminSelfHostedWorker: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Storage-backed self-hosted worker record. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerRecord"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listAdminSelfHostedWorkerRecords: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated self-hosted worker records backed by storage evidence. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerRecordList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminObservability: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Observability exporter status list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ObservabilityStatusList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminExtensions: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Extension list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminExtensionList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminPlugins: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Plugin list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPluginList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminPlugin: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminPluginMutation"];
            };
        };
        responses: {
            /** @description Plugin created. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPluginMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
        };
    };
    getAdminPlugin: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Gateway plugin id. */
                plugin_id: components["parameters"]["PluginId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Plugin status. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPlugin"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    deleteAdminPlugin: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Gateway plugin id. */
                plugin_id: components["parameters"]["PluginId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Plugin deleted. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    updateAdminPlugin: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Gateway plugin id. */
                plugin_id: components["parameters"]["PluginId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminPluginMutation"];
            };
        };
        responses: {
            /** @description Plugin updated. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPluginMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    listAdminPluginTools: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Gateway plugin id. */
                plugin_id: components["parameters"]["PluginId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Plugin tool list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminToolList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listAdminTools: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tool list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminToolList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminToolApprovals: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tool approval list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ToolApprovalList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getAdminToolApproval: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Tool approval id. */
                approval_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tool approval record. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ToolApprovalRecord"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    approveAdminToolApproval: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Tool approval id. */
                approval_id: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["ToolApprovalDecisionRequest"];
        responses: {
            /** @description Approved tool approval record. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ToolApprovalRecord"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    denyAdminToolApproval: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Tool approval id. */
                approval_id: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["ToolApprovalDecisionRequest"];
        responses: {
            /** @description Denied tool approval record. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ToolApprovalRecord"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    expireAdminToolApproval: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Tool approval id. */
                approval_id: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["ToolApprovalDecisionRequest"];
        responses: {
            /** @description Expired tool approval record. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ToolApprovalRecord"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    listAdminMcpServers: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description MCP server status list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["McpServerStatusList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminMcpServer: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminMcpServerMutation"];
        responses: {
            201: components["responses"]["AdminMcpServerMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
        };
    };
    getAdminMcpServer: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable MCP server name. */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["AdminMcpServerStatusOk"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminMcpServer: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable MCP server name. */
                name: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminMcpServerMutation"];
        responses: {
            200: components["responses"]["AdminMcpServerMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    deleteAdminMcpServer: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable MCP server name. */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeleteMcpServerResponse"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    patchAdminMcpServer: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable MCP server name. */
                name: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminMcpServerMutation"];
        responses: {
            200: components["responses"]["AdminMcpServerMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    listAdminToolSessionEvents: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Caller-supplied tool session identifier. */
                session_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tool session audit timeline. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditEventList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminModels: {
        parameters: {
            query?: {
                /** @description Case-insensitive match against model name, provider, or provider model. */
                search?: string;
                offset?: number;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Model list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminModelList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminApiKeys: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description API key list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminApiKeyList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminApiKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminApiKeyMutation"];
        responses: {
            201: components["responses"]["AdminApiKeyMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminApiKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["ApiKeyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["AdminApiKeyMutationOk"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminApiKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["ApiKeyId"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminApiKeyMutation"];
        responses: {
            200: components["responses"]["AdminApiKeyMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    deleteAdminApiKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["ApiKeyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeleteApiKeyOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    patchAdminApiKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["ApiKeyId"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminApiKeyMutation"];
        responses: {
            200: components["responses"]["AdminApiKeyMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listAdminTenants: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tenant reference list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminTenantRefList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminPolicies: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Policy list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PolicyRuleList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminPolicyMutation"];
        responses: {
            201: components["responses"]["AdminPolicyMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                name: components["parameters"]["PolicyName"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["AdminPolicyMutationOk"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                name: components["parameters"]["PolicyName"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminPolicyMutation"];
        responses: {
            200: components["responses"]["AdminPolicyMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    deleteAdminPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                name: components["parameters"]["PolicyName"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeletePolicyOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    patchAdminPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                name: components["parameters"]["PolicyName"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminPolicyMutation"];
        responses: {
            200: components["responses"]["AdminPolicyMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listAdminRequestLogs: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated request log list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RequestLogList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminAgentRuns: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated agent run summary list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentRunList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getAdminAgentRunTimeline: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Agent run id from x-ferrogate-agent-run-id or the gateway-generated run id. */
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent run timeline. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentRunTimeline"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    getAdminSelfHostedRunTimeline: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Self-hosted run id reported by a customer-owned worker. */
                run_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Self-hosted worker reported run timeline. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedRunTimeline"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    exportAdminRequestLogsJsonl: {
        parameters: {
            query?: {
                organization_id?: string;
                project_id?: string;
                model?: string;
                provider?: string;
                status?: number;
                since?: number;
                until?: number;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Newline-delimited JSON request-log export records. Prompt and response bodies are included only when body recording was explicitly enabled and configured secrets have been redacted. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/x-ndjson": string;
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminBillingEventsCompat: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated token metering event list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TokenMeteringEventList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminMeteringExportStatus: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Recent metering export status list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MeteringExportStatusList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminUsageAggregates: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Usage aggregate list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UsageAggregateList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminAuditEvents: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated audit event list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditEventList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listGuardrailEvaluations: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
                request_id?: string;
                trace_id?: string;
                agent_run_id?: string;
                scope_type?: string;
                scope_id?: string;
                subject_id?: string;
                policy_id?: string;
                policy_revision?: number;
                detector_id?: string;
                category?: string;
                verdict?: "pass" | "fail" | "error";
                action?: "allow" | "block" | "redact" | "record";
                error_kind?: string;
                since_unix?: number;
                until_unix?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated Guardrail evaluation evidence. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailEvaluationList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description Guardrail evidence storage unavailable. */
            500: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getGuardrailInvestigation: {
        parameters: {
            query?: {
                request_id?: string;
                trace_id?: string;
                agent_run_id?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Unified investigation timeline. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailInvestigationTimeline"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            /** @description Investigation evidence storage unavailable. */
            500: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    validateAdminConfig: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminConfigValidateRequest"];
        responses: {
            /** @description Validation result. Invalid candidate config returns valid=false rather than a 4xx. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminConfigValidateResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    reloadAdminConfig: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminConfigValidateRequest"];
        responses: {
            /** @description Reload result. Rejected candidates return committed=false rather than a 4xx. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminConfigReloadResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminDrain: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Drain status. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminDrainResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    setAdminDrain: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminDrainRequest"];
        responses: {
            /** @description Updated drain status. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminDrainResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getMetrics: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Prometheus text exposition. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "text/plain": string;
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminMeteringEvents: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated token metering event list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TokenMeteringEventList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminGatewayConfigs: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Gateway config profile list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminGatewayConfigProfileList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminGatewayConfig: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminGatewayConfigMutation"];
        responses: {
            201: components["responses"]["AdminGatewayConfigMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminGatewayConfig: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["GatewayConfigId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["AdminGatewayConfigMutationOk"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminGatewayConfig: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["GatewayConfigId"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminGatewayConfigMutation"];
        responses: {
            200: components["responses"]["AdminGatewayConfigMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    deleteAdminGatewayConfig: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["GatewayConfigId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeleteGatewayConfigOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    patchAdminGatewayConfig: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["GatewayConfigId"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminGatewayConfigMutation"];
        responses: {
            200: components["responses"]["AdminGatewayConfigMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listAdminAgentWorkflows: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent workflow policy list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentWorkflowList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminAgentWorkflow: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminAgentWorkflowMutation"];
        responses: {
            201: components["responses"]["AdminAgentWorkflowMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminAgentWorkflow: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["AgentWorkflowId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["AdminAgentWorkflowMutationOk"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminAgentWorkflow: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["AgentWorkflowId"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminAgentWorkflowMutation"];
        responses: {
            200: components["responses"]["AdminAgentWorkflowMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    deleteAdminAgentWorkflow: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["AgentWorkflowId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeleteAgentWorkflowOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    patchAdminAgentWorkflow: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: components["parameters"]["AgentWorkflowId"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminAgentWorkflowMutation"];
        responses: {
            200: components["responses"]["AdminAgentWorkflowMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listAdminAgentSchedules: {
        parameters: {
            query?: {
                tenant?: string;
                workspace?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent schedule list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentScheduleList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminAgentSchedule: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminAgentScheduleMutation"];
            };
        };
        responses: {
            /** @description Agent schedule created. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentScheduleMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminAgentSchedule: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent schedule. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentScheduleMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminAgentSchedule: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminAgentScheduleMutation"];
            };
        };
        responses: {
            /** @description Agent schedule replaced. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentScheduleMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    deleteAdminAgentSchedule: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent schedule deleted. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    patchAdminAgentSchedule: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminAgentScheduleMutation"];
            };
        };
        responses: {
            /** @description Agent schedule updated. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentScheduleMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    runAdminAgentScheduleNow: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Manual fire recorded. */
            202: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentScheduleRunNowResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listAdminAgentScheduleFires: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Agent schedule fire history. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentScheduleFireList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listAdminPromptTemplates: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Prompt template list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPromptTemplateList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminPromptTemplate: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminPromptTemplateMutation"];
        responses: {
            201: components["responses"]["AdminPromptTemplateMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminPromptTemplate: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["AdminPromptTemplateMutationOk"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminPromptTemplate: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminPromptTemplateMutation"];
        responses: {
            200: components["responses"]["AdminPromptTemplateMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    archiveAdminPromptTemplate: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeletePromptTemplateOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    patchAdminPromptTemplate: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminPromptTemplateMutation"];
        responses: {
            200: components["responses"]["AdminPromptTemplateMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listAdminPromptTemplateLabels: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deployment labels on this template, sorted by label. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPromptTemplateLabelList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminPromptTemplateLabel: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
                /** @description Deployment label name, URL-decoded and normalized before use. An illegal name is 400 `invalid_prompt_label` — an operator who typed `Production!` needs to be told the name is illegal, not sent looking for a missing resource. */
                label: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminPromptTemplateLabelMutation"];
            };
        };
        responses: {
            /** @description The stored label record. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPromptTemplateLabelMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    deleteAdminPromptTemplateLabel: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
                /** @description Deployment label name, URL-decoded and normalized before use. An illegal name is 400 `invalid_prompt_label` — an operator who typed `Production!` needs to be told the name is illegal, not sent looking for a missing resource. */
                label: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Label retirement acknowledgement. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeletePromptTemplateLabelResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    renderPromptTemplate: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Stable prompt template id. */
                id: components["parameters"]["PromptTemplateId"];
            };
            cookie?: never;
        };
        requestBody?: components["requestBodies"]["PromptTemplateRenderRequest"];
        responses: {
            /** @description Rendered OpenAI-compatible chat or Responses request body. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RenderedPromptRequest"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listAgentSkills: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Visible skill packages. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentSkillPackageList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getAgentSkill: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Visible skill package. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentSkillPackage"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listAdminAgentUpstreams: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Registered agent upstreams. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentUpstreamList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminAgentUpstream: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminAgentUpstreamMutationRequest"];
        responses: {
            201: components["responses"]["AdminAgentUpstreamMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
        };
    };
    getAdminAgentUpstream: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["AdminAgentUpstreamMutationOk"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminAgentUpstream: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminAgentUpstreamMutationRequest"];
        responses: {
            200: components["responses"]["AdminAgentUpstreamMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    deleteAdminAgentUpstream: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeleteAgentUpstreamResponse"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    patchAdminAgentUpstream: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminAgentUpstreamMutationRequest"];
        responses: {
            200: components["responses"]["AdminAgentUpstreamMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    getAgentDiscovery: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Visible agent upstreams. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AgentUpstreamDiscoveryList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminSkillPackages: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Registered skill packages. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSkillPackageList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createAdminSkillPackage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminSkillPackageMutation"];
        responses: {
            201: components["responses"]["AdminSkillPackageMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
        };
    };
    getAdminSkillPackage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["AdminSkillPackageMutationOk"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    putAdminSkillPackage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminSkillPackageMutation"];
        responses: {
            200: components["responses"]["AdminSkillPackageMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    deleteAdminSkillPackage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeleteSkillPackageResponse"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    patchAdminSkillPackage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: components["requestBodies"]["AdminSkillPackageMutation"];
        responses: {
            200: components["responses"]["AdminSkillPackageMutationOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    listAdminManagedWorkerSessions: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated managed worker session list backed by storage records. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminManagedWorkerSessionList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    recordAdminSelfHostedWorkerArtifact: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSelfHostedWorkerArtifactRequest"];
            };
        };
        responses: {
            /** @description Recorded self-hosted worker artifact metadata and updated worker projection. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerArtifactResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    recordAdminSelfHostedWorkerCheckpoint: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSelfHostedWorkerCheckpointRequest"];
            };
        };
        responses: {
            /** @description Recorded self-hosted worker checkpoint metadata and updated worker projection. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerCheckpointResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listGuardrailPolicyRevisions: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Guardrail policy revision list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyRevisionList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createGuardrailPolicyRevision: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GuardrailPolicyRevisionMutation"];
            };
        };
        responses: {
            /** @description Draft revision created. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyRevisionResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    listGuardrailPolicyRevisionsByPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                policy_id: components["parameters"]["GuardrailPolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Guardrail policy revision list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyRevisionList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listGuardrailPolicyRevisionHistory: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                policy_id: components["parameters"]["GuardrailPolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Guardrail policy revision history. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyRevisionList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    createNextGuardrailPolicyRevision: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                policy_id: components["parameters"]["GuardrailPolicyId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GuardrailPolicyRevisionMutation"];
            };
        };
        responses: {
            /** @description Draft revision created. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyRevisionResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getGuardrailPolicyRevision: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                policy_id: components["parameters"]["GuardrailPolicyId"];
                revision: components["parameters"]["GuardrailPolicyRevision"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Exact policy revision. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyRevisionResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    archiveGuardrailPolicyRevision: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                policy_id: components["parameters"]["GuardrailPolicyId"];
                revision: components["parameters"]["GuardrailPolicyRevision"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revision archived and runtime reloaded. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    activateGuardrailPolicyRevision: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                policy_id: components["parameters"]["GuardrailPolicyId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GuardrailPolicyRevisionSelection"];
            };
        };
        responses: {
            /** @description Revision activated. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyBindingResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    rollbackGuardrailPolicyRevision: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                policy_id: components["parameters"]["GuardrailPolicyId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GuardrailPolicyRollbackSelection"];
            };
        };
        responses: {
            /** @description Revision rolled back. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyBindingResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    dryRunGuardrailPolicyRevision: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                policy_id: components["parameters"]["GuardrailPolicyId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GuardrailPolicyDryRunRequest"];
            };
        };
        responses: {
            /** @description Sanitized dry-run plan. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GuardrailPolicyDryRunResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    rotateAdminSelfHostedWorkerIdentity: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSelfHostedWorkerRotateRequest"];
            };
        };
        responses: {
            /** @description Rotated self-hosted worker identity fingerprint and returned the updated worker projection. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSelfHostedWorkerRotateResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getAdminDashboardSlash: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Serve the Admin dashboard alias. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    getAdminDashboardAlias: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Serve the Admin dashboard alias. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    listModels: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List enabled models. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OpenAiModelList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getModel: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                model: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Describe one enabled model. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OpenAiModel"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    createChatCompletion: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AiRequest"];
            };
        };
        responses: {
            /** @description Create an OpenAI-compatible chat completion. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createResponse: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AiRequest"];
            };
        };
        responses: {
            /** @description Create an OpenAI-compatible response. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createEmbedding: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["EmbeddingsRequest"];
            };
        };
        responses: {
            /** @description Create OpenAI-compatible embeddings for one or more inputs. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createRerank: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RerankRequest"];
            };
        };
        responses: {
            /** @description Ranked results, in the order the provider ranked them. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createMessage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    [key: string]: unknown;
                };
            };
        };
        responses: {
            /** @description Create an Anthropic-native Messages completion (streaming or non-streaming). */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    countMessageTokens: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    [key: string]: unknown;
                };
            };
        };
        responses: {
            /** @description The input-token count for the supplied Messages request. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @description Prompt tokens the request would be charged. */
                        input_tokens: number;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createImage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    [key: string]: unknown;
                };
            };
        };
        responses: {
            /** @description Generate images from a text prompt (OpenAI-compatible; billed per generated image). */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description The resolved model's provider family does not support image generation. */
            422: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    listTools: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List tools visible to the authenticated tenant. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    executeTool: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ToolExecutionRequest"];
            };
        };
        responses: {
            /** @description Execute an extension tool. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ToolExecutionResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    executeFunction: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ToolExecutionRequest"];
            };
        };
        responses: {
            /** @description Execute a function through the fail-closed egress broker. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    invokeAgent: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AiRequest"];
            };
        };
        responses: {
            /** @description Invoke a configured agent upstream. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    recordSelfHostedWorkerHeartbeat: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MachineTransportRequest"];
            };
        };
        responses: {
            /** @description Self-hosted worker heartbeat transport. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
        };
    };
    recordSelfHostedWorkerEvent: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MachineTransportRequest"];
            };
        };
        responses: {
            /** @description Self-hosted worker events transport. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
        };
    };
    uploadSelfHostedWorkerArtifact: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MachineTransportRequest"];
            };
        };
        responses: {
            /** @description Self-hosted worker artifacts transport. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
        };
    };
    uploadSelfHostedWorkerCheckpoint: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MachineTransportRequest"];
            };
        };
        responses: {
            /** @description Self-hosted worker checkpoints transport. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
        };
    };
    pollSelfHostedWorkerRun: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MachineTransportRequest"];
            };
        };
        responses: {
            /** @description Self-hosted worker runs/poll transport. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
        };
    };
    acknowledgeSelfHostedWorkerRun: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MachineTransportRequest"];
            };
        };
        responses: {
            /** @description Self-hosted worker runs/ack transport. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
        };
    };
    listTenantAccounts: {
        parameters: {
            query?: {
                /** @description Case-insensitive match against tenant ID, name, or slug. */
                search?: string;
                offset?: number;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List tenant accounts. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminTenantAccount"][];
                        total?: number;
                        offset?: number;
                        limit?: number;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createTenantAccount: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminTenantAccountCreateRequest"];
            };
        };
        responses: {
            /** @description Create a tenant account. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminTenantAccountMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getTenantAccount: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a tenant account. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminTenantAccountMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    replaceTenantAccount: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminTenantAccountCreateRequest"];
            };
        };
        responses: {
            /** @description Replace a tenant account. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminTenantAccountMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    updateTenantAccount: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminTenantAccountCreateRequest"];
            };
        };
        responses: {
            /** @description Update a tenant account. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminTenantAccountMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getTenantResolvedDefaults: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Read effective plan and quota defaults. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminTenantResolvedDefaults"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    assignTenantPlan: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminTenantPlanAssignmentRequest"];
            };
        };
        responses: {
            /** @description Assign a subscription plan to a tenant. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminTenantAccountMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listProjects: {
        parameters: {
            query?: {
                /** @description Case-insensitive match against project ID, name, or slug. */
                search?: string;
                /** @description Restrict projects to one tenant. */
                tenant_id?: string;
                offset?: number;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List projects. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminProject"][];
                        total?: number;
                        offset?: number;
                        limit?: number;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createProject: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminProjectCreateRequest"];
            };
        };
        responses: {
            /** @description Create a project. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProjectMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getProject: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                project_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a project. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProjectMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    replaceProject: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                project_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminProjectCreateRequest"];
            };
        };
        responses: {
            /** @description Replace a project. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProjectMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    deleteProject: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                project_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeleteProjectOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    updateProject: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                project_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminProjectCreateRequest"];
            };
        };
        responses: {
            /** @description Update a project. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProjectMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listWorkspaces: {
        parameters: {
            query?: {
                /** @description Case-insensitive match against workspace ID, name, or slug. */
                search?: string;
                /** @description Restrict workspaces to one tenant. */
                tenant_id?: string;
                /** @description Restrict workspaces to one project. */
                project_id?: string;
                offset?: number;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List workspaces. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminWorkspace"][];
                        total?: number;
                        offset?: number;
                        limit?: number;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createWorkspace: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminWorkspaceCreateRequest"];
            };
        };
        responses: {
            /** @description Create a workspace. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWorkspaceMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getWorkspace: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                workspace_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a workspace. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWorkspaceMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    replaceWorkspace: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                workspace_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminWorkspaceCreateRequest"];
            };
        };
        responses: {
            /** @description Replace a workspace. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWorkspaceMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    deleteWorkspace: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                workspace_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            200: components["responses"]["DeleteWorkspaceOk"];
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    updateWorkspace: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                workspace_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminWorkspaceCreateRequest"];
            };
        };
        responses: {
            /** @description Update a workspace. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWorkspaceMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listVirtualKeys: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List durable virtual API keys. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminVirtualApiKey"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createVirtualKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminVirtualApiKeyCreateRequest"];
            };
        };
        responses: {
            /** @description Create a durable virtual API key. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminVirtualApiKeyMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getVirtualKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                key_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a virtual API key. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminVirtualApiKeyMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    revokeVirtualKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                key_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revoke a virtual API key. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listQuotaPolicies: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List quota policies. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminQuotaPolicy"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createQuotaPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminQuotaPolicyMutation"];
            };
        };
        responses: {
            /** @description Create a quota policy. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminQuotaPolicyMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getQuotaPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                scope_type: string;
                scope_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a scoped quota policy. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminQuotaPolicyMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    replaceQuotaPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                scope_type: string;
                scope_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminQuotaPolicyMutation"];
            };
        };
        responses: {
            /** @description Replace a scoped quota policy. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminQuotaPolicyMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    deleteQuotaPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                scope_type: string;
                scope_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Delete a scoped quota policy. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    updateQuotaPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                scope_type: string;
                scope_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminQuotaPolicyMutation"];
            };
        };
        responses: {
            /** @description Update a scoped quota policy. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminQuotaPolicyMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listSemanticCachePolicies: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Semantic cache policy listing. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSemanticCachePolicyList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createSemanticCachePolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSemanticCachePolicyMutation"];
            };
        };
        responses: {
            /** @description The created policy. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSemanticCachePolicyMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    getSemanticCachePolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Scope kind. `tenant` is the only kind accepted today; anything else answers 404, like a route miss. */
                scope_type: "tenant";
                /** @description Scope identifier. Resolved to its owning tenant before a tenant-scoped caller may touch it; resolution failure denies. */
                scope_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The stored policy. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSemanticCachePolicyMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    replaceSemanticCachePolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Scope kind. `tenant` is the only kind accepted today; anything else answers 404, like a route miss. */
                scope_type: "tenant";
                /** @description Scope identifier. Resolved to its owning tenant before a tenant-scoped caller may touch it; resolution failure denies. */
                scope_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSemanticCachePolicyMutation"];
            };
        };
        responses: {
            /** @description The replaced policy. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSemanticCachePolicyMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
        };
    };
    deleteSemanticCachePolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Scope kind. `tenant` is the only kind accepted today; anything else answers 404, like a route miss. */
                scope_type: "tenant";
                /** @description Scope identifier. Resolved to its owning tenant before a tenant-scoped caller may touch it; resolution failure denies. */
                scope_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deletion acknowledgement. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteSemanticCachePolicyResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    invalidateSemanticCachePolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Scope kind. `tenant` is the only kind accepted today; anything else answers 404, like a route miss. */
                scope_type: "tenant";
                /** @description Scope identifier. Resolved to its owning tenant before a tenant-scoped caller may touch it; resolution failure denies. */
                scope_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The invalidation epoch now in force. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSemanticCacheInvalidationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    listPlans: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List subscription plans. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminPlan"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createPlan: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminPlanMutation"];
            };
        };
        responses: {
            /** @description Create a subscription plan. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPlanMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getPlan: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                plan_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a subscription plan. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPlanMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    replacePlan: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                plan_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminPlanMutation"];
            };
        };
        responses: {
            /** @description Replace a subscription plan. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPlanMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    updatePlan: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                plan_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminPlanMutation"];
            };
        };
        responses: {
            /** @description Update a subscription plan. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPlanMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listWallets: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List tenant wallets. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminWallet"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createWallet: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminWalletMutation"];
            };
        };
        responses: {
            /** @description Create a tenant wallet. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWalletMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getWallet: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a tenant wallet. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWalletMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    updateWallet: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminWalletMutation"];
            };
        };
        responses: {
            /** @description Update wallet recharge settings. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWalletMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    adjustWallet: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminWalletAdjustRequest"];
            };
        };
        responses: {
            /** @description Atomically adjust wallet credits. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWalletMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    chargeWallet: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminWalletChargeRequest"];
            };
        };
        responses: {
            /** @description Charge a payment method and credit a wallet. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminWalletChargeResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listWalletLedger: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List wallet ledger entries. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listPaymentMethods: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List tenant payment methods. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminPaymentMethod"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createPaymentMethod: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminPaymentMethodCreateRequest"];
            };
        };
        responses: {
            /** @description Create a tenant payment method. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPaymentMethodMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    deletePaymentMethod: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                payment_method_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Delete a payment method. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAssets: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List tenant assets. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AssetSummary"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    listWithheldAssets: {
        parameters: {
            query?: {
                /** @description Optional asset family filter (cli_tool, mcp_manifest, skill_bundle, static_site, config_file, or a custom family). */
                asset_type?: string;
                /** @description Case-insensitive match against asset id, name, version, asset_type, or visibility. */
                search?: string;
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List withheld (pending_scan/quarantined) assets for operators. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["WithheldAssetSummary"][];
                        total?: number;
                        offset?: number;
                        limit?: number;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    listAssetsByType: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List tenant assets by type. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AssetSummary"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    getAsset: {
        parameters: {
            query?: {
                /** @description Platform/architecture variant. Required when resolution is otherwise ambiguous. */
                platform?: string;
                /** @description Serve ONE file out of an expanded static_site bundle version (issue #736), e.g. `assets/app.css`. Bundle-relative; absolute paths and `..` segments are refused with 400 `asset_bundle_path_invalid`, and a path the bundle does not contain answers 404 `asset_bundle_file_not_found`. This does NOT change how the version is resolved: the `version` path segment is still resolved through channels, semver ranges and yank exactly as it is without it, and only the bytes served differ. Omit it to download the version's archive itself. */
                path?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
                /** @description Platform fallback when the platform query parameter is omitted. */
                "x-ferrogate-platform"?: string;
                Range?: string;
                "If-None-Match"?: string;
                "If-Modified-Since"?: string;
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Exact version, channel name, or semver range to resolve to a concrete version. */
                version: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Complete resolved asset bytes. Content-Type is the stored asset content type. */
            200: {
                headers: {
                    /** @description Strong SHA-256 entity validator for the resolved object. */
                    ETag?: string;
                    /** @description Last asset update time in HTTP-date form. */
                    "Last-Modified"?: string;
                    /** @description Tenant-private revalidation policy. */
                    "Cache-Control"?: string;
                    /** @description Advertises single byte-range support. */
                    "Accept-Ranges"?: "bytes";
                    /** @description How the requested reference resolved to a concrete version. */
                    "x-ferrogate-asset-resolved"?: string;
                    /** @description Concrete version selected by exact, channel, or semver resolution. */
                    "x-ferrogate-asset-version"?: string;
                    /** @description Selected platform variant when the asset is variant-specific. */
                    "x-ferrogate-asset-variant"?: string;
                    /** @description Present with value true when the resolved version is yanked. */
                    "x-ferrogate-asset-yanked"?: "true";
                    /** @description RFC 7234 warning emitted when serving a yanked version. */
                    Warning?: string;
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "*/*": string;
                };
            };
            /** @description Requested byte range of the resolved asset. */
            206: {
                headers: {
                    /** @description Strong SHA-256 entity validator for the resolved object. */
                    ETag?: string;
                    /** @description Last asset update time in HTTP-date form. */
                    "Last-Modified"?: string;
                    /** @description Tenant-private revalidation policy. */
                    "Cache-Control"?: string;
                    /** @description Advertises single byte-range support. */
                    "Accept-Ranges"?: "bytes";
                    /** @description How the requested reference resolved to a concrete version. */
                    "x-ferrogate-asset-resolved"?: string;
                    /** @description Concrete version selected by exact, channel, or semver resolution. */
                    "x-ferrogate-asset-version"?: string;
                    /** @description Selected platform variant when the asset is variant-specific. */
                    "x-ferrogate-asset-variant"?: string;
                    /** @description Present with value true when the resolved version is yanked. */
                    "x-ferrogate-asset-yanked"?: "true";
                    /** @description RFC 7234 warning emitted when serving a yanked version. */
                    Warning?: string;
                    /** @description Inclusive range and complete representation size. */
                    "Content-Range"?: string;
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "*/*": string;
                };
            };
            /** @description The supplied validator matches; no response body is sent. */
            304: {
                headers: {
                    /** @description Strong SHA-256 entity validator for the resolved object. */
                    ETag?: string;
                    /** @description Last asset update time in HTTP-date form. */
                    "Last-Modified"?: string;
                    /** @description Tenant-private revalidation policy. */
                    "Cache-Control"?: string;
                    /** @description Advertises single byte-range support. */
                    "Accept-Ranges"?: "bytes";
                    /** @description How the requested reference resolved to a concrete version. */
                    "x-ferrogate-asset-resolved"?: string;
                    /** @description Concrete version selected by exact, channel, or semver resolution. */
                    "x-ferrogate-asset-version"?: string;
                    /** @description Selected platform variant when the asset is variant-specific. */
                    "x-ferrogate-asset-variant"?: string;
                    /** @description Present with value true when the resolved version is yanked. */
                    "x-ferrogate-asset-yanked"?: "true";
                    /** @description RFC 7234 warning emitted when serving a yanked version. */
                    Warning?: string;
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["AssetBadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            /** @description The requested byte range is outside the asset body; the runtime sends no JSON body. */
            416: {
                headers: {
                    /** @description Unsatisfied range in bytes *\/{complete-length} form. */
                    "Content-Range"?: string;
                    /** @description Strong SHA-256 entity validator for the resolved object. */
                    ETag?: string;
                    /** @description Last asset update time in HTTP-date form. */
                    "Last-Modified"?: string;
                    /** @description Tenant-private revalidation policy. */
                    "Cache-Control"?: string;
                    /** @description Advertises single byte-range support. */
                    "Accept-Ranges"?: "bytes";
                    /** @description How the requested reference resolved to a concrete version. */
                    "x-ferrogate-asset-resolved"?: string;
                    /** @description Concrete version selected by exact, channel, or semver resolution. */
                    "x-ferrogate-asset-version"?: string;
                    /** @description Selected platform variant when the asset is variant-specific. */
                    "x-ferrogate-asset-variant"?: string;
                    /** @description Present with value true when the resolved version is yanked. */
                    "x-ferrogate-asset-yanked"?: "true";
                    /** @description RFC 7234 warning emitted when serving a yanked version. */
                    Warning?: string;
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content?: never;
            };
            429: components["responses"]["AssetTooManyRequests"];
            500: components["responses"]["AssetInternalServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    putAsset: {
        parameters: {
            query?: {
                /** @description Platform/architecture variant stored for this logical version. */
                platform?: string;
                /** @description Move this channel to the new version after the durable publish succeeds. */
                channel?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
                /** @description Detached minisign file or base64 Ed25519 signature material. */
                "x-asset-signature"?: string;
                /** @description Detached signature encoding; defaults to minisign. */
                "x-asset-signature-format"?: "minisign" | "ed25519" | "cosign";
                /** @description Publisher key hint for bare Ed25519 signatures. */
                "x-asset-signature-key-id"?: string;
                /** @description Publish visibility. Cross-tenant values require a durable approval record. */
                "x-asset-visibility"?: "private" | "tenant" | "tenant_private" | "shared" | "cross_tenant" | "cross_tenant_shared" | "public";
                /** @description Durable tool-approval record used for cross-tenant publication. */
                "x-asset-approval-id"?: string;
                /** @description static_site bundle publish only: opt the served site into public (unauthenticated) access. Truthy values are 'true', '1', or 'yes' (case-insensitive); any other value or an absent header keeps the site tenant-private. */
                "x-site-public"?: string;
                /** @description static_site bundle publish only: serve index.html for unmatched paths (single-page-app history fallback). Same truthy encoding as x-site-public; absent or non-truthy disables the fallback. */
                "x-site-spa-fallback"?: string;
                /** @description static_site bundle publish only: override the Cache-Control header the gateway emits when serving this bundle's files. Absent or empty falls back to the default 'public, max-age=300'. */
                "x-site-cache-control"?: string;
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        /** @description Raw asset bytes. The request Content-Type becomes the stored content type; it defaults to application/octet-stream when absent. */
        requestBody: {
            content: {
                "*/*": string;
            };
        };
        responses: {
            /** @description Published asset metadata including authoritative size and SHA-256. The push screened clean: asset.visibility is visible and the asset is serving now. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetMutationResponse"];
                };
            };
            /** @description Stored but WITHHELD (#528). The version is durably claimed and immutable, yet the screening verdict was not clean: asset.visibility is pending_scan (a deferred or out-of-band scan has not completed) or quarantined, so the asset is absent from every list, manifest, resolution, and download surface until it is promoted via POST /v1/assets/{asset_type}/{name}/{version}/visibility. Do not report a successful publication on this status; branch on it, or read asset.visibility. */
            202: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetMutationResponse"];
                };
            };
            400: components["responses"]["AssetBadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["AssetConflict"];
            413: components["responses"]["AssetPayloadTooLarge"];
            422: components["responses"]["AssetUnprocessableEntity"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    deleteAsset: {
        parameters: {
            query?: {
                /** @description Platform/architecture variant to delete. */
                platform?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted asset identity. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    listPermissions: {
        parameters: {
            query?: {
                /** @description Case-insensitive match against permission ID, key, name, or description. */
                search?: string;
                offset?: number;
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List DB-backed permission actions. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminPermission"][];
                        total?: number;
                        offset?: number;
                        limit?: number;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createPermission: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminPermissionMutation"];
            };
        };
        responses: {
            /** @description Create a DB-backed permission action. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPermissionMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getPermission: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                permission_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a permission action. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPermissionMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    deletePermission: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                permission_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Delete a permission action. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listRoles: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List DB-backed roles. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminRole"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    createRole: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminRoleMutation"];
            };
        };
        responses: {
            /** @description Create a role from permission action keys. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminRoleMutationResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getRole: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                role_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Get a DB-backed role. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminRoleMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    deleteRole: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                role_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Delete a DB-backed role. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listTenantRoles: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List role bindings for a tenant. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminTenantRoleBinding"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    bindTenantRole: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminTenantRoleBindingRequest"];
            };
        };
        responses: {
            /** @description Bind a DB-backed role to a tenant. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminTenantRoleBinding"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    unbindTenantRole: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                tenant_id: string;
                role_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Remove a tenant role binding. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listUsageReports: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Read usage and cost report rows. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        object: "list";
                        data: components["schemas"]["AdminUsageReportRow"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listBillingOutboxDeadLetters: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List dead-lettered billing outbox records. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    replayBillingOutboxDeadLetter: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                report_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Replay a dead-lettered billing outbox record. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminBillingOutboxReplayResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    rotateVirtualKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                key_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Rotate a virtual API key. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    enableVirtualKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                key_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Enable a virtual API key. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    disableVirtualKey: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                key_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Disable a virtual API key. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    revokeVirtualKeyAction: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                key_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revoke a virtual API key. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    sendAgentMessage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AiRequest"];
            };
        };
        responses: {
            /** @description Agent upstream response. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    streamAgentMessage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                name: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AiRequest"];
            };
        };
        responses: {
            /** @description Streaming agent upstream response. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listSiteDomains: {
        parameters: {
            query?: {
                tenant?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Site domain binding list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSiteDomainList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    bindSiteDomain: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSiteDomainMutation"];
            };
        };
        responses: {
            /** @description Existing servable binding for the hostname updated (re-bound within the same tenant). Still serving; ACME enrollment still requires a live `verified` ownership proof, not a `grandfathered` migration record. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSiteDomainResponse"];
                };
            };
            /** @description Site domain bound and serving immediately. Only a live `verified` ownership proof enrolls the hostname in the ACME order set; a `grandfathered` migration record may keep serving but is not certificate-issuance proof. */
            201: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSiteDomainResponse"];
                };
            };
            /** @description Binding recorded but NOT serving. Ownership is unproven, so the gateway deliberately keeps the hostname out of the ACME order set (no certificate is ordered) and it will not answer traffic until POST /admin/v1/site-domains/{hostname}/verify succeeds. Clients must branch on this: it is a 2xx that does not mean the domain is live. Read `site_domain.serving` / `verification.state` rather than inferring from the status code. */
            202: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSiteDomainResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    getSiteDomain: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                hostname: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Site domain binding. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSiteDomainResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    unbindSiteDomain: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                hostname: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Site domain unbound. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    verifySiteDomain: {
        parameters: {
            query?: {
                /** @description Whose challenge to redeem. Required for a platform-operator key; a tenant-scoped key is always pinned to its own tenant. */
                tenant?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                hostname: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Ownership verified; the hostname now serves. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSiteDomainResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    getAssetStorageSummary: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Authoritative storage usage, quota, and transfer constraints. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetStorageSummary"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    getAssetManifest: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Typed asset registry manifest. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetManifest"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    listAssetChannels: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Asset channel list. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetChannelListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    putAssetChannel: {
        parameters: {
            query: {
                /** @description Existing, non-yanked concrete version that the channel will target. */
                version: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Channel pointer to create or move. */
                channel: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Durably stored channel pointer. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetChannelMutationResponse"];
                };
            };
            400: components["responses"]["AssetBadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    deleteAssetChannel: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Channel pointer to delete. */
                channel: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted channel identity. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    yankAssetVersion: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Updated variant summaries. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetYankMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    unyankAssetVersion: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Updated variant summaries. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetYankMutationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    promoteAssetVisibility: {
        parameters: {
            query?: {
                /** @description Platform/arch variant key (issue #260), e.g. linux-x86_64. Omit for the default no-variant artifact. */
                platform?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AssetVisibilityPromotionRequest"];
            };
        };
        responses: {
            /** @description The asset was promoted to its terminal visibility. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetVisibilityPromotionResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["Conflict"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    createAssetUploadIntent: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        /** @description Declared byte length and SHA-256 of the bytes that will be PUT directly to the returned URL. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["AssetPresignUploadIntentRequest"];
            };
        };
        responses: {
            /** @description Presigned direct-upload intent bound to one unique staging upload. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetPresignUploadIntentResponse"];
                };
            };
            400: components["responses"]["AssetBadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["AssetConflict"];
            413: components["responses"]["AssetPayloadTooLarge"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    commitAssetUpload: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        /** @description The upload_id returned by the intent plus its registered size/checksum and the content type to persist after verification. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["AssetPresignCommitRequest"];
            };
        };
        responses: {
            /** @description Verified committed asset metadata. The commit screened clean: asset.visibility is visible and the asset is serving now. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetMutationResponse"];
                };
            };
            /** @description Verified and stored, but WITHHELD (#528). The bytes are durable and the version is immutable, yet asset.visibility is pending_scan or quarantined, so the asset is absent from every list, manifest, resolution, and download surface until promoted via POST /v1/assets/{asset_type}/{name}/{version}/visibility. This is the ordinary terminal for a large object under a configured async scan threshold or an out-of-process scanner that never saw the streamed bytes. Do not report a successful publication on this status; branch on it, or read asset.visibility. */
            202: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetMutationResponse"];
                };
            };
            400: components["responses"]["AssetBadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["AssetConflict"];
            413: components["responses"]["AssetPayloadTooLarge"];
            422: components["responses"]["AssetUnprocessableEntity"];
            503: components["responses"]["AssetServiceUnavailable"];
        };
    };
    abortAssetUpload: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        /** @description The upload_id returned by the intent plus the size and checksum it registered, and optionally why it is being released. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["AssetPresignAbortRequest"];
            };
        };
        responses: {
            /** @description The recorded release outcome for this upload intent. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetPresignAbortResponse"];
                };
            };
            400: components["responses"]["AssetBadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["AssetConflict"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    getAssetDownloadUrl: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Asset family. Built-in families include cli_tool, mcp_manifest, skill_bundle, static_site, and config_file. */
                asset_type: string;
                /** @description Tenant-scoped asset name. */
                name: string;
                /** @description Concrete immutable asset version. */
                version: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Presigned direct-download metadata. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AssetPresignDownloadResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AssetNotFound"];
            405: components["responses"]["MethodNotAllowed"];
            409: components["responses"]["AssetConflict"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    listAdminObservedAgentActivity: {
        parameters: {
            query?: {
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated observed unattributed virtual-API-key activity rows. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminObservedAgentActivityList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAdminAgentCostBurn: {
        parameters: {
            query?: {
                /** @description Billing window as YYYY-MM (UTC). Defaults to the current month when omitted or malformed. */
                period?: string;
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paginated durable per-agent cost-burn rows for the period, biggest accumulated total first. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminAgentCostBurnList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    listAdminCostRecords: {
        parameters: {
            query?: {
                /** @description Narrow to one tenant id. AND-ed with the caller's tenant fence rather than replacing it, so a tenant-scoped caller asking for another tenant gets the empty set. */
                tenant?: string;
                /** @description Narrow to one project id. */
                project?: string;
                /** @description Narrow to one workspace id. */
                workspace?: string;
                /** @description Narrow to the API key that authenticated the request. */
                api_key_id?: string;
                /** @description Narrow to one agent run, so an agent's spend can be totalled across its requests. */
                agent_run_id?: string;
                /** @description Narrow to one upstream provider id. */
                provider?: string;
                /** @description Matches EITHER spelling of the model -- the logical name the caller asked for or the provider model actually invoked -- because matching only one would silently drop half of that model's spend. */
                model?: string;
                /** @description Unix seconds, INCLUSIVE lower bound on the request start. */
                since?: number;
                /** @description Unix seconds, EXCLUSIVE upper bound on the request start, so two adjacent billing periods partition the traffic instead of both claiming the boundary second. */
                until?: number;
                /** @description Attribution metadata filter. `key` matches the presence of the key; `key:value` matches the pair, and only the FIRST colon splits, so a value may itself contain colons (owner:svc:checkout is owner -> svc:checkout). */
                tag?: string;
                offset?: components["parameters"]["Offset"];
                /** @description Clamped by storage.admin_list_max_limit. */
                limit?: components["parameters"]["Limit"];
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One page of per-request cost records, ordered by start time descending with the request id as a stable tiebreaker. `total` counts the records the caller may see after the tenant fence and the filters. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedList_CostRecord"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    exportAdminCostRecords: {
        parameters: {
            query?: {
                /** @description Export encoding. An unrecognised value is refused with 400 rather than silently falling back to CSV, so a pipeline that asked for Parquet never parses bytes as something they are not. */
                format?: "csv" | "jsonl" | "parquet";
                /** @description Narrow to one tenant id. AND-ed with the caller's tenant fence rather than replacing it. */
                tenant?: string;
                /** @description Narrow to one project id. */
                project?: string;
                /** @description Narrow to one workspace id. */
                workspace?: string;
                /** @description Narrow to the API key that authenticated the request. */
                api_key_id?: string;
                /** @description Narrow to one agent run. */
                agent_run_id?: string;
                /** @description Narrow to one upstream provider id. */
                provider?: string;
                /** @description Matches either the logical model or the provider model actually invoked. */
                model?: string;
                /** @description Unix seconds, INCLUSIVE lower bound on the request start. */
                since?: number;
                /** @description Unix seconds, EXCLUSIVE upper bound on the request start. */
                until?: number;
                /** @description Attribution metadata filter: `key` or `key:value`, split on the first colon. */
                tag?: string;
                offset?: components["parameters"]["Offset"];
                /** @description Rows in this export. Larger default than the list because a finance pull wants a month, and bounded because the whole file is built in memory before it is written. */
                limit?: number;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The export file. Exactly ONE of the three media types below is returned, selected by `?format=`; the body is a download rather than an inline document, so content-disposition names the file. */
            200: {
                headers: {
                    /** @description Always `attachment` with a filename -- ferrogate-cost-records.csv, .jsonl or .parquet, matching `?format=`. */
                    "content-disposition"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "text/csv": string;
                    "application/x-ndjson": string;
                    "application/vnd.apache.parquet": string;
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listX402SpendPolicies: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The declarations visible to the caller. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["X402DeclaredSpendPolicyList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
        };
    };
    getEffectiveX402SpendPolicy: {
        parameters: {
            query: {
                /** @description Tenant (organization) id. Required. */
                tenant_id: string;
                project_id?: string;
                workspace_id?: string;
                key_id?: string;
                /** @description Agent-run id. Platform-operator keys only. */
                run_id?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The effective policy, its revision, and the inheritance chain. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["X402EffectiveSpendPolicy"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
        };
    };
    evaluateX402SpendPolicy: {
        parameters: {
            query?: never;
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["X402SpendPolicyEvaluationRequest"];
            };
        };
        responses: {
            /** @description The dry-run decision and the policy that produced it. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["X402SpendPolicyEvaluation"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            413: components["responses"]["PayloadTooLarge"];
            /** @description The declared policy in force failed structural validation, so no decision was produced. */
            500: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    listPaymentAttempts: {
        parameters: {
            query: {
                /** @description Tenant (organization) id. Required -- an unscoped listing would be a cross-tenant scan. */
                tenant_id: string;
                /** @description Page size. Defaults to 50 and is clamped to 500; a non-positive or non-numeric value is a 400 rather than a silent default. */
                limit?: number;
                /** @description Opaque next_cursor from a previous page. A malformed cursor is a 400 rather than a silent restart from page one. */
                cursor?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One bounded page of the tenant's attempts. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPaymentAttemptList"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    getPaymentAttemptLinks: {
        parameters: {
            query?: {
                /** @description Owning tenant. Optional: defaults to the caller's own tenant, and may be omitted entirely by a platform-operator key. */
                tenant_id?: string;
            };
            header?: {
                /** @description Client-minted identifier of one operator action (issue #548). Sent by the FerroGate CLI on every request it issues, reads included, and identical across every page of an --all-pages walk and across retries of the same logical action. It is an identifier, not a claim: the client is the authority on it. Distinct from an idempotency key, which governs whether an effect may be applied twice. */
                "x-ferrogate-action-id"?: components["parameters"]["ClientActionIdHeader"];
                /** @description Client-asserted descriptor of where an action came from (issue #548), rendered as a v1 semicolon-delimited list of `cli`, `os`, `arch`, `context`, `cred` and `host` fields; `host` is present only when the operator sets FERROGATE_CLIENT_HOST_LABEL, and other optional fields are omitted rather than sent empty. Values are percent-encoded, so the blob is always printable ASCII and a value can never introduce a delimiter. Never carries credential material -- `cred` names the credential SOURCE (env:VAR, stdin, inline, none) and never the token. This is NOT the canonical_target_sha256 action fingerprint, which digests the target of a call rather than the client and is a sha256: digest; this one is not a digest at all. */
                "x-ferrogate-client-fingerprint"?: components["parameters"]["ClientFingerprintHeader"];
                /** @description The client's own clock, in seconds since the Unix epoch, as read on the machine that issued the request (issue #548). Client-asserted and untrusted: it must never be used as the event time, nor read by any authorization or ordering decision. Its only purpose is to be compared against the server-issued instant so client clock skew is measurable. */
                "x-ferrogate-client-clock-unverified"?: components["parameters"]["ClientClockUnverifiedHeader"];
                /** @description A short-lived, server-issued time token echoed verbatim by the client (issue #548), rendered as a v1 semicolon-delimited list of issued_at (unix seconds), ttl (seconds), action_id and sig fields. It is the authoritative client_sent_at: the client never fills that field from its own clock. A token outside its TTL, or presented with an action id other than the one it was issued for, is refused. The server also records its own receive time; the two together bound the action. */
                "x-ferrogate-time-token"?: components["parameters"]["ClientTimeTokenHeader"];
                /** @description An address the operator chose to disclose about the client (issue #548). Client-asserted, opt-in and trivially forged: it is stored and rendered as client-reported and must never be merged with the source IP the server observes, which is the authoritative record. */
                "x-ferrogate-client-reported-ip"?: components["parameters"]["ClientReportedIpHeader"];
            };
            path: {
                /** @description Payment attempt (idempotency) id. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The attempt with its linked reservation and settlement. */
            200: {
                headers: {
                    "x-ferrogate-time-token": components["headers"]["ClientTimeTokenResponseHeader"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminPaymentAttemptLinks"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            405: components["responses"]["MethodNotAllowed"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
}
