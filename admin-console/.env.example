# Base URL of the ferrogate-auth service (register/login/session endpoints).
VITE_AUTH_BASE_URL=http://localhost:8081

# Base URL of the gateway's Admin API (proxied resources: tenants, keys,
# quotas, providers, agents, logs, etc.).
VITE_GATEWAY_ADMIN_BASE_URL=http://localhost:8080
