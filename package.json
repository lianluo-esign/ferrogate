{
  "name": "ferrogate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "FerroGate AI Gateway \u2014 TypeScript on Cloudflare Workers (Bun + Hono + Zod + Wrangler).",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "apps/*",
    "e2e",
    "tools/sdk-conformance",
    "tools/generated-clients",
    "sdks/typescript"
  ],
  "scripts": {
    "generate": "bun tools/generated-clients/generate.mjs",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test",
    "test:sdk-python": "python3 -m unittest discover -s sdks/python",
    "check:sdk-packages": "bun tools/sdk-packaging/check.mjs",
    "test:e2e": "playwright test --config e2e/playwright.config.ts",
    "build": "bun run --filter '*' build",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.25.76",
    "@hono/zod-validator": "^0.4.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^4.20260702.1",
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.2",
    "typescript": "5.5.4",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
