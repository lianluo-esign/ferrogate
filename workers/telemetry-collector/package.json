{
  "name": "ferrogate-telemetry-collector",
  "version": "0.1.0",
  "private": true,
  "description": "FerroGate telemetry-collector Worker: the OTLP/JSON ingest endpoint Cloudflare does not provide, fanning telemetry into Analytics Engine + Workers Logs (issue #520).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "teardown": "wrangler delete"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.18.8",
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "5.5.4",
    "vitest": "4.1.10",
    "wrangler": "^4.114.0"
  }
}
