{
  "name": "ferrogate-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "FerroGate-hosted MCP server Worker: a tenant's own MCP tool surface stood up on Cloudflare Workers + Agents SDK (issue #409).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "teardown": "wrangler delete"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.0.5",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "0.0.109",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250620.0",
    "typescript": "5.5.4",
    "wrangler": "4.20.5"
  }
}
