{
  "name": "ferrogate-d1-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "FerroGate d1-proxy Worker: a native D1 binding fronted as a bearer-authenticated HTTP API for the atomic control-plane hot path (issue #450).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "teardown": "wrangler delete"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260702.1",
    "typescript": "5.5.4",
    "wrangler": "4.107.1"
  }
}
